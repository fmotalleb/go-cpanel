// Code generated by go-cpanel/tools/gen from the official
// cPanel & WHM OpenAPI documents. DO NOT EDIT.
// Source: https://api.docs.cpanel.net (cPanel & WHM version 138)

package uapi

import (
	"context"
	"encoding/json"
	"net/http"

	cpanel "github.com/fmotalleb/go-cpanel"
)

// AddonDomainAddaddondomainArgs are the parameters of the UAPI function `AddonDomain::addaddondomain`.
type AddonDomainAddaddondomainArgs struct {
	// The addon domain to create.
	//
	// This parameter is required.
	NewDomain string `cpanel:"newdomain"`

	// The label of the subdomain to create on the account's primary domain and park the addon domain on. This value may **not** contain an asterisk (`*`).
	//
	// This parameter is required.
	Subdomain string `cpanel:"subdomain"`

	// The addon domain's document root, given as a directory path relative to the account's home directory.
	//
	// This value defaults to the subdomain label beneath the account's `public_html` directory.
	Dir *string `cpanel:"dir,omitempty"`

	// Whether to remove the dot (`.`) characters from the `subdomain` value.
	// * `1` - Remove dots from the subdomain label.
	// * `0` - Do **not** remove dots from the subdomain label.
	//
	// Possible values: `0`, `1`.
	//
	// Defaults to `0`.
	DisallowDoT *int64 `cpanel:"disallowdot,omitempty"`

	// Whether to create the addon domain even when the domain fails the system's name, [DNS](https://go.cpanel.net/dnsrecords) and registration checks. When you pass this parameter, the system overwrites an existing DNS zone for the domain.
	//
	// The system still refuses to create a domain that another account owns.
	// * `1` - Skip the checks and overwrite an existing zone.
	// * `0` - Enforce the checks.
	//
	// Possible values: `0`, `1`.
	//
	// Defaults to `0`.
	Force *int64 `cpanel:"force,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// Addaddondomain calls the UAPI function `AddonDomain::addaddondomain` — Create an addon domain
//
// This function creates an addon domain on the cPanel account. It creates a
// subdomain of the account's primary domain and then parks the new domain on
// that subdomain.
//
// **Important:**
//
//   - The account's primary domain may **not** be a subdomain of the addon
//     domain that you create.
//   - You cannot create a wildcard addon domain.
//   - If the subdomain already exists on the account, the system reuses it
//     rather than failing.
//   - If the park step fails, the system removes the subdomain it created, so
//     that a failed call does not leave a partial addon domain behind.
//   - The system also creates an [FTP](https://go.cpanel.net/ftpaccounts)
//     account for the addon domain, which counts against the account's FTP
//     account limit.
//
// When you disable the [Web Server role](https://go.cpanel.net/serverroles#roles),
// the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 138.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/addondomain/addondomain-addaddondomain.md
func (c *AddonDomainClient) Addaddondomain(ctx context.Context, args *AddonDomainAddaddondomainArgs) (*cpanel.UAPIResult[AddonDomainAddaddondomainData], error) {
	return cpanel.UAPICall[AddonDomainAddaddondomainData](ctx, c.c, http.MethodGet, "AddonDomain", "addaddondomain", args)
}

// The created addon domain.
type AddonDomainAddaddondomainData struct {
	// The addon domain that the system created.
	Domain string `json:"domain"`
}

// AddonDomainDeladdondomainArgs are the parameters of the UAPI function `AddonDomain::deladdondomain`.
type AddonDomainDeladdondomainArgs struct {
	// The addon domain to remove.
	//
	// This parameter is required.
	Domain string `cpanel:"domain"`

	// The subdomain that the addon domain is parked on, given in the underscore-separated `subdomain_rootdomain` form that the `AddonDomain::listaddondomains` function returns in its `domainkey` field.
	//
	// This parameter is required.
	Subdomain string `cpanel:"subdomain"`

	// Whether to remove the dot (`.`) characters from the subdomain label when the system removes the subdomain.
	// * `1` - Remove dots from the subdomain label.
	// * `0` - Do **not** remove dots from the subdomain label.
	//
	// Possible values: `0`, `1`.
	//
	// Defaults to `0`.
	DisallowDoT *int64 `cpanel:"disallowdot,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// Deladdondomain calls the UAPI function `AddonDomain::deladdondomain` — Remove an addon domain
//
// This function removes an addon domain from the cPanel account. It unparks
// the addon domain and then removes the subdomain that the domain was parked
// on.
//
// **Important:**
//
//   - The `domain` and `subdomain` values must match each other. If the
//     subdomain does not correspond to the addon domain, the function fails.
//   - If other domains are still parked on the subdomain, the system unparks
//     the addon domain but leaves the subdomain in place, and says so in the
//     `messages` field.
//   - This function does **not** remove the addon domain's document root or
//     the files in it.
//
// When you disable the [Web Server role](https://go.cpanel.net/serverroles#roles),
// the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 138.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/addondomain/addondomain-deladdondomain.md
func (c *AddonDomainClient) Deladdondomain(ctx context.Context, args *AddonDomainDeladdondomainArgs) (*cpanel.UAPIResult[json.RawMessage], error) {
	return cpanel.UAPICall[json.RawMessage](ctx, c.c, http.MethodGet, "AddonDomain", "deladdondomain", args)
}

// AddonDomainListaddondomainsArgs are the parameters of the UAPI function `AddonDomain::listaddondomains`.
type AddonDomainListaddondomainsArgs struct {
	// A Perl regular expression. The system returns only the addon domains whose name matches this pattern, case-insensitively.
	Regex *string `cpanel:"regex,omitempty"`

	// Whether to include the `can_https_redirect` and `is_https_redirecting` fields in each entry.
	// * `1` - Include the HTTPS redirect status.
	// * `0` - Omit the HTTPS redirect status.
	//
	// Possible values: `0`, `1`.
	//
	// Defaults to `0`.
	ReturnHTTPSRedirectStatus *int64 `cpanel:"return_https_redirect_status,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// Listaddondomains calls the UAPI function `AddonDomain::listaddondomains` — List the addon domains
//
// This function lists the cPanel account's addon domains: the domains that
// are parked on a subdomain of the account's primary domain, rather than on
// the primary domain itself.
//
// **Note:**
//
// Use the `Park::listparkeddomains` function for the domains parked on the
// primary domain.
//
// Available since cPanel & WHM version cPanel 138.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/addondomain/addondomain-listaddondomains.md
func (c *AddonDomainClient) Listaddondomains(ctx context.Context, args *AddonDomainListaddondomainsArgs) (*cpanel.UAPIResult[[]AddonDomainListaddondomainsDataItem], error) {
	return cpanel.UAPICall[[]AddonDomainListaddondomainsDataItem](ctx, c.c, http.MethodGet, "AddonDomain", "listaddondomains", args)
}

// AddonDomainListaddondomainsDataItem is a generated payload type.
type AddonDomainListaddondomainsDataItem struct {
	// The addon domain's document root, relative to the account's home directory.
	Basedir string `json:"basedir"`

	// Whether the system can redirect the addon domain to `HTTPS`. The system only returns this field when you pass the `return_https_redirect_status` parameter.
	// * `1` - The system can redirect the addon domain.
	// * `0` - The system cannot redirect the addon domain.
	//
	// Possible values: `0`, `1`.
	CanHTTPSRedirect int64 `json:"can_https_redirect"`

	// The absolute path to the addon domain's document root.
	Dir string `json:"dir"`

	// The addon domain.
	Domain string `json:"domain"`

	// The subdomain that the addon domain is parked on, in the underscore-separated `subdomain_rootdomain` form. Pass this value as the `subdomain` parameter of the `AddonDomain::deladdondomain` function.
	DomainKey string `json:"domainkey"`

	// The fully qualified name of the subdomain that the addon domain is parked on.
	FullSubdomain string `json:"fullsubdomain"`

	// Whether the system currently redirects the addon domain to `HTTPS`. The system only returns this field when you pass the `return_https_redirect_status` parameter.
	// * `1` - The system redirects the addon domain.
	// * `0` - The system does **not** redirect the addon domain.
	//
	// Possible values: `0`, `1`.
	IsHTTPSRedirecting int64 `json:"is_https_redirecting"`

	// The addon domain's document root, relative to the account's home directory and prefixed with `home:`.
	Reldir string `json:"reldir"`

	// The domain that the addon domain's subdomain belongs to.
	RootDomain string `json:"rootdomain"`

	// The addon domain's rewrite status, or the URL that it redirects to.
	Status string `json:"status"`

	// The label of the subdomain that the addon domain is parked on, without the root domain.
	Subdomain string `json:"subdomain"`

	// The built-in service subdomain aliases, for example `www`, that the system serves for the addon domain.
	WebSubdomainAliases []string `json:"web_subdomain_aliases"`
}
