// Code generated by go-cpanel/tools/gen from the official
// cPanel & WHM OpenAPI documents. DO NOT EDIT.
// Source: https://api.docs.cpanel.net (cPanel & WHM version 138)

package uapi

import (
	"context"
	"net/http"

	cpanel "github.com/fmotalleb/go-cpanel"
)

// RestoreDirectoryListingArgs are the parameters of the UAPI function `Restore::directory_listing`.
type RestoreDirectoryListingArgs struct {
	// A path to a subdirectory within the user's home directory, or any level below it.
	//
	// **Note:**
	//
	// The value of this parameter **must** begin and end with a forward slash (`/`) for security purposes.
	//
	// This parameter is required.
	Path string `cpanel:"path"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// DirectoryListing calls the UAPI function `Restore::directory_listing` — Return backups in home directory
//
// This function lists all of the backup files and directories in the user's home directory.
//
// **Important:**
//
// When you disable the [File Storage role](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 68.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/file-restoration/restore-directory_listing.md
func (c *RestoreClient) DirectoryListing(ctx context.Context, args *RestoreDirectoryListingArgs) (*cpanel.UAPIResult[[]RestoreDirectoryListingDataItem], error) {
	return cpanel.UAPICall[[]RestoreDirectoryListingDataItem](ctx, c.c, http.MethodGet, "Restore", "directory_listing", args)
}


// RestoreDirectoryListingDataItem is a generated payload type.
type RestoreDirectoryListingDataItem struct {
	// Whether a difference exists between the `type` and `onDiskType` returns.
	// * `1` - Conflict exists.
	// * `0` - **No** conflict exists.
	//
	// Possible values: `0`, `1`.
	Conflict   int64 `json:"conflict"`

	// Whether the file exists in the user's directory or **only** in the backup.
	// * `1` - File exists in the user's directory.
	// * `0` - File exists **only** in the backup.
	//
	// Possible values: `0`, `1`.
	Exists     int64 `json:"exists"`

	// The name of the file or directory.
	Name       string `json:"name"`

	// The item type stored on the disk.
	// * `dir` - A directory.
	// * `file` - A file.
	// * `symlink` - A symlink.
	// * `unknown` - An unknown file type.
	//
	// Possible values: `dir`, `file`, `symlink`, `unknown`.
	OnDiskType string `json:"onDiskType"`

	// The item type stored in the backup.
	// * `dir` - A directory.
	// * `file` - A file.
	// * `symlink` - A symlink.
	// * `unknown` - An unknown file type.
	//
	// Possible values: `dir`, `file`, `symlink`, `unknown`.
	Type2      string `json:"type"`
}

// GetUsers calls the UAPI function `Restore::get_users` — Return cPanel accounts with backup metadata
//
// This function lists a reseller's users that have existing backup metadata.
//
// **Note:**
//
// When you disable the [File Storage role](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 72.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/file-restoration/restore-get_users.md
func (c *RestoreClient) GetUsers(ctx context.Context, extra ...cpanel.Args) (*cpanel.UAPIResult[[]string], error) {
	return cpanel.UAPICall[[]string](ctx, c.c, http.MethodGet, "Restore", "get_users", cpanel.CombineArgs(extra...))
}


// RestoreQueryFileInfoArgs are the parameters of the UAPI function `Restore::query_file_info`.
type RestoreQueryFileInfoArgs struct {
	// A file, directory, or symlink in the user's directory tree.
	//
	// **Note:**
	//
	// The value of this parameter **must** begin with a forward slash (`/`).
	//
	// This parameter is required.
	Path string `cpanel:"path"`

	// Whether to show the `exist` return, which indicates whether the item exists in the
	// local disk or only in the backup.
	//
	// * `1` — Show the `exist` return's value.
	// * `0` — Do **not** show the `exists` return's value.
	//
	// Possible values: `1`, `0`.
	//
	// Defaults to `0`.
	Exists *int64 `cpanel:"exists,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// QueryFileInfo calls the UAPI function `Restore::query_file_info` — Return backup storage locations
//
// This function lists all of an item's backup locations. An item can be a file, a directory, or a symlink.
//
// **Important:**
//
// When you disable the [File Storage role](https://go.cpanel.net/serverroles#roles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 68.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/file-restoration/restore-query_file_info.md
func (c *RestoreClient) QueryFileInfo(ctx context.Context, args *RestoreQueryFileInfoArgs) (*cpanel.UAPIResult[[]RestoreQueryFileInfoDataItem], error) {
	return cpanel.UAPICall[[]RestoreQueryFileInfoDataItem](ctx, c.c, http.MethodGet, "Restore", "query_file_info", args)
}


// RestoreQueryFileInfoDataItem is a generated payload type.
type RestoreQueryFileInfoDataItem struct {
	// The date when the system created the backup.
	BackupDate string `json:"backupDate"`

	// The backup's identification.
	//
	// * A date, in `YYYY-MM-DD` format.
	// * `incremental` — An incremental daily backup.
	// * The backup frequency (`weekly` or `monthly`) , a slash character (`/`), and the value `incremental`.
	// * The backup frequency (`weekly` or `monthly`), a slash character (`/`), and the backup date, in `YYYY-MM-DD` format.
	BackupID   string `json:"backupID"`

	// The backup type.
	//
	// * `compressed` —  A compressed tar file.
	// * `incremental` — A full tree of files and directories.
	// * `uncompressed` — An uncompressed tar file.
	//
	// Possible values: `compressed`, `incremental`, `uncompressed`.
	BackupType string `json:"backupType"`

	// Whether the item (a file, a directory, or a symlink) exists in the local disk or only in the backup.
	//
	// * `1` — The item exists in the local disk.
	// * `0` — The item exists only in the backup.
	//
	// **Note:**
	//
	// This return appears **only** if you set the `exists` parameter to `1`.
	//
	// Possible values: `1`, `0`.
	Exists     int64 `json:"exists"`

	// The size, in bytes, of the file in the backup.
	//
	// **Note:**
	//
	// The function returns this value **only** if the item is a file.
	FileSize   int64 `json:"fileSize"`

	// The file's last modification time.
	Mtime      int64 `json:"mtime"`

	// The identical file path value that the system passed in the function.
	Path       string `json:"path"`

	// The item type stored in the backup.
	//
	// * `dir` — A directory.
	// * `file` — A file.
	// * `symlink` — A symlink.
	// * `unknown` — An unknown file type.
	//
	// Possible values: `dir`, `file`, `symlink`, `unknown`.
	Type2      string `json:"type"`
}

// RestoreRestoreFileArgs are the parameters of the UAPI function `Restore::restore_file`.
type RestoreRestoreFileArgs struct {
	// The backup's identification.
	//
	// * `YYYY-MM-DD` — Restore a daily backup from the specified backup date.
	// * `incremental` — Restore a daily incremental backup.
	// * `weekly/YYYY-MM-DD` — Restore a weekly backup from the specified backup date.
	// * `monthly/YYYY-MM-DD` — Restore a monthly backup from the specified backup date.
	// * `weekly/incremental` — Restore a weekly incremental backup.
	// * `monthly/incremental` — Restore a monthly incremental backup.
	//
	// This parameter is required.
	BackupID string `cpanel:"backupID"`

	// Whether to overwrite the file or directory on the disc with its backup replacement.
	//
	// * `1` — Overwrite the file or directory.
	// * `0` — Do **not** overwrite the file or directory.
	//
	// Possible values: `1`, `0`.
	//
	// This parameter is required.
	Overwrite int64 `cpanel:"overwrite"`

	// The absolute file or directory's path, within a backup, that you wish to restore.
	//
	// **Important:**
	//
	// * The value of this parameter **must** begin with a forward slash (/).
	// * You **must** parse filenames properly to prevent a cross-site scripting (XSS) attack.
	//
	// This parameter is required.
	Path string `cpanel:"path"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// RestoreFile calls the UAPI function `Restore::restore_file` — Restore file or directory
//
// This function restores a file or directory from a backup to the file or directory's original location.
//
// **Important:**
//
// When you disable the [File Storage role](https://go.cpanel.net/serverroles#roles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 68.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/file-restoration/restore-restore_file.md
func (c *RestoreClient) RestoreFile(ctx context.Context, args *RestoreRestoreFileArgs) (*cpanel.UAPIResult[RestoreRestoreFileData], error) {
	return cpanel.UAPICall[RestoreRestoreFileData](ctx, c.c, http.MethodGet, "Restore", "restore_file", args)
}


// An object containing the status of the operation.
type RestoreRestoreFileData struct {
	// Whether the `overwrite` parameter succeeded.
	//
	// * `1` — Success.
	// * `0` — Failure.
	//
	// Possible values: `1`, `0`.
	Success int64 `json:"success"`
}
