// Code generated by go-cpanel/tools/gen from the official
// cPanel & WHM OpenAPI documents. DO NOT EDIT.
// Source: https://api.docs.cpanel.net (cPanel & WHM version 138)

package uapi

import (
	"context"
	"net/http"

	cpanel "github.com/fmotalleb/go-cpanel"
)

// ListActiveSessions calls the UAPI function `ActiveSessions::list_active_sessions` — Return active cPanel sessions
//
// This function returns the active cPanel sessions for the current user,
// so the account owner can review where their account is currently signed
// in and spot a session they do not recognize.
//
// **Note:**
//
//   - This function returns an array, so UAPI's generic `api.filter_*`,
//     `api.sort_*` and `api.paginate_*` parameters apply, and a
//     caller-supplied sort replaces the default ordering of the `data`
//     return value.
//   - Session addresses are private, so this function is unavailable to demo
//     (shared) accounts.
//
// Available since cPanel & WHM version 138.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/activesessions/activesessions-list_active_sessions.md
func (c *ActiveSessionsClient) ListActiveSessions(ctx context.Context, extra ...cpanel.Args) (*cpanel.UAPIResult[[]ActiveSessionsListActiveSessionsDataItem], error) {
	return cpanel.UAPICall[[]ActiveSessionsListActiveSessionsDataItem](ctx, c.c, http.MethodGet, "ActiveSessions", "list_active_sessions", cpanel.CombineArgs(extra...))
}

// ActiveSessionsListActiveSessionsDataItem is a generated payload type.
type ActiveSessionsListActiveSessionsDataItem struct {
	// Remote address the session connects from. Empty
	// when the session recorded none.
	IPAddress string `json:"ip_address"`

	// Whether this is the session that made this
	// request.
	//
	// * `1` - This is the current session.
	// * `0` - It is another session.
	//
	// Possible values: `1`, `0`.
	IsCurrent int64 `json:"is_current"`

	// Whether an operator, such as the account's
	// reseller or root, is acting as the user through
	// this session.
	//
	// * `1` - The session is being operated on the
	//   user's behalf.
	// * `0` - The user signed in directly.
	//
	// Possible values: `1`, `0`.
	IsPossessed int64 `json:"is_possessed"`

	// Unix timestamp of the session's last activity,
	// taken from the session file's modification time.
	LastActive int64 `json:"last_active"`

	// The interface the session logged in to, for example `cpanel` or `webmail`.
	LoginTheme string `json:"login_theme"`

	// Whether two-factor authentication was completed
	// for this session.
	//
	// * `1` - Two-factor authentication was completed.
	// * `0` - It was not.
	//
	// Possible values: `1`, `0`.
	TFAVerified int64 `json:"tfa_verified"`
}
