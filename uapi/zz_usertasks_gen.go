// Code generated by go-cpanel/tools/gen from the official
// cPanel & WHM OpenAPI documents. DO NOT EDIT.
// Source: https://api.docs.cpanel.net (cPanel & WHM version 138)

package uapi

import (
	"context"
	"encoding/json"
	"net/http"

	cpanel "github.com/fmotalleb/go-cpanel"
)

// UserTasksDeleteArgs are the parameters of the UAPI function `UserTasks::delete`.
type UserTasksDeleteArgs struct {
	// The task's ID number.
	//
	// This parameter is required.
	ID string `cpanel:"id"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// Delete calls the UAPI function `UserTasks::delete` — Remove item from task queue
//
// This function deletes a process from the user task queue.
//
// Each cPanel account uses a separate user task queue in order to ensure that
// processes for one user do not impede another user on the server.
//
// * For example, the [*Git Version Control*](https://go.cpanel.net/GitVersionControl)
// interface (*cPanel >> Home >> Files >> Git Version Control*) uses the user task queue
// to clone repositories (via the `VersionControl::create` function).
// * For more information, read our
// [Task Queue Monitor](https://go.cpanel.net/whmdocsTaskQueueMonitor) documentation.
//
// Available since cPanel & WHM version cPanel 72.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/sse-task-management/usertasks-delete.md
func (c *UserTasksClient) Delete(ctx context.Context, args *UserTasksDeleteArgs) (*cpanel.UAPIResult[json.RawMessage], error) {
	return cpanel.UAPICall[json.RawMessage](ctx, c.c, http.MethodGet, "UserTasks", "delete", args)
}

// Retrieve calls the UAPI function `UserTasks::retrieve` — Return task queue information
//
// This function retrieves a cPanel account's user task queue information.
//
// Each cPanel account uses a separate user task queue in order to ensure that
// processes for one user do not impede another user on the server.
//
// * For example, the [*Git Version Control*](https://go.cpanel.net/GitVersionControl)
// interface (*cPanel >> Home >> Files >> Git Version Control*) uses the user task queue
// to clone repositories (via the `VersionControl::create` function).
// * For more information, read our
// [Task Queue Monitor](https://go.cpanel.net/whmdocsTaskQueueMonitor) documentation.
//
// Available since cPanel & WHM version cPanel 72.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/sse-task-management/usertasks-retrieve.md
func (c *UserTasksClient) Retrieve(ctx context.Context, extra ...cpanel.Args) (*cpanel.UAPIResult[[]UserTasksRetrieveDataItem], error) {
	return cpanel.UAPICall[[]UserTasksRetrieveDataItem](ctx, c.c, http.MethodGet, "UserTasks", "retrieve", cpanel.CombineArgs(extra...))
}

// An object containing one or more action- and subsystem-specific
// arguments, if any exist.
//
// For example, a task that returns an `action` value of `create` and
// a `subsystem` value of `VersionControl` will include the
// `repository_root` argument.
type UserTasksRetrieveDataItemArgs struct {
	// The absolute path to the repostiory's `root` directory.
	RepositoryRoot string `json:"repository_root"`
}

// UserTasksRetrieveDataItem is a generated payload type.
type UserTasksRetrieveDataItem struct {
	// The task's action.
	//
	// * `create`
	//
	// **Note:**
	//
	// `create` is the only possible value.
	//
	// Possible values: `create`.
	Action string `json:"action"`

	// An object containing one or more action- and subsystem-specific
	// arguments, if any exist.
	//
	// For example, a task that returns an `action` value of `create` and
	// a `subsystem` value of `VersionControl` will include the
	// `repository_root` argument.
	Args UserTasksRetrieveDataItemArgs `json:"args"`

	// The task's ID number.
	ID string `json:"id"`

	// The subsystem that will handle the process.
	//
	// * `VersionControl`
	//
	// **Note:**
	//
	// `VersionControl` is the only possible value.
	//
	// Possible values: `VersionControl`.
	SubSystem string `json:"subsystem"`
}
