// Code generated by go-cpanel/tools/gen from the official
// cPanel & WHM OpenAPI documents. DO NOT EDIT.
// Source: https://api.docs.cpanel.net (cPanel & WHM version 138)

package uapi

import (
	"context"
	"encoding/json"
	"net/http"

	cpanel "github.com/fmotalleb/go-cpanel"
)

// ParkListParkedDomainsArgs are the parameters of the UAPI function `Park::listparkeddomains`.
type ParkListParkedDomainsArgs struct {
	// A Perl regular expression. The system returns only the parked domains whose name matches this pattern, case-insensitively.
	Regex *string `cpanel:"regex,omitempty"`

	// Whether to include the `can_https_redirect` and `is_https_redirecting` fields in each entry.
	// * `1` - Include the HTTPS redirect status.
	// * `0` - Omit the HTTPS redirect status.
	//
	// Possible values: `0`, `1`.
	//
	// Defaults to `0`.
	ReturnHTTPSRedirectStatus *int64 `cpanel:"return_https_redirect_status,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// ListParkedDomains calls the UAPI function `Park::listparkeddomains` — List the parked (aliased) domains
//
// This function lists the domains that are parked (aliased) on the cPanel
// account's primary domain.
//
// **Note:**
//
// This function lists only the domains parked on the primary domain. Use
// the `AddonDomain::listaddondomains` function for the domains parked on a
// subdomain.
//
// Available since cPanel & WHM version cPanel 138.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/park/park-listparkeddomains.md
func (c *ParkClient) ListParkedDomains(ctx context.Context, args *ParkListParkedDomainsArgs) (*cpanel.UAPIResult[[]ParkListParkedDomainsDataItem], error) {
	return cpanel.UAPICall[[]ParkListParkedDomainsDataItem](ctx, c.c, http.MethodGet, "Park", "listparkeddomains", args)
}

// ParkListParkedDomainsDataItem is a generated payload type.
type ParkListParkedDomainsDataItem struct {
	// The domain's document root, relative to the account's home directory.
	Basedir string `json:"basedir"`

	// Whether the system can redirect the domain to `HTTPS`. The system only returns this field when you pass the `return_https_redirect_status` parameter.
	// * `1` - The system can redirect the domain.
	// * `0` - The system cannot redirect the domain.
	//
	// Possible values: `0`, `1`.
	CanHTTPSRedirect int64 `json:"can_https_redirect"`

	// The absolute path to the domain's document root.
	Dir string `json:"dir"`

	// The parked domain.
	Domain string `json:"domain"`

	// Whether the system currently redirects the domain to `HTTPS`. The system only returns this field when you pass the `return_https_redirect_status` parameter.
	// * `1` - The system redirects the domain.
	// * `0` - The system does **not** redirect the domain.
	//
	// Possible values: `0`, `1`.
	IsHTTPSRedirecting int64 `json:"is_https_redirecting"`

	// The domain's document root, relative to the account's home directory and prefixed with `home:`.
	Reldir string `json:"reldir"`

	// The URL that the domain redirects to, or the domain's rewrite status when it does not redirect.
	Status string `json:"status"`

	// The built-in service subdomain aliases, for example `www`, that the system serves for the parked domain.
	WebSubdomainAliases []string `json:"web_subdomain_aliases"`
}

// ParkParkArgs are the parameters of the UAPI function `Park::park`.
type ParkParkArgs struct {
	// The domain to park. The system removes a leading `www.` from this value and converts it to lowercase before it validates the domain.
	//
	// This parameter is required.
	Domain string `cpanel:"domain"`

	// Whether to remove the dot (`.`) characters from the `topdomain` value, rather than only trimming a trailing dot. This parameter has no effect unless you also pass the `topdomain` parameter.
	// * `1` - Remove dots from the subdomain label.
	// * `0` - Do **not** remove dots from the subdomain label.
	//
	// Possible values: `0`, `1`.
	//
	// Defaults to `0`.
	DisallowDoT *int64 `cpanel:"disallowdot,omitempty"`

	// Whether to allow the system to overwrite an existing [DNS](https://go.cpanel.net/dnsrecords) zone for the `domain` value.
	// * `1` - Overwrite an existing zone.
	// * `0` - Fail if a zone already exists.
	//
	// Possible values: `0`, `1`.
	//
	// Defaults to `0`.
	Force *int64 `cpanel:"force,omitempty"`

	// The subdomain label of the account's primary domain to park the `domain` value on. The system appends the primary domain to this value.
	//
	// Omit this parameter to park the domain on the primary domain itself.
	TopDomain *string `cpanel:"topdomain,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// Park calls the UAPI function `Park::park` — Park an alias domain on an existing domain
//
// This function parks (aliases) a domain on the cPanel account, so that the
// domain serves the content of a domain that already exists on the account.
//
// **Important:**
//
//   - Without the `topdomain` parameter, the system parks the domain on the
//     account's primary domain.
//   - With the `topdomain` parameter, the system parks the domain on a
//     subdomain of the primary domain. That arrangement is how an addon
//     domain is represented. To create an addon domain and its subdomain in
//     one call, use the `AddonDomain::addaddondomain` function instead.
//   - The account must have either the *Parked Domains* or the *Addon
//     Domains* feature. Parking on the primary domain requires the *Parked
//     Domains* feature specifically.
//
// When you disable the [Web Server role](https://go.cpanel.net/serverroles#roles),
// the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 138.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/park/park-park.md
func (c *ParkClient) Park(ctx context.Context, args *ParkParkArgs) (*cpanel.UAPIResult[json.RawMessage], error) {
	return cpanel.UAPICall[json.RawMessage](ctx, c.c, http.MethodGet, "Park", "park", args)
}

// ParkUnparkArgs are the parameters of the UAPI function `Park::unpark`.
type ParkUnparkArgs struct {
	// The parked domain to remove.
	//
	// This parameter is required.
	Domain string `cpanel:"domain"`

	// The subdomain that the domain is parked on, given in the underscore-separated form that the `AddonDomain::listaddondomains` function returns in its `domainkey` field.
	//
	// Omit this parameter to let the system determine the subdomain. Pass it only when the domain is parked on a subdomain rather than on the primary domain.
	Subdomain *string `cpanel:"subdomain,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// Unpark calls the UAPI function `Park::unpark` — Remove a parked (aliased) domain
//
// This function unparks (removes the alias for) a domain on the cPanel
// account.
//
// **Important:**
//
//   - This function removes the alias only. It does **not** remove the
//     subdomain that an addon domain is parked on. To remove both, use the
//     `AddonDomain::deladdondomain` function instead.
//   - The account must have either the *Parked Domains* or the *Addon
//     Domains* feature.
//
// When you disable the [Web Server role](https://go.cpanel.net/serverroles#roles),
// the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 138.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/park/park-unpark.md
func (c *ParkClient) Unpark(ctx context.Context, args *ParkUnparkArgs) (*cpanel.UAPIResult[json.RawMessage], error) {
	return cpanel.UAPICall[json.RawMessage](ctx, c.c, http.MethodGet, "Park", "unpark", args)
}
