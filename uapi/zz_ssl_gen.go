// Code generated by go-cpanel/tools/gen from the official
// cPanel & WHM OpenAPI documents. DO NOT EDIT.
// Source: https://api.docs.cpanel.net (cPanel & WHM version 138)

package uapi

import (
	"context"
	"encoding/json"
	"net/http"

	cpanel "github.com/fmotalleb/go-cpanel"
)

// SSLAddAutoSSLExcludedDomainsArgs are the parameters of the UAPI function `SSL::add_autossl_excluded_domains`.
type SSLAddAutoSSLExcludedDomainsArgs struct {
	// A comma-separated list of domains for which to disable AutoSSL.
	//
	// **Note:**
	//
	// For browser-based calls, use a URI encoded comma (`%2C`).
	//
	// This parameter is required.
	Domains string `cpanel:"domains"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// AddAutoSSLExcludedDomains calls the UAPI function `SSL::add_autossl_excluded_domains` — Disable AutoSSL for domains
//
// This function disables AutoSSL for the domains that you specify.
//
// **Important:**
//
// When you disable the Calendars and Contacts, Receive Mail, Web Disk, Webmail, and Web Server [roles](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 66.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/auto-generated-ssl-certificates/ssl-add_autossl_excluded_domains.md
func (c *SSLClient) AddAutoSSLExcludedDomains(ctx context.Context, args *SSLAddAutoSSLExcludedDomainsArgs) (*cpanel.UAPIResult[json.RawMessage], error) {
	return cpanel.UAPICall[json.RawMessage](ctx, c.c, http.MethodGet, "SSL", "add_autossl_excluded_domains", args)
}

// CanSSLRedirect calls the UAPI function `SSL::can_ssl_redirect` — Return whether domains can redirect to secure URL
//
// This function determines whether the system can automatically redirect domains on a cPanel account to use SSL.
//
// Available since cPanel & WHM version cPanel 80.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/cpanel-account-ssl-management/ssl-can_ssl_redirect.md
func (c *SSLClient) CanSSLRedirect(ctx context.Context, extra ...cpanel.Args) (*cpanel.UAPIResult[int64], error) {
	return cpanel.UAPICall[int64](ctx, c.c, http.MethodGet, "SSL", "can_ssl_redirect", cpanel.CombineArgs(extra...))
}

// CheckSharedCert calls the UAPI function `SSL::check_shared_cert` — Return whether shared SSL certificate exists
//
// This function checks whether a shared SSL certificate is associated with the account.
//
// This function is deprecated upstream.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/cpanel-account-ssl-management/ssl-check_shared_cert.md
//
// Deprecated: cPanel & WHM mark this function as deprecated.
func (c *SSLClient) CheckSharedCert(ctx context.Context, extra ...cpanel.Args) (*cpanel.UAPIResult[int64], error) {
	return cpanel.UAPICall[int64](ctx, c.c, http.MethodGet, "SSL", "check_shared_cert", cpanel.CombineArgs(extra...))
}

// SSLDeleteCertArgs are the parameters of the UAPI function `SSL::delete_cert`.
type SSLDeleteCertArgs struct {
	// The certificate's friendly name.
	FriendlyName *string `cpanel:"friendly_name,omitempty"`

	// The certificate's ID.
	ID *string `cpanel:"id,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// DeleteCert calls the UAPI function `SSL::delete_cert` — Delete SSL certificate
//
// This function deletes an SSL certificate.
//
// **Note:**
//
// * When you call this function, you **must** include the `id` or the `friendly_name`
// parameter.
// * This function **only** deletes certificates from SSL storage. To end SSL
// coverage for a domain, use the UAPI `SSL::delete_ssl` function instead.
//
// **Important:**
//
// When you disable the Calendars and Contacts, Receive Mail, Web Disk, Webmail,
// and Web Server [roles](https://go.cpanel.net/howtouseserverprofiles#roles),
// the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/ssl-certificate-management/ssl-delete_cert.md
func (c *SSLClient) DeleteCert(ctx context.Context, args *SSLDeleteCertArgs) (*cpanel.UAPIResult[[]SSLDeleteCertDataItem], error) {
	return cpanel.UAPICall[[]SSLDeleteCertDataItem](ctx, c.c, http.MethodGet, "SSL", "delete_cert", args)
}

// SSLDeleteCertDataItem is a generated payload type.
type SSLDeleteCertDataItem struct {
	// The date the certificate was created.
	Created int64 `json:"created"`

	// A list of the domains that the certificate covers.
	Domains []string `json:"domains"`

	// The ECDSA curve that the certificate's key uses.
	//
	// * `prime256v1`
	// * `secp384r1`
	// * `null` — The certificate's key is **not** an ECDSA key.
	//
	// Possible values: `prime256v1`, `secp384r1`.
	EcdsaCurveName *string `json:"ecdsa_curve_name"`

	// The certificate's key's ECDSA compressed public point, in hexadecimal format.
	//
	// * `null` — The certificate's key is **not** an ECDSA key.
	EcdsaPublic *string `json:"ecdsa_public"`

	// The certificate's friendly name.
	FriendlyName string `json:"friendly_name"`

	// The certificate's ID.
	ID string `json:"id"`

	// Whether the certificate is self-signed.
	//
	// * `1` — Self-signed.
	// * `0` — Not self-signed.
	//
	// Possible values: `1`, `0`.
	IsSelfSigned int64 `json:"is_self_signed"`

	// The certificate's Common Name.
	IssuerCommonName string `json:"issuer.commonName"`

	// The certificate's Organization Name.
	IssuerOrganizationName string `json:"issuer.organizationName"`

	// The certificate's issuer information.
	IssuerText string `json:"issuer_text"`

	// The certificate's key's algorithm.
	//
	// * `rsaEncryption` — RSA.
	// * `id-ecPublicKey` — ECDSA.
	//
	// Possible values: `rsaEncryption`, `id-ecPublicKey`.
	KeyAlgorithm string `json:"key_algorithm"`

	// The certificate's key's modulus, in hexadecimal format.
	//
	// * `null` — The certificate's key is **not** an RSA key.
	Modulus *string `json:"modulus"`

	// The length, in bits, of the certificate's key's modulus.
	//
	// * `null` — The certificate's key is **not** an RSA key.
	ModulusLength *int64 `json:"modulus_length"`

	// The date the certificate expired.
	NotAfter int64 `json:"not_after"`

	// The date the certificate started.
	NotBefore int64 `json:"not_before"`

	// The certificate's serial number.
	Serial string `json:"serial"`

	// The certificate's OID signature hash algorithm.
	SignatureAlgorithm string `json:"signature_algorithm"`

	// The certificate's Common Name.
	SubjectCommonName string `json:"subject.commonName"`

	// The certificate's subject text information.
	SubjectText string `json:"subject_text"`

	// The certificate's validation type.
	//
	// * `ev` — Extended Validation.
	// * `ov` — Organization Validation.
	// * `dv` — Domain Validation.
	// * `null` — The system could not parse and determine the
	// certificate's validation type.
	//
	// Possible values: `ev`, `ov`, `dv`.
	ValidationType *string `json:"validation_type"`
}

// SSLDeleteCsrArgs are the parameters of the UAPI function `SSL::delete_csr`.
type SSLDeleteCsrArgs struct {
	// The CSR's friendly name.
	FriendlyName *string `cpanel:"friendly_name,omitempty"`

	// The CSR's ID.
	ID *string `cpanel:"id,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// DeleteCsr calls the UAPI function `SSL::delete_csr` — Delete certificate signing request
//
// This function deletes a certificate signing request (CSR).
//
// **Note:**
//
// * When you call this function, you **must** include the `id` or the `friendly_name`
// parameter.
// * To delete a private key, use the UAPI `SSL::delete_key` function instead.
//
// **Important:**
//
// When you disable the Calendars and Contacts, Receive Mail, Web Disk, Webmail,
// and Web Server [roles](https://go.cpanel.net/howtouseserverprofiles#roles),
// the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/ssl-certificate-management/ssl-delete_csr.md
func (c *SSLClient) DeleteCsr(ctx context.Context, args *SSLDeleteCsrArgs) (*cpanel.UAPIResult[[]SSLDeleteCsrDataItem], error) {
	return cpanel.UAPICall[[]SSLDeleteCsrDataItem](ctx, c.c, http.MethodGet, "SSL", "delete_csr", args)
}

// SSLDeleteCsrDataItem is a generated payload type.
type SSLDeleteCsrDataItem struct {
	// The CSR's Common Name.
	CommonName string `json:"commonName"`

	// The CSR's creation date.
	Created int64 `json:"created"`

	// A list of the domains that the CSR covers.
	Domains []string `json:"domains"`

	// The ECDSA curve that the CSR's key uses.
	//
	// * `prime256v1`
	// * `secp384r1`
	// * `null` — The CSR's key is **not** an ECDSA key.
	//
	// Possible values: `prime256v1`, `secp384r1`.
	EcdsaCurveName *string `json:"ecdsa_curve_name"`

	// The CSR's key's ECDSA compressed public point, in hexadecimal format.
	//
	// * `null` — The CSR's key is **not** an ECDSA key.
	EcdsaPublic *string `json:"ecdsa_public"`

	// The CSR's friendly name.
	FriendlyName string `json:"friendly_name"`

	// The CSR's ID.
	ID string `json:"id"`

	// The CSR's key's algorithm.
	//
	// * `rsaEncryption` — RSA.
	// * `id-ecPublicKey` — ECDSA.
	//
	// Possible values: `rsaEncryption`, `id-ecPublicKey`.
	KeyAlgorithm string `json:"key_algorithm"`

	// The CSR's key's modulus, in hexadecimal format.
	//
	// * `null` — The CSR's key is **not** an RSA key.
	Modulus *string `json:"modulus"`
}

// SSLDeleteKeyArgs are the parameters of the UAPI function `SSL::delete_key`.
type SSLDeleteKeyArgs struct {
	// The private key's friendly name.
	FriendlyName *string `cpanel:"friendly_name,omitempty"`

	// The private key's ID.
	ID *string `cpanel:"id,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// DeleteKey calls the UAPI function `SSL::delete_key` — Delete private key
//
// This function deletes a private key.
//
// **Note:**
//
// * When you call this function, you **must** include the `id` or the `friendly_name`
// parameter.
// * To delete a certificate signing request (CSR), use the UAPI `SSL::delete_csr`
// function instead.
//
// **Important:**
//
// When you disable the Calendars and Contacts, Receive Mail, Web Disk, Webmail,
// and Web Server [roles](https://go.cpanel.net/howtouseserverprofiles#roles),
// the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/ssl-certificate-management/ssl-delete_key.md
func (c *SSLClient) DeleteKey(ctx context.Context, args *SSLDeleteKeyArgs) (*cpanel.UAPIResult[[]SSLDeleteKeyDataItem], error) {
	return cpanel.UAPICall[[]SSLDeleteKeyDataItem](ctx, c.c, http.MethodGet, "SSL", "delete_key", args)
}

// SSLDeleteKeyDataItem is a generated payload type.
type SSLDeleteKeyDataItem struct {
	// The private key's creation date.
	Created int64 `json:"created"`

	// The ECDSA curve that the key uses.
	//
	// * `prime256v1`
	// * `secp384r1`
	// * `null` — The key is **not** an ECDSA key.
	//
	// Possible values: `prime256v1`, `secp384r1`.
	EcdsaCurveName *string `json:"ecdsa_curve_name"`

	// The key's ECDSA compressed public point, in hexadecimal format.
	//
	// * `null` — The key is **not** an ECDSA key.
	EcdsaPublic *string `json:"ecdsa_public"`

	// The private key's friendly name.
	FriendlyName string `json:"friendly_name"`

	// The key's algorithm.
	//
	// * `rsaEncryption` — RSA.
	// * `id-ecPublicKey` — ECDSA.
	//
	// Possible values: `rsaEncryption`, `id-ecPublicKey`.
	KeyAlgorithm string `json:"key_algorithm"`

	// The key's modulus, in hexadecimal format.
	//
	// * `null` — The key is **not** an RSA key.
	Modulus *string `json:"modulus"`

	// The length, in bits, of the key's modulus.
	//
	// * `null` — The key is **not** an RSA key.
	ModulusLength *int64 `json:"modulus_length"`
}

// SSLDeleteSSLArgs are the parameters of the UAPI function `SSL::delete_ssl`.
type SSLDeleteSSLArgs struct {
	// The domain name.
	//
	// This parameter is required.
	Domain string `cpanel:"domain"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// DeleteSSL calls the UAPI function `SSL::delete_ssl` — Remove SSL for domain
//
// This function removes SSL from a domain.
//
// **Note:**
//
// This function removes domains from the current certificate to end SSL coverage for those
// domains. To delete certificates from SSL storage, use the UAPI function `SSL::delete_cert`
// instead.
//
// **Important:**
//
// When you disable the Calendars and Contacts, Receive Mail, Web Disk, Webmail, **and** Web Server
// [roles](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/cpanel-account-ssl-management/ssl-delete_ssl.md
func (c *SSLClient) DeleteSSL(ctx context.Context, args *SSLDeleteSSLArgs) (*cpanel.UAPIResult[json.RawMessage], error) {
	return cpanel.UAPICall[json.RawMessage](ctx, c.c, http.MethodGet, "SSL", "delete_ssl", args)
}

// SSLDisableMailSniArgs are the parameters of the UAPI function `SSL::disable_mail_sni`.
type SSLDisableMailSniArgs struct {
	// A pipe-delimited list of the account's domains.
	//
	// This parameter is required.
	Domains string `cpanel:"domains"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// DisableMailSni calls the UAPI function `SSL::disable_mail_sni` — Disable SNI mail services for domain
//
// This function disables SNI mail services on the specified domains.
//
// **Note:**
//
//	Mail SNI is **always** enabled.
//
//	* After you change the SNI status, you **must** run UAPI's `rebuild_mail_sni_config` function.
//	* Functions that enable Mail SNI succeed with a warning that Mail SNI is always enabled.
//	* Functions that disable Mail SNI fail and make no changes.
//
// Available since cPanel & WHM version cPanel 11.48.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/sni-email-settings/ssl-disable_mail_sni.md
func (c *SSLClient) DisableMailSni(ctx context.Context, args *SSLDisableMailSniArgs) (*cpanel.UAPIResult[SSLDisableMailSniData], error) {
	return cpanel.UAPICall[SSLDisableMailSniData](ctx, c.c, http.MethodGet, "SSL", "disable_mail_sni", args)
}

// SSLDisableMailSniData is a generated payload type.
type SSLDisableMailSniData struct {
	// An object containing the domains that failed to disable
	//  mail SNI.
	FailedDomains map[string]string `json:"failed_domains"`

	// AN object containing the domains with disabled mail SNI.
	UpdatedDomains map[string]int64 `json:"updated_domains"`
}

// SSLEnableMailSniArgs are the parameters of the UAPI function `SSL::enable_mail_sni`.
type SSLEnableMailSniArgs struct {
	// A pipe-delimited list of the account's domains.
	//
	// This parameter is required.
	Domains string `cpanel:"domains"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// EnableMailSni calls the UAPI function `SSL::enable_mail_sni` — Enable SNI mail services for domain
//
// This function enables SNI mail services on the specified domains.
//
// **Warning:**
//
// Mail SNI is **always** enabled.
// * Mail SNI is **not** compatible with Webmail and will **not** function for any Webmail connection. Webmail connections use the cPanel service SSL certificate.
// * Functions that enable Mail SNI succeed with a warning that Mail SNI is always enabled.
// * Functions that disable Mail SNI fail and make no changes.
//
// **Important:**
//
//	When you disable the *Calendars and Contacts*, *Mail Receive*, *Web Disk*, *Webmail*, **and** *Web Server* [roles](https://go.cpanel.net/howtouseserverprofiles#roles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.48.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/sni-email-settings/ssl-enable_mail_sni.md
func (c *SSLClient) EnableMailSni(ctx context.Context, args *SSLEnableMailSniArgs) (*cpanel.UAPIResult[SSLEnableMailSniData], error) {
	return cpanel.UAPICall[SSLEnableMailSniData](ctx, c.c, http.MethodGet, "SSL", "enable_mail_sni", args)
}

// SSLEnableMailSniData is a generated payload type.
type SSLEnableMailSniData struct {
	// An object that contains the domains that did not enable mail SNI.
	FailedDomains map[string]string `json:"failed_domains"`

	// An object that contains the domains with disabled mail SNI.
	UpdatedDomains map[string]int64 `json:"updated_domains"`
}

// SSLFetchBestForDomainArgs are the parameters of the UAPI function `SSL::fetch_best_for_domain`.
type SSLFetchBestForDomainArgs struct {
	// The domain name.
	//
	// This parameter is required.
	Domain string `cpanel:"domain"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// FetchBestForDomain calls the UAPI function `SSL::fetch_best_for_domain` — Request best SSL certificate
//
// This function retrieves the best-available certificate for the domain. The function also
// retrieves the certificate's associated private key and CA bundle, if available.
//
// **Important:**
//
// When you disable the [Calendars and Contacts, Receive Mail, Web Disk, Webmail, and Web Server roles](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/cpanel-account-ssl-management/ssl-fetch_best_for_domain.md
func (c *SSLClient) FetchBestForDomain(ctx context.Context, args *SSLFetchBestForDomainArgs) (*cpanel.UAPIResult[SSLFetchBestForDomainData], error) {
	return cpanel.UAPICall[SSLFetchBestForDomainData](ctx, c.c, http.MethodGet, "SSL", "fetch_best_for_domain", args)
}

// SSLFetchBestForDomainData is a generated payload type.
type SSLFetchBestForDomainData struct {
	// The CA bundle's contents (if applicable).
	Cab string `json:"cab"`

	// The certificate's contents.
	Crt string `json:"crt"`

	// The username that generated the certificate.
	CrtOrigin string `json:"crt_origin"`

	// The domain that generated the private key.
	Domain string `json:"domain"`

	// The IP address.
	IP string `json:"ip"`

	// The private key.
	Key string `json:"key"`

	// The username that generated the private key.
	KeyOrigin string `json:"key_origin"`

	// The cPanel accounts that the system searched for domain information.
	SearchedUsers []string `json:"searched_users"`

	// Whether the certificate is active.
	// * `1` - Active.
	// * `0` - Inactive.
	//
	// Possible values: `0`, `1`.
	Status int64 `json:"status"`

	// The certificate's status.
	StatusMsg string `json:"statusmsg"`

	// The username that stores the private key.
	User string `json:"user"`
}

// SSLFetchCertInfoArgs are the parameters of the UAPI function `SSL::fetch_cert_info`.
type SSLFetchCertInfoArgs struct {
	// The certificate's human readable name.
	//
	// This parameter is required.
	FriendlyName string `cpanel:"friendly_name"`

	// The certificate's ID.
	//
	// This parameter is required.
	ID string `cpanel:"id"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// FetchCertInfo calls the UAPI function `SSL::fetch_cert_info` — Return SSL certificate information
//
// This function retrieves all of a certificate's available information.
//
// **Important:**
//
// * You **must** call either the `friendly_name` or `id` parameter.
// * When you disable the Calendars and Contacts, Receive Mail, Web Disk, Webmail, and Web Server [roles](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/cpanel-account-ssl-management/ssl-fetch_cert_info.md
func (c *SSLClient) FetchCertInfo(ctx context.Context, args *SSLFetchCertInfoArgs) (*cpanel.UAPIResult[SSLFetchCertInfoData], error) {
	return cpanel.UAPICall[SSLFetchCertInfoData](ctx, c.c, http.MethodGet, "SSL", "fetch_cert_info", args)
}

// SSLFetchCertInfoData is a generated payload type.
type SSLFetchCertInfoData struct {
	// The CA bundle's contents (if applicable).
	Cabundle *string `json:"cabundle"`

	// The certificate's contents.
	Certificate string `json:"certificate"`

	// Whether the certificate is self-signed.
	// * `1` - Self-signed.
	// * `0` - **Not** self-signed.
	//
	// Possible values: `0`, `1`.
	IsSelfSigned int64 `json:"is_self_signed"`

	// The private key.
	Key string `json:"key"`

	// The IP address.
	SubjectCommonNameIP string `json:"subject.commonName_ip"`
}

// SSLFetchCertificatesForFqdnsArgs are the parameters of the UAPI function `SSL::fetch_certificates_for_fqdns`.
type SSLFetchCertificatesForFqdnsArgs struct {
	// A domain or comma-delimited list of domains for which to retrieve information.
	//
	// This parameter is required.
	Domains string `cpanel:"domains"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// FetchCertificatesForFqdns calls the UAPI function `SSL::fetch_certificates_for_fqdns` — Return SSL certificate information for all FQDN
//
// This function retrieves the certificate information for all fully qualified domain names (FQDNs) that the account owns.
//
// Available since cPanel & WHM version cPanel 66.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/cpanel-account-ssl-management/ssl-fetch_certificates_for_fqdns.md
func (c *SSLClient) FetchCertificatesForFqdns(ctx context.Context, args *SSLFetchCertificatesForFqdnsArgs) (*cpanel.UAPIResult[[]SSLFetchCertificatesForFqdnsDataItem], error) {
	return cpanel.UAPICall[[]SSLFetchCertificatesForFqdnsDataItem](ctx, c.c, http.MethodGet, "SSL", "fetch_certificates_for_fqdns", args)
}

// SSLFetchCertificatesForFqdnsDataItem is a generated payload type.
type SSLFetchCertificatesForFqdnsDataItem struct {
	// The CA bundle's contents.
	Cab string `json:"cab"`

	// When the certificate was created.
	Created int64 `json:"created"`

	// The certificate's contents in Base64 PEM format.
	Crt string `json:"crt"`

	// Whether the certificate is installed on the account.
	// * `1` — Installed.
	// * `0` — Not installed.
	//
	// Possible values: `0`, `1`.
	DomainIsConfigured int64 `json:"domain_is_configured"`

	// The domains that the CSR covers.
	Domains []string `json:"domains"`

	// The ECDSA curve that the certificate's key uses.
	//
	// * `prime256v1`
	// * `secp384r1`
	// * `null` — The certificate's key is **not** an ECDSA key.
	//
	// Possible values: `prime256v1`, `secp384r1`.
	EcdsaCurveName *string `json:"ecdsa_curve_name"`

	// The certificate's key's ECDSA compressed public point, in hexadecimal format.
	//
	// * `null` — The certificate's key is **not** an ECDSA key.
	EcdsaPublic *string `json:"ecdsa_public"`

	// The certificate's friendly name.
	FriendlyName string `json:"friendly_name"`

	// The certificate's identification.
	ID string `json:"id"`

	// Whether the certificate is self-signed.
	// - `1` — Self-signed.
	// - `0` — Not self-signed.
	//
	// Possible values: `0`, `1`.
	IsSelfSigned int64 `json:"is_self_signed"`

	// The name that issued the certificate.
	IssuerCommonName string `json:"issuer.commonName"`

	// The certificate's organization.
	IssuerOrganizationName string `json:"issuer.organizationName"`

	// The certificate's issuer information.
	IssuerText string `json:"issuer_text"`

	// The private key in Base64 PEM format.
	Key string `json:"key"`

	// The certificate's key's algorithm.
	//
	// * `rsaEncryption` — RSA.
	// * `id-ecPublicKey` — ECDSA.
	//
	// Possible values: `rsaEncryption`, `id-ecPublicKey`.
	KeyAlgorithm string `json:"key_algorithm"`

	// The length, in bits, of the certificate's key's modulus.
	//
	// * `null` — The certificate's key is **not** an RSA key.
	Modulus *string `json:"modulus"`

	// The length, in bits, of the certificate's key's modulus.
	//
	// * `null` — The certificate's key is **not** an RSA key.
	ModulusLength *int64 `json:"modulus_length"`

	// When the certificate expired.
	NotAfter int64 `json:"not_after"`

	// When the certificate started.
	NotBefore int64 `json:"not_before"`

	// The certificate's serial number.
	Serial string `json:"serial"`

	// The OID of the hash algorithm used to sign the certificate request.
	SignatureAlgorithm string `json:"signature_algorithm"`

	// The certificate's common name.
	SubjectCommonName string `json:"subject.commonName"`

	// The certificate's subject text information.
	SubjectText string `json:"subject_text"`

	// The certificate's validation type.
	// * `ev` — Extended Validation.
	// * `ov` — Organization Validated.
	// * `dv` — Domain Validated.
	// * `null` —  The system could not parse and determine the certificate's validation type.
	ValidationType string `json:"validation_type"`

	// A message that explains the reason for a verification error.
	VerifyError string `json:"verify_error"`
}

// SSLFetchKeyAndCabundleForCertificateArgs are the parameters of the UAPI function `SSL::fetch_key_and_cabundle_for_certificate`.
type SSLFetchKeyAndCabundleForCertificateArgs struct {
	// An SSL certificate.
	//
	// This parameter is required.
	Certificate string `cpanel:"certificate"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// FetchKeyAndCabundleForCertificate calls the UAPI function `SSL::fetch_key_and_cabundle_for_certificate` — Return private key and CA bundle
//
// This function extracts the private key and CA bundle information from a certificate.
//
// **Note:**
//
// Due to the limited field length of `HTTP GET` method calls, you **must** use the `HTTP POST` method. For this reason, you **cannot** use a cPanel or Webmail session URL to call this function.
//
// This function requires an HTTP POST request.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/cpanel-account-ssl-management/ssl-fetch_key_and_cabundle_for_certificate.md
func (c *SSLClient) FetchKeyAndCabundleForCertificate(ctx context.Context, args *SSLFetchKeyAndCabundleForCertificateArgs) (*cpanel.UAPIResult[SSLFetchKeyAndCabundleForCertificateData], error) {
	return cpanel.UAPICall[SSLFetchKeyAndCabundleForCertificateData](ctx, c.c, http.MethodPost, "SSL", "fetch_key_and_cabundle_for_certificate", args)
}

// SSLFetchKeyAndCabundleForCertificateData is a generated payload type.
type SSLFetchKeyAndCabundleForCertificateData struct {
	// The CA bundle's contents (if applicable).
	Cab string `json:"cab"`

	// The certificate's contents.
	Crt string `json:"crt"`

	// The username that generated the certificate.
	CrtOrigin string `json:"crt_origin"`

	// The domain that generated the private key.
	Domain string `json:"domain"`

	// The IP address.
	IP string `json:"ip"`

	// The private key.
	Key string `json:"key"`

	// The username that generated the private key.
	KeyOrigin string `json:"key_origin"`

	// The users that the system searched for certificate information.
	SearchedUsers []string `json:"searched_users"`

	// Whether the certificate is active.
	// * `1` - Active.
	// * `0` - Inactive.
	//
	// Possible values: `0`, `1`.
	Status int64 `json:"status"`

	// The certificate's status.
	StatusMsg string `json:"statusmsg"`

	// The username that stores the private key.
	User string `json:"user"`
}

// SSLFindCertificatesForKeyArgs are the parameters of the UAPI function `SSL::find_certificates_for_key`.
type SSLFindCertificatesForKeyArgs struct {
	// The key's friendly name.
	FriendlyName *string `cpanel:"friendly_name,omitempty"`

	// The key's ID.
	ID *string `cpanel:"id,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// FindCertificatesForKey calls the UAPI function `SSL::find_certificates_for_key` — Return SSL certificate for private key
//
// This function retrieves SSL certificates for a private key.
//
// **Note:**
//
// When you call this function, you **must** include either the `id`
// **or** the `friendly_name` parameter.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/cpanel-account-ssl-management/ssl-find_certificates_for_key.md
func (c *SSLClient) FindCertificatesForKey(ctx context.Context, args *SSLFindCertificatesForKeyArgs) (*cpanel.UAPIResult[[]SSLFindCertificatesForKeyDataItem], error) {
	return cpanel.UAPICall[[]SSLFindCertificatesForKeyDataItem](ctx, c.c, http.MethodGet, "SSL", "find_certificates_for_key", args)
}

// SSLFindCertificatesForKeyDataItem is a generated payload type.
type SSLFindCertificatesForKeyDataItem struct {
	// The date the certificate was created.
	Created int64 `json:"created"`

	// A list of the domains that the certificate covers.
	Domains []string `json:"domains"`

	// The ECDSA curve that the certificate's key uses.
	//
	// * `prime256v1`
	// * `secp384r1`
	// * `null` — The certificate's key is **not** an ECDSA key.
	//
	// Possible values: `prime256v1`, `secp384r1`.
	EcdsaCurveName *string `json:"ecdsa_curve_name"`

	// The certificate's key's ECDSA compressed public point, in hexadecimal format.
	//
	// * `null` — The certificate's key is **not** an ECDSA key.
	EcdsaPublic *string `json:"ecdsa_public"`

	// The certificate's friendly name.
	FriendlyName string `json:"friendly_name"`

	// The certificate's ID.
	ID string `json:"id"`

	// Whether the certificate is self-signed.
	//
	// * `1` — Self-signed.
	// * `0` — Not self-signed.
	//
	// Possible values: `1`, `0`.
	IsSelfSigned int64 `json:"is_self_signed"`

	// The name that issued the certificate.
	IssuerCommonName string `json:"issuer.commonName"`

	// The certificate's organization name.
	IssuerOrganizationName string `json:"issuer.organizationName"`

	// The certificate's issuer information.
	IssuerText string `json:"issuer_text"`

	// The certificate's key's algorithm.
	//
	// * `rsaEncryption` — RSA.
	// * `id-ecPublicKey` — ECDSA.
	//
	// Possible values: `rsaEncryption`, `id-ecPublicKey`.
	KeyAlgorithm string `json:"key_algorithm"`

	// The certificate's key's modulus, in hexadecimal format.
	//
	// * `null` — The certificate's key is **not** an RSA key.
	Modulus *string `json:"modulus"`

	// The length, in bits, of the certificate's key's modulus.
	//
	// * `null` — The certificate's key is **not** an RSA key.
	ModulusLength *int64 `json:"modulus_length"`

	// The date the certificate expired.
	NotAfter int64 `json:"not_after"`

	// The date the certificate started.
	NotBefore int64 `json:"not_before"`

	// The certificate's signature OID hash algorithm.
	SignatureAlgorithm string `json:"signature_algorithm"`

	// The certificate's Common Name.
	SubjectCommonName string `json:"subject.commonName"`

	// The certificate's subject text information.
	SubjectText string `json:"subject_text"`

	// The certificate's validation type.
	//
	// * `ev` — Extended Validation.
	// * `ov` — Organization Validation.
	// * `dv` — Domain Validation.
	// * `null` — The system could not parse and determine the
	// certificate's validation type.
	//
	// Possible values: `ev`, `ov`, `dv`.
	ValidationType *string `json:"validation_type"`
}

// SSLFindCsrsForKeyArgs are the parameters of the UAPI function `SSL::find_csrs_for_key`.
type SSLFindCsrsForKeyArgs struct {
	// The key's friendly name.
	FriendlyName *string `cpanel:"friendly_name,omitempty"`

	// The key's ID.
	ID *string `cpanel:"id,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// FindCsrsForKey calls the UAPI function `SSL::find_csrs_for_key` — Return private key's certificate signing requests
//
// This function retrieves certificate signing requests (CSR) for
// a private key.
//
// **Note:**
//
// When you call this function, you **must** include either the `id`
// or the `friendly_name` parameter.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/cpanel-account-ssl-management/ssl-find_csrs_for_key.md
func (c *SSLClient) FindCsrsForKey(ctx context.Context, args *SSLFindCsrsForKeyArgs) (*cpanel.UAPIResult[[]SSLFindCsrsForKeyDataItem], error) {
	return cpanel.UAPICall[[]SSLFindCsrsForKeyDataItem](ctx, c.c, http.MethodGet, "SSL", "find_csrs_for_key", args)
}

// SSLFindCsrsForKeyDataItem is a generated payload type.
type SSLFindCsrsForKeyDataItem struct {
	// The CSR's common name.
	CommonName string `json:"commonName"`

	// The date the CSR was created.
	Created int64 `json:"created"`

	// A list of the domains that the CSR covers.
	Domains []string `json:"domains"`

	// The ECDSA curve that the CSR's key uses.
	//
	// * `prime256v1`
	// * `secp384r1`
	// * `null` — The CSR's key is **not** an ECDSA key.
	//
	// Possible values: `prime256v1`, `secp384r1`.
	EcdsaCurveName *string `json:"ecdsa_curve_name"`

	// The CSR's key's ECDSA compressed public point, in hexadecimal format.
	//
	// * `null` — The CSR's key is **not** an ECDSA key.
	EcdsaPublic *string `json:"ecdsa_public"`

	// The CSR's friendly name.
	FriendlyName string `json:"friendly_name"`

	// The CSR's ID.
	ID string `json:"id"`

	// The CSR's key's algorithm.
	//
	// * `rsaEncryption` — RSA.
	// * `id-ecPublicKey` — ECDSA.
	//
	// Possible values: `rsaEncryption`, `id-ecPublicKey`.
	KeyAlgorithm string `json:"key_algorithm"`

	// The CSR's key's modulus, in hexadecimal format.
	//
	// * `null` — The CSR's key is **not** an RSA key.
	Modulus *string `json:"modulus"`
}

// SSLGenerateCertArgs are the parameters of the UAPI function `SSL::generate_cert`.
type SSLGenerateCertArgs struct {
	// The two-letter country code.
	//
	// This parameter is required.
	CountryName string `cpanel:"countryName"`

	// A comma-separated list of domains for which to generate the certificate.
	//
	// This parameter is required.
	Domains string `cpanel:"domains"`

	// The key's ID.
	//
	// This parameter is required.
	KeyID string `cpanel:"key_id"`

	// The certificate's city or locality name.
	//
	// This parameter is required.
	LocalityName string `cpanel:"localityName"`

	// The certificate's Organization Name.
	//
	// This parameter is required.
	OrganizationName string `cpanel:"organizationName"`

	// The two-letter state or locality abbreviation.
	//
	// This parameter is required.
	StateOrProvinceName string `cpanel:"stateOrProvinceName"`

	// An email address to associate with the certificate.
	EmailAddress *string `cpanel:"emailAddress,omitempty"`

	// A friendly name for the new certificate.
	//
	// This parameter defaults to the domain's name for which you generated the
	// certificate.
	FriendlyName *string `cpanel:"friendly_name,omitempty"`

	// The certificate's organizational unit or department name.
	//
	// Defaults to ``.
	OrganizationalUnitName *string `cpanel:"organizationalUnitName,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// GenerateCert calls the UAPI function `SSL::generate_cert` — Create self-signed SSL certificate
//
// This function generates a self-signed SSL certificate.
//
// **Important:**
//
// When you disable the Calendars and Contacts, Receive Mail, Web Disk, Webmail,
// and Web Server [roles](https://go.cpanel.net/howtouseserverprofiles#roles),
// the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/ssl-certificate-management/ssl-generate_cert.md
func (c *SSLClient) GenerateCert(ctx context.Context, args *SSLGenerateCertArgs) (*cpanel.UAPIResult[SSLGenerateCertData], error) {
	return cpanel.UAPICall[SSLGenerateCertData](ctx, c.c, http.MethodGet, "SSL", "generate_cert", args)
}

// SSLGenerateCertData is a generated payload type.
type SSLGenerateCertData struct {
	// The certificate's creation date.
	Created int64 `json:"created"`

	// A list of domains that the certificate covers.
	Domains []string `json:"domains"`

	// The ECDSA curve that the certificate's key uses.
	//
	// * `prime256v1`
	// * `secp384r1`
	// * `null` — The certificate's key is **not** an ECDSA key.
	//
	// Possible values: `prime256v1`, `secp384r1`.
	EcdsaCurveName *string `json:"ecdsa_curve_name"`

	// The certificate's key's ECDSA compressed public point, in hexadecimal format.
	//
	// * null — The certificate's key is **not** an ECDSA key.
	EcdsaPublic *string `json:"ecdsa_public"`

	// The certificate's friendly name.
	FriendlyName string `json:"friendly_name"`

	// The certificate's ID.
	ID string `json:"id"`

	// Whether the certificate is self-signed.
	//
	// * `1` — Self-signed.
	// * `0` — Not self-signed.
	//
	// Possible values: `1`, `0`.
	IsSelfSigned int64 `json:"is_self_signed"`

	// The certificate's Common Name.
	IssuerCommonName string `json:"issuer.commonName"`

	// The certificate's Organization Name.
	IssuerOrganizationName string `json:"issuer.organizationName"`

	// The certificate's key's algorithm.
	//
	// * `rsaEncryption` — RSA.
	// * `id-ecPublicKey` — ECDSA.
	//
	// Possible values: `rsaEncryption`, `id-ecPublicKey`.
	KeyAlgorithm string `json:"key_algorithm"`

	// The certificate's key's modulus, in hexadecimal format.
	//
	// * `null` — The certificate's key is **not** an RSA key.
	Modulus *string `json:"modulus"`

	// The length, in bits, of the certificate's key's modulus.
	//
	// * `null` — The certificate's key is **not** an RSA key.
	ModulusLength *int64 `json:"modulus_length"`

	// The date that the certificate expires.
	NotAfter int64 `json:"not_after"`

	// The certificate's start date.
	NotBefore int64 `json:"not_before"`

	// The certificate's serial number.
	Serial string `json:"serial"`

	// The certificate's OID hash algorithm signature.
	SignatureAlgorithm string `json:"signature_algorithm"`

	// The domain that issued the certificate.
	SubjectCommonName string `json:"subject.commonName"`

	// The certificate's text.
	Text string `json:"text"`

	// The certificate's validation type, if one exists.
	ValidationType *string `json:"validation_type"`
}

// SSLGenerateCsrArgs are the parameters of the UAPI function `SSL::generate_csr`.
type SSLGenerateCsrArgs struct {
	// The two-letter country code.
	//
	// This parameter is required.
	CountryName string `cpanel:"countryName"`

	// A comma-separated list of the domains for which to generate the certificate.
	//
	// This parameter is required.
	Domains string `cpanel:"domains"`

	// The key's ID.
	//
	// This parameter is required.
	KeyID string `cpanel:"key_id"`

	// The certificate's city or locality name.
	//
	// This parameter is required.
	LocalityName string `cpanel:"localityName"`

	// The certificate's organization.
	//
	// This parameter is required.
	OrganizationName string `cpanel:"organizationName"`

	// The certificate's state or locality name.
	//
	// This parameter is required.
	StateOrProvinceName string `cpanel:"stateOrProvinceName"`

	// An email address to associate with the certificate.
	EmailAddress *string `cpanel:"emailAddress,omitempty"`

	// A friendly name for the new certificate.
	//
	// This parameter defaults to the domain name for which you generated the
	// certificate.
	FriendlyName *string `cpanel:"friendly_name,omitempty"`

	// The certificate's organizational unit or department name.
	//
	// Defaults to ``.
	OrganizationalUnitName *string `cpanel:"organizationalUnitName,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// GenerateCsr calls the UAPI function `SSL::generate_csr` — Create certificate signing request
//
// This function generates a certificate signing request (CSR).
//
// **Note:**
//
// This function **requires** a valid key in the account's `ssl` directory.
// You can generate a key with UAPI's `SSL::generate_key` function.
//
// **Important:**
//
// When you disable the Calendars and Contacts, Receive Mail, Web Disk, Webmail,
// and WebServer [roles](https://go.cpanel.net/howtouseserverprofiles#roles),
// the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/ssl-certificate-management/ssl-generate_csr.md
func (c *SSLClient) GenerateCsr(ctx context.Context, args *SSLGenerateCsrArgs) (*cpanel.UAPIResult[SSLGenerateCsrData], error) {
	return cpanel.UAPICall[SSLGenerateCsrData](ctx, c.c, http.MethodGet, "SSL", "generate_csr", args)
}

// SSLGenerateCsrData is a generated payload type.
type SSLGenerateCsrData struct {
	// The name that issued the CSR.
	CommonName string `json:"commonName"`

	// The date the CSR was created.
	Created int64 `json:"created"`

	// The domains that the CSR covers.
	Domains []string `json:"domains"`

	// The ECDSA curve that the CSR's key uses.
	//
	// * `prime256v1`
	// * `secp384r1`
	// * `null` — The CSR's key is **not** an ECDSA key.
	//
	// Possible values: `prime256v1`, `secp384r1`.
	EcdsaCurveName *string `json:"ecdsa_curve_name"`

	// The CSR's key's ECDSA compressed public point, in hexadecimal format.
	//
	// * `null` — The CSR's key is **not** an ECDSA key.
	EcdsaPublic *string `json:"ecdsa_public"`

	// The CSR's friendly name.
	FriendlyName string `json:"friendly_name"`

	// The CSR's ID.
	ID string `json:"id"`

	// The CSR's key's algorithm.
	//
	// * `rsaEncryption` — RSA.
	// * `id-ecPublicKey` — ECDSA.
	//
	// Possible values: `rsaEncryption`, `id-ecPublicKey`.
	KeyAlgorithm string `json:"key_algorithm"`

	// The CSR's key's modulus, in hexadecimal format.
	//
	// * `null` — The CSR's key is **not** an RSA key.
	Modulus *string `json:"modulus"`

	// The CSR's text.
	Text string `json:"text"`
}

// SSLGenerateKeyArgs are the parameters of the UAPI function `SSL::generate_key`.
type SSLGenerateKeyArgs struct {
	// A friendly name for the new key.
	//
	// This parameter defaults to the key's type, creation date, and creation time.
	FriendlyName *string `cpanel:"friendly_name,omitempty"`

	// The key's modulus size.
	//
	// **Note:**
	//
	// Use the `keytype` parameter.
	//
	// Defaults to `2048`.
	Keysize *int64 `cpanel:"keysize,omitempty"`

	// The key's type.
	//
	// * `system` — The system's default value.
	// * `rsa-2048` — 2,408-bit RSA.
	// * `rsa-4096` — 4,096-bit RSA.
	// * `ecdsa-prime256v1` — ECDSA prime256v1 ("P-256").
	// * `ecdsa-secp384r1` — ECDSA secp384r1 ("P-384").
	//
	// This parameter defaults to the user's
	// [default SSL/TLS key type](https://go.cpanel.net/cpaneldocsSSLTLS#default-ssl-tls-key-type).
	//
	// **Note:**
	//
	// If you do **not** use this parameter, the system defaults to the `keysize` parameter's default value. …
	Keytype *string `cpanel:"keytype,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// GenerateKey calls the UAPI function `SSL::generate_key` — Create private key
//
// This function generates a private key.
//
// **Important:**
//
// * You **cannot** call both the `keytype` and `keysize` parameters in a single call.
// * When you disable the Calendars and Contacts, Receive Mail, Web Disk, Webmail, **and**
// Web Server [roles](https://go.cpanel.net/howtouseserverprofiles#roles),
// the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/ssl-certificate-management/ssl-generate_key.md
func (c *SSLClient) GenerateKey(ctx context.Context, args *SSLGenerateKeyArgs) (*cpanel.UAPIResult[SSLGenerateKeyData], error) {
	return cpanel.UAPICall[SSLGenerateKeyData](ctx, c.c, http.MethodGet, "SSL", "generate_key", args)
}

// SSLGenerateKeyData is a generated payload type.
type SSLGenerateKeyData struct {
	// The key's creation date.
	Created int64 `json:"created"`

	// The ECDSA curve that the key uses.
	//
	// * `prime256v1`
	// * `secp384r1`
	// * `null` — The key is **not** an ECDSA key.
	//
	// Possible values: `prime256v1`, `secp384r1`.
	EcdsaCurveName *string `json:"ecdsa_curve_name"`

	// The key's ECDSA compressed public point, in hexadecimal format.
	//
	// * `null` — The key is **not** an ECDSA key.
	EcdsaPublic *string `json:"ecdsa_public"`

	// The key's friendly name.
	FriendlyName string `json:"friendly_name"`

	// The key's ID.
	ID string `json:"id"`

	// The key's algorithm.
	//
	// * `rsaEncryption` — RSA.
	// * `id-ecPublicKey` — ECDSA.
	//
	// Possible values: `rsaEncryption`, `id-ecPublicKey`.
	KeyAlgorithm string `json:"key_algorithm"`

	// The key's modulus, in hexadecimal format.
	//
	// * `null` — The key is **not** an RSA key.
	Modulus *string `json:"modulus"`

	// The length, in bits, of the key's modulus.
	//
	// * `null` — The key is **not** an RSA key.
	ModulusLength *int64 `json:"modulus_length"`

	// The key's contents.
	Text string `json:"text"`
}

// GetAutoSSLExcludedDomains calls the UAPI function `SSL::get_autossl_excluded_domains` — Return AutoSSL disabled domains
//
// This function lists the domains with AutoSSL disabled.
//
// Available since cPanel & WHM version cPanel 66.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/auto-generated-ssl-certificates/ssl-get_autossl_excluded_domains.md
func (c *SSLClient) GetAutoSSLExcludedDomains(ctx context.Context, extra ...cpanel.Args) (*cpanel.UAPIResult[[]SSLGetAutoSSLExcludedDomainsDataItem], error) {
	return cpanel.UAPICall[[]SSLGetAutoSSLExcludedDomainsDataItem](ctx, c.c, http.MethodGet, "SSL", "get_autossl_excluded_domains", cpanel.CombineArgs(extra...))
}

// SSLGetAutoSSLExcludedDomainsDataItem is a generated payload type.
type SSLGetAutoSSLExcludedDomainsDataItem struct {
	// A domain that has AutoSSL disabled.
	ExcludedDomain string `json:"excluded_domain"`
}

// GetAutoSSLProblems calls the UAPI function `SSL::get_autossl_problems` — Return domains with AutoSSL problems
//
// This function retrieves a list of domains that possess AutoSSL problems.
//
// **Important:**
//
// When you disable the [Calendars and Contacts, Receive Mail, Web Disk, Webmail, and Web Server roles](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 68.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/auto-generated-ssl-certificates/ssl-get_autossl_problems.md
func (c *SSLClient) GetAutoSSLProblems(ctx context.Context, extra ...cpanel.Args) (*cpanel.UAPIResult[[]SSLGetAutoSSLProblemsDataItem], error) {
	return cpanel.UAPICall[[]SSLGetAutoSSLProblemsDataItem](ctx, c.c, http.MethodGet, "SSL", "get_autossl_problems", cpanel.CombineArgs(extra...))
}

// SSLGetAutoSSLProblemsDataItem is a generated payload type.
type SSLGetAutoSSLProblemsDataItem struct {
	// The certificate's hostname.
	Domain string `json:"domain"`

	// text description of the problem.
	Problem string `json:"problem"`

	// When the problem occurred.
	Time string `json:"time"`
}

// SSLGetAutoSSLRenewalStatusArgs are the parameters of the UAPI function `SSL::get_autossl_renewal_status`.
type SSLGetAutoSSLRenewalStatusArgs struct {
	// The fully qualified domain name to check.
	//
	// This parameter is required.
	Domain string `cpanel:"domain"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// GetAutoSSLRenewalStatus calls the UAPI function `SSL::get_autossl_renewal_status` — Return AutoSSL renewal status for a domain
//
// This function returns the AutoSSL renewal status for a domain. It indicates whether AutoSSL is active, the domain is excluded, the domain has DCV problems, and whether the certificate will auto-renew.
//
// Available since cPanel & WHM version cPanel 134.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/auto-generated-ssl-certificates/ssl-get_autossl_renewal_status.md
func (c *SSLClient) GetAutoSSLRenewalStatus(ctx context.Context, args *SSLGetAutoSSLRenewalStatusArgs) (*cpanel.UAPIResult[SSLGetAutoSSLRenewalStatusData], error) {
	return cpanel.UAPICall[SSLGetAutoSSLRenewalStatusData](ctx, c.c, http.MethodGet, "SSL", "get_autossl_renewal_status", args)
}

// An object that contains the AutoSSL renewal status for the domain.
type SSLGetAutoSSLRenewalStatusData struct {
	// Whether AutoSSL has DCV failures recorded for this domain. Defaults to 1 if the problems database is unreachable.
	//
	// Possible values: `0`, `1`.
	HasProblems int64 `json:"has_problems"`

	// Whether an AutoSSL provider is configured on the server.
	//
	// Possible values: `0`, `1`.
	IsActive int64 `json:"is_active"`

	// Whether the domain is on the user's AutoSSL exclusion list.
	//
	// Possible values: `0`, `1`.
	IsExcluded int64 `json:"is_excluded"`

	// Whether AutoSSL will auto-renew the certificate for this domain. Only true when the server has an active AutoSSL provider, the current certificate was issued by that provider, the domain is not excluded, and there are no recorded DCV problems.
	//
	// Possible values: `0`, `1`.
	WillRenew int64 `json:"will_renew"`
}

// SSLGetCabundleArgs are the parameters of the UAPI function `SSL::get_cabundle`.
type SSLGetCabundleArgs struct {
	// The certificate's text.
	//
	// This parameter is required.
	Cert string `cpanel:"cert"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// GetCabundle calls the UAPI function `SSL::get_cabundle` — Return certificate's CA bundle and hostname
//
// This function retrieves a certificate's Certificate Authority (CA) bundle and hostname.
//
// **Important:**
//
// When you disable the Calendars and Contacts, Receive Mail, Web Disk, Webmail, and Web Server [roles](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/cpanel-account-ssl-management/ssl-get_cabundle.md
func (c *SSLClient) GetCabundle(ctx context.Context, args *SSLGetCabundleArgs) (*cpanel.UAPIResult[SSLGetCabundleData], error) {
	return cpanel.UAPICall[SSLGetCabundleData](ctx, c.c, http.MethodGet, "SSL", "get_cabundle", args)
}

// SSLGetCabundleData is a generated payload type.
type SSLGetCabundleData struct {
	// The CA bundle's tag.
	Bundle string `json:"bundle"`

	// The CA bundle's contents.
	Cab string `json:"cab"`

	// The certificate's hostname.
	Domain string `json:"domain"`
}

// SSLGetCnNameArgs are the parameters of the UAPI function `SSL::get_cn_name`.
type SSLGetCnNameArgs struct {
	// A domain name, cPanel username, or email address.
	//
	// This parameter is required.
	Domain string `cpanel:"domain"`

	// The service's name.
	//
	// * `cpanel`
	// * `imap`
	// * `pop3`
	// * `smtp`
	//
	// Possible values: `cpanel`, `imap`, `pop3`, `smtp`.
	//
	// This parameter is required.
	Service string `cpanel:"service"`

	// Whether to append `mail` to the `domain` value to find the best match.
	//
	// For example, if you specify the domain `example.com` and call this parameter, the
	// function only searches the `mail.example.com` service domains.
	//
	// * `1` — Append `mail` to the `domain` value during the search.
	// * `0` — Match on the specified `domain` value **only**.
	//
	// Possible values: `1`, `0`.
	//
	// Defaults to `0`.
	AddMailSubdomain *int64 `cpanel:"add_mail_subdomain,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// GetCnName calls the UAPI function `SSL::get_cn_name` — Request best SSL domain for service
//
// This function retrieves the most secure domain for a service.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/cpanel-account-ssl-management/ssl-get_cn_name.md
func (c *SSLClient) GetCnName(ctx context.Context, args *SSLGetCnNameArgs) (*cpanel.UAPIResult[SSLGetCnNameData], error) {
	return cpanel.UAPICall[SSLGetCnNameData](ctx, c.c, http.MethodGet, "SSL", "get_cn_name", args)
}

// SSLGetCnNameData is a generated payload type.
type SSLGetCnNameData struct {
	// The method that the system used to match the certificate with the mail
	// service.
	//
	// * `none` — No domain matches the certificate.
	// * `exact` — The domain exactly matches the certificate.
	// * `exact-wildcard` — The domain exactly matches the certificate.
	// * `mail-wildcard` — The `mail` subdomain of the domain matches the domain of the wildcard certificate.
	// * `www-wildcard` — The `www` subdomain of the dom …
	CertMatchMethod string `json:"cert_match_method"`

	// The certificate's expiration date.
	CertValidNotAfter int64 `json:"cert_valid_not_after"`

	// Whether the certificate is currently valid.
	//
	// * `1` — Valid.
	// * `0` — Invalid.
	//
	// **Note:**
	//
	// If the certificate is valid, the following statements are true:
	//   * The certificate is **not** self-signed (the `ssldomain_matches_cert` value is `1`).
	//   * The certificate matches the SSL domain (the `ssldomain_matches_cert` value is `1`).
	//   * The certificate is within its validity period (the `cert_valid_not_ …
	IsCurrentlyValid int64 `json:"is_currently_valid"`

	// Whether the certificate is self-signed.
	//
	// * `1` — Self-signed.
	// * `0` — Not self-signed.
	//
	// **Note:**
	//
	// If the `cert_match_method` value is `none`, you should ignore this return's value.
	//
	// Possible values: `1`, `0`.
	IsSelfSigned int64 `json:"is_self_signed"`

	// Whether the certificate is a wildcard certificate.
	//
	// * `1` — Wildcard.
	// * `0` — Not a wildcard.
	//
	// Possible values: `1`, `0`.
	IsWildCard int64 `json:"is_wild_card"`

	// The best domain to use to access the service.
	Ssldomain string `json:"ssldomain"`

	// Whether an SSL-protected domain matches the certificate.
	//
	// * `1` — Matches.
	// * `0` — Does **not** match.
	//
	// Possible values: `1`, `0`.
	SsldomainMatchesCert int64 `json:"ssldomain_matches_cert"`
}

// SSLInstallSSLArgs are the parameters of the UAPI function `SSL::install_ssl`.
type SSLInstallSSLArgs struct {
	// The certificate to install.
	//
	// **Note:**
	//
	// * You **must** URI-encode this value.
	// * You can use a Perl command to URI-encode your SSL certificate for this parameter.
	// For example, you can use the following string, where `CERT.FILE` is the SSL certificate file:
	//
	//   ```$(perl -MURI::Escape -ne 'print uri_escape($_);' CERT.FILE)```
	//
	// This parameter is required.
	Cert string `cpanel:"cert"`

	// The domain name.
	//
	// This parameter is required.
	Domain string `cpanel:"domain"`

	// The Certificate Authority (CA) bundle data, if the certificate requires it.
	//
	// **Note:**
	//
	// * You **must** URI-encode this value.
	// * You can use a Perl command to URI-encode your SSL certificate for this parameter.
	// For example, you can use the following string, where `CABUNDLE.FILE` is the SSL certificate file:
	//
	//   ```$(perl -MURI::Escape -ne 'print uri_escape($_);' CABUNDLE.FILE)```
	Cabundle *string `cpanel:"cabundle,omitempty"`

	// A map of each domain to its domain control validation (DCV) method.
	DomainDCVMethod map[string]string `cpanel:"domain_dcv_method,omitempty"`

	// The certificate's key. If you omit it, the system uses the matching key already saved to your account.
	//
	// **Note:**
	//
	// * You **must** URI-encode this value.
	// * You can use a Perl command to URI-encode your SSL certificate for this parameter.
	// For example, you can use the following string, where `KEY.FILE` is the SSL certificate file:
	//
	//   ```$(perl -MURI::Escape -ne 'print uri_escape($_);' KEY.FILE)```
	Key *string `cpanel:"key,omitempty"`

	// The cPanel Store order item ID associated with the certificate.
	OrderItemID *string `cpanel:"order_item_id,omitempty"`

	// The cPanel Store certificate ID that this certificate descends from. The
	// short-lived certificate re-issue workflow sets this so the system can
	// supersede the prior certificate after installation; end users do not
	// normally set it.
	OriginatingCertificateID *string `cpanel:"originating_certificate_id,omitempty"`

	// The expiration of the parent subscription certificate, as a Unix timestamp.
	ParentCertExpiry *int64 `cpanel:"parent_cert_expiry,omitempty"`

	// The virtual host names onto which to install the certificate. To set
	// multiple names, duplicate or increment the parameter name. For example,
	// `vhost_names-1` and `vhost_names-2`.
	VhostNames []string `cpanel:"vhost_names,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// InstallSSL calls the UAPI function `SSL::install_ssl` — Install SSL certificate
//
// This function installs an SSL certificate.
//
// **Note:**
//
// Due to their inherent complexities, SSL-related functions often present problems for third-party developers. For the additional steps required to successfully call this function, read our [Call UAPI's SSL::install_ssl Function in Custom Code](https://go.cpanel.net/tutorial-call-uapis-ssl-install-ssl-function-in-custom-code) documentation.
//
// **Important:**
//
// When you disable the *Calendars and Contacts*, *Receive Mail*, *Web Disk*, *Webmail*, and *Web Server* [roles](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/ssl-certificate-management/ssl-install_ssl.md
func (c *SSLClient) InstallSSL(ctx context.Context, args *SSLInstallSSLArgs) (*cpanel.UAPIResult[SSLInstallSSLData], error) {
	return cpanel.UAPICall[SSLInstallSSLData](ctx, c.c, http.MethodGet, "SSL", "install_ssl", args)
}

// SSLInstallSSLData is a generated payload type.
type SSLInstallSSLData struct {
	// The action that the function used to install the certificate.
	Action string `json:"action"`

	// The certificate ID.
	CertID string `json:"cert_id"`

	// The domain that the certificate covers.
	Domain string `json:"domain"`

	// The domains that require extra certificates for mail and other services.
	ExtraCertificateDomains []string `json:"extra_certificate_domains"`

	// The results, in HTML format.
	HTML string `json:"html"`

	// The domain's IP address.
	IP string `json:"ip"`

	// The key ID.
	KeyID string `json:"key_id"`

	// The results, in text format.
	Message string `json:"message"`

	// The results.
	StatusMsg string `json:"statusmsg"`

	// The domain's owner.
	User string `json:"user"`

	// The domains that the certificate does not cover.
	WarningDomains []string `json:"warning_domains"`

	// The domains that the certificate covers.
	WorkingDomains []string `json:"working_domains"`
}

// SSLInstalledHostArgs are the parameters of the UAPI function `SSL::installed_host`.
type SSLInstalledHostArgs struct {
	// The domain name.
	//
	// **Note:**
	//
	// The parameter defaults to the account's main domain.
	Domain *string `cpanel:"domain,omitempty"`

	// Verify the certificate.
	// * `1` — Verify the certificate.
	// * `0` — Do **not** verify the certificate.
	//
	// Possible values: `0`, `1`.
	//
	// Defaults to `0`.
	VerifyCertificate *int64 `cpanel:"verify_certificate,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// InstalledHost calls the UAPI function `SSL::installed_host` — Return SSL certificate's info for dedicated IP
//
// This function retrieves information about a certificate that is installed on a domain's dedicated IP address.
//
// **Important:**
//
//   - If you do **not** possess a dedicated IP address, this function will **fail**. For non-dedicated IP addresses, use the `SSL::installed_hosts` function.
//   - When you disable the _Calendars and Contacts_, _Receive Mail_, _Web Disk_ , _Webmail_ , **and**  _Web Server_ [roles](https://go.cpanel.net/howtouseserverprofiles#roles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/cpanel-account-ssl-management/ssl-installed_host.md
func (c *SSLClient) InstalledHost(ctx context.Context, args *SSLInstalledHostArgs) (*cpanel.UAPIResult[SSLInstalledHostData], error) {
	return cpanel.UAPICall[SSLInstalledHostData](ctx, c.c, http.MethodGet, "SSL", "installed_host", args)
}

// An object containing the certificate information.
type SSLInstalledHostDataCertificate struct {
	// The domains that the certificate covers.
	Domains []string `json:"domains"`

	// The ECDSA curve that the certificate's key uses.
	//
	// * `prime256v1`
	// * `secp384r1`
	// * `null` — The certificate's key is **not** an ECDSA key.
	//
	// Possible values: `prime256v1`, `secp384r1`.
	EcdsaCurveName *string `json:"ecdsa_curve_name"`

	// The certificate's key's ECDSA compressed public point, in hexadecimal format.
	//
	// * `null` — The certificate's key is **not** an ECDSA key.
	EcdsaPublic *string `json:"ecdsa_public"`

	// The certificate's ID.
	ID string `json:"id"`

	// Whether the certificate is self-signed.
	// * `1` — Self-signed.
	// * `0` — Not self-signed.
	//
	// Possible values: `0`, `1`.
	IsSelfSigned int64 `json:"is_self_signed"`

	// The issuer's Common Name, typically a domain name.
	IssuerCommonName string `json:"issuer.commonName"`

	// The certificate's organization.
	IssuerOrganizationName string `json:"issuer.organizationName"`

	// The X.509 information about the issuer that contains CSR information.
	IssuerText string `json:"issuer_text"`

	// The certificate's key's algorithm.
	//
	//   * `rsaEncryption` — RSA.
	//   * `id-ecPublicKey` — ECDSA.
	//
	// Possible values: `rsaEncryption`, `id-ecPublicKey`.
	KeyAlgorithm string `json:"key_algorithm"`

	// The certificate's key's modulus, in hexadecimal format.
	//
	// * `null` — The certificate's key is **not** an RSA key.
	Modulus *string `json:"modulus"`

	// The length, in bits, of the certificate's key's modulus.
	//
	// * `null` — The certificate's key is **not** an RSA key.
	ModulusLength *int64 `json:"modulus_length"`

	// The certificate's expiration time.
	NotAfter int64 `json:"not_after"`

	// The certificate's start time.
	NotBefore int64 `json:"not_before"`

	// The signature algorithm of the certificate.
	SignatureAlgorithm string `json:"signature_algorithm"`

	// The certificate's Common Name.
	SubjectCommonName string `json:"subject.commonName"`

	// The X.509 information about the certificate's subject that contains CSR information.
	SubjectText string `json:"subject_text"`

	// The certificate's validation type.
	// * `ev` — Extended Validation.
	// * `ov` — Organization Validation.
	// * `dv` — Domain Validation.
	// * `null` — The system could not parse and determine the certificate's validation type.
	//
	// Possible values: `ev`, `ov`, `dv`.
	ValidationType *string `json:"validation_type"`

	// Any errors that exist during the certificate verification process. If there are no errors, this will return an empty string.
	//
	// **Note:**
	//
	// You **must** set the `verify_certificate` parameter to `1` for this return to appear.
	VerifyError string `json:"verify_error"`
}

// SSLInstalledHostData is a generated payload type.
type SSLInstalledHostData struct {
	// An object containing the certificate information.
	Certificate SSLInstalledHostDataCertificate `json:"certificate"`

	// The issuer's hostname.
	Host string `json:"host"`
}

// InstalledHosts calls the UAPI function `SSL::installed_hosts` — Return domains with SSL certificate information
//
// This function retrieves a list of the account's websites, their domains, and certificate information.
//
// **Important:**
//
//	For a dedicated IP address, use the UAPI `SSL::installed_host` function.
//
// **Important:**
//
//	When you disable the *CalendarContact* , *MailReceive* , *WebDisk* , *Webmail* , and  *WebServer* [roles](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/cpanel-account-ssl-management/ssl-installed_hosts.md
func (c *SSLClient) InstalledHosts(ctx context.Context, extra ...cpanel.Args) (*cpanel.UAPIResult[[]SSLInstalledHostsDataItem], error) {
	return cpanel.UAPICall[[]SSLInstalledHostsDataItem](ctx, c.c, http.MethodGet, "SSL", "installed_hosts", cpanel.CombineArgs(extra...))
}

// An object that contains information about each certificate.
type SSLInstalledHostsDataItemCertificate struct {
	// The AutoSSL provider's name.
	AutoSSLProvider string `json:"auto_ssl_provider"`

	// The AutoSSL provider's display name.
	AutoSSLProviderDisplayName string `json:"auto_ssl_provider_display_name"`

	// The domains that the certificate covers.
	Domains []string `json:"domains"`

	// The ECDSA curve that the certificate's key uses.
	//
	// * `prime256v1`
	// * `secp384r1`
	// * `null` — The certificate's key is **not** an ECDSA key.
	//
	// Possible values: `prime256v1`, `secp384r1`.
	EcdsaCurveName *string `json:"ecdsa_curve_name"`

	// The certificate's key's ECDSA compressed public point, in hexadecimal format.
	//
	// * `null` — The certificate's key is **not** an ECDSA key.
	EcdsaPublic *string `json:"ecdsa_public"`

	// The certificate's ID.
	ID string `json:"id"`

	// Whether the AutoSSL service provided the certificate.
	// * `1` - Provided by the AutoSSL service.
	// * `0` - Not provided by the AutoSSL service.
	//
	// Possible values: `0`, `1`.
	IsAutoSSL int64 `json:"is_autossl"`

	// Whether the certificate is self-signed.
	// * `1` - Self-signed.
	// * `0` - Not self-signed.
	//
	// Possible values: `0`, `1`.
	IsSelfSigned int64 `json:"is_self_signed"`

	// The name that issued the certificate.
	IssuerCommonName string `json:"issuer.commonName"`

	// The certificate's organization name.
	IssuerOrganizationName string `json:"issuer.organizationName"`

	// The issuer's X.509 information.
	IssuerText string `json:"issuer_text"`

	// The certificate's key's algorithm.
	//
	// * `rsaEncryption` — RSA.
	// * `id-ecPublicKey` — ECDSA.
	//
	// Possible values: `rsaEncryption`, `id-ecPublicKey`.
	KeyAlgorithm string `json:"key_algorithm"`

	// The certificate's key's modulus, in hexadecimal format.
	//
	// * `null` — The certificate's key is **not** an RSA key.
	Modulus *string `json:"modulus"`

	// The length, in bits, of the certificate's key's modulus.
	//
	// * `null` — The certificate's key is **not** an RSA key.
	ModulusLength *int64 `json:"modulus_length"`

	// When the certificate expired.
	NotAfter int64 `json:"not_after"`

	// When the certificate started.
	NotBefore int64 `json:"not_before"`

	// The signature algorithm of the certificate.
	SignatureAlgorithm string `json:"signature_algorithm"`

	// The certificate's common name.
	SubjectCommonName string `json:"subject.commonName"`

	// The subject's X.509 information.
	SubjectText string `json:"subject_text"`

	// The certificate's validation type.
	//
	// **Note:**
	// * `ev` -  Extended Validation.
	// * `ov` - Organization Validation.
	// * `dv` - Domain Validation.
	// * null - The system could not parse and determine the certificate's validation type.
	//
	// Possible values: `ev`, `ov`, `dv`.
	ValidationType *string `json:"validation_type"`
}

// SSLInstalledHostsDataItem is a generated payload type.
type SSLInstalledHostsDataItem struct {
	// An object that contains information about each certificate.
	Certificate SSLInstalledHostsDataItemCertificate `json:"certificate"`

	// The certificate's text.
	CertificateText string `json:"certificate_text"`

	// The document root of the domain that the certificate covers.
	DocRoot string `json:"docroot"`

	// The domains that the certificate covers.
	Domains []string `json:"domains"`

	// An array of every valid fully qualified domain
	// name (FQDN) on the virtual host, which includes service
	// subdomains (proxy subdomains).
	Fqdns []string `json:"fqdns"`

	// The host's IP address.
	IP string `json:"ip"`

	// Whether the website is primary on the IP address.
	// * `1` - Primary.
	// * `0` - Not primary.
	//
	// Possible values: `0`, `1`.
	IsPrimaryOnIP int64 `json:"is_primary_on_ip"`

	// Whether SNI is active on the domain.
	// * `1` - Active.
	// * `0` - Inactive.
	//
	// Possible values: `0`, `1`.
	MailSniStatus int64 `json:"mail_sni_status"`

	// Whether the website requires SNI to function.
	// * `1` - Requires SNI.
	// * `0` - Does not require SNI.
	//
	// Possible values: `0`, `1`.
	NeedsSni int64 `json:"needs_sni"`

	// The server's hostname.
	Servername string `json:"servername"`
}

// IsAutoSSLCheckInProgress calls the UAPI function `SSL::is_autossl_check_in_progress` — Return whether AutoSSL check in progress
//
// This function verifies whether the `autossl_check` task is in progress for the current user.
//
// **Important:**
//
// When you disable the [Calendars and Contacts, Receive Mail, Web Disk, Webmail, and Web Server roles](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 68.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/auto-generated-ssl-certificates/ssl-is_autossl_check_in_progress.md
func (c *SSLClient) IsAutoSSLCheckInProgress(ctx context.Context, extra ...cpanel.Args) (*cpanel.UAPIResult[int64], error) {
	return cpanel.UAPICall[int64](ctx, c.c, http.MethodGet, "SSL", "is_autossl_check_in_progress", cpanel.CombineArgs(extra...))
}

// IsMailSniSupported calls the UAPI function `SSL::is_mail_sni_supported` — Return whether mail SNI is enabled
//
// This function checks whether the sslinstall feature is enabled.
//
// **Warning:**
//
// Mail SNI is always enabled.
// * Mail SNI is **not** compatible with Webmail and will **not** function for any Webmail connection. Webmail connections use the cPanel service SSL certificate.
// * Functions that enable Mail SNI succeed with a warning that Mail SNI is always enabled.
// * Functions that disable Mail SNI will fail and make no changes.
//
// **Important:**
//
// When you disable the [Calendars and Contacts, Receive Mail, Web Disk, Webmail, and Web Server roles](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.48.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/sni-email-settings/ssl-is_mail_sni_supported.md
func (c *SSLClient) IsMailSniSupported(ctx context.Context, extra ...cpanel.Args) (*cpanel.UAPIResult[int64], error) {
	return cpanel.UAPICall[int64](ctx, c.c, http.MethodGet, "SSL", "is_mail_sni_supported", cpanel.CombineArgs(extra...))
}

// IsSniSupported calls the UAPI function `SSL::is_sni_supported` — Return whether Apache web server supports mail SNI
//
// This function checks whether the Apache web server supports SNI.
//
// **Important:**
//
// When you disable the [Calendars and Contacts, Receive Mail, Web Disk, Webmail, and Web Server roles](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/sni-email-settings/ssl-is_sni_supported.md
func (c *SSLClient) IsSniSupported(ctx context.Context, extra ...cpanel.Args) (*cpanel.UAPIResult[int64], error) {
	return cpanel.UAPICall[int64](ctx, c.c, http.MethodGet, "SSL", "is_sni_supported", cpanel.CombineArgs(extra...))
}

// ListCerts calls the UAPI function `SSL::list_certs` — Return all SSL certificates
//
// This function lists an account's certificates.
//
// **Important:**
//
// When you disable the Calendars and Contacts, Receive Mail, Web Disk, Webmail,
// and Web Server [roles](https://go.cpanel.net/howtouseserverprofiles#roles),
// the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/cpanel-account-ssl-management/ssl-list_certs.md
func (c *SSLClient) ListCerts(ctx context.Context, extra ...cpanel.Args) (*cpanel.UAPIResult[[]SSLListCertsDataItem], error) {
	return cpanel.UAPICall[[]SSLListCertsDataItem](ctx, c.c, http.MethodGet, "SSL", "list_certs", cpanel.CombineArgs(extra...))
}

// SSLListCertsDataItem is a generated payload type.
type SSLListCertsDataItem struct {
	// The date the certificate was created.
	Created int64 `json:"created"`

	// Whether the certificate is installed on the account.
	//
	// * `1` — Installed.
	// * `0` — Not installed.
	//
	// Possible values: `1`, `0`.
	DomainIsConfigured int64 `json:"domain_is_configured"`

	// A list of domains that the certificate covers.
	Domains []string `json:"domains"`

	// The ECDSA curve that the certificate's key uses.
	//
	// * `prime256v1`
	// * `secp384r1`
	// * `null` — The certificate's key is **not** an ECDSA key.
	//
	// Possible values: `prime256v1`, `secp384r1`.
	EcdsaCurveName *string `json:"ecdsa_curve_name"`

	// The certificate's key's ECDSA compressed public point, in hexadecimal format.
	//
	// * `null` — The certificate's key is **not** an ECDSA key.
	EcdsaPublic *string `json:"ecdsa_public"`

	// The certificate's friendly name.
	FriendlyName string `json:"friendly_name"`

	// The certificate's ID.
	ID string `json:"id"`

	// Whether the certificate is self-signed.
	//
	// * `1` — Self-signed.
	// * `0` — Not self-signed.
	//
	// Possible values: `1`, `0`.
	IsSelfSigned int64 `json:"is_self_signed"`

	// The issuer's name.
	IssuerCommonName string `json:"issuer.commonName"`

	// The certificate's organization.
	IssuerOrganizationName string `json:"issuer.organizationName"`

	// The certificate's issuer information.
	IssuerText string `json:"issuer_text"`

	// The certificate's key's algorithm.
	//
	// * `rsaEncryption` — RSA.
	// * `id-ecPublicKey` — ECDSA.
	//
	// Possible values: `rsaEncryption`, `id-ecPublicKey`.
	KeyAlgorithm string `json:"key_algorithm"`

	// The certificate's key's modulus, in hexadecimal format.
	//
	// * `null` — The certificate's key is **not** an RSA key.
	Modulus *string `json:"modulus"`

	// The length, in bits, of the certificate's key's modulus.
	//
	// * `null` — The certificate's key is **not** an RSA key.
	ModulusLength *int64 `json:"modulus_length"`

	// The certificate's expiration date.
	NotAfter int64 `json:"not_after"`

	// The certificate's start date.
	NotBefore int64 `json:"not_before"`

	// The certificate's serial number.
	Serial string `json:"serial"`

	// The OID hash algorithm signature of the certificate.
	SignatureAlgorithm string `json:"signature_algorithm"`

	// The certificate's Common Name (CN).
	SubjectCommonName string `json:"subject.commonName"`

	// The certificate's subject text information.
	SubjectText string `json:"subject_text"`

	// The certificate's validation type.
	//
	// * `ev` — Extended Validation.
	// * `ov` — Organization Validation.
	// * `dv` — Domain Validation.
	// * `null` — The system could not parse and determine the
	// certificate's validation type.
	//
	// Possible values: `ev`, `ov`, `dv`.
	ValidationType *string `json:"validation_type"`
}

// ListCsrs calls the UAPI function `SSL::list_csrs` — Return all certificate signing requests
//
// This function lists an account's certificate signing requests (CSR).
//
// **Important:**
//
// When you disable the Calendars and Contacts, Receive Mail, Web Disk,
// Webmail, and Web Server
// [roles](https://go.cpanel.net/howtouseserverprofiles#roles),
// the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/cpanel-account-ssl-management/ssl-list_csrs.md
func (c *SSLClient) ListCsrs(ctx context.Context, extra ...cpanel.Args) (*cpanel.UAPIResult[[]SSLListCsrsDataItem], error) {
	return cpanel.UAPICall[[]SSLListCsrsDataItem](ctx, c.c, http.MethodGet, "SSL", "list_csrs", cpanel.CombineArgs(extra...))
}

// SSLListCsrsDataItem is a generated payload type.
type SSLListCsrsDataItem struct {
	// The CSR's Common Name or Distinguished Name.
	CommonName string `json:"commonName"`

	// The CSR's creation date.
	Created int64 `json:"created"`

	// A list of the domains that the CSR covers.
	Domains []string `json:"domains"`

	// The ECDSA curve that the CSR's key uses.
	//
	// * `prime256v1`
	// * `secp384r1`
	// * `null` — The CSR's key is **not** an ECDSA key.
	//
	// Possible values: `prime256v1`, `secp384r1`.
	EcdsaCurveName *string `json:"ecdsa_curve_name"`

	// The CSR's key's ECDSA compressed public point, in hexadecimal format.
	//
	// * `null` — The CSR's key is **not** an ECDSA key.
	EcdsaPublic *string `json:"ecdsa_public"`

	// The CSR's friendly name.
	FriendlyName string `json:"friendly_name"`

	// The CSR's ID.
	ID string `json:"id"`

	// The CSR's key's algorithm.
	//
	// * `rsaEncryption` — RSA.
	// * `id-ecPublicKey` — ECDSA.
	//
	// Possible values: `rsaEncryption`, `id-ecPublicKey`.
	KeyAlgorithm string `json:"key_algorithm"`

	// The CSR's key's modulus, in hexadecimal format.
	//
	// * `null` — The CSR's key is **not** an RSA key.
	Modulus *string `json:"modulus"`
}

// ListKeys calls the UAPI function `SSL::list_keys` — Return all private keys
//
// This function lists an account's private keys.
//
// **Important:**
//
// When you disable the Calendars and Contacts, Receive Mail, Web Disk, Webmail,
// and Web Server [roles](https://go.cpanel.net/howtouseserverprofiles#roles),
// the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/cpanel-account-ssl-management/ssl-list_keys.md
func (c *SSLClient) ListKeys(ctx context.Context, extra ...cpanel.Args) (*cpanel.UAPIResult[[]SSLListKeysDataItem], error) {
	return cpanel.UAPICall[[]SSLListKeysDataItem](ctx, c.c, http.MethodGet, "SSL", "list_keys", cpanel.CombineArgs(extra...))
}

// SSLListKeysDataItem is a generated payload type.
type SSLListKeysDataItem struct {
	// The key's creation date.
	Created int64 `json:"created"`

	// The ECDSA curve that the key uses.
	//
	// * `prime256v1`
	// * `secp384r1`
	// * `null` — The key is **not** an ECDSA key.
	//
	// Possible values: `prime256v1`, `secp384r1`.
	EcdsaCurveName *string `json:"ecdsa_curve_name"`

	// The key's ECDSA compressed public point, in hexadecimal format.
	//
	// * `null` — The key is **not** an ECDSA key.
	EcdsaPublic *string `json:"ecdsa_public"`

	// The key's friendly name.
	FriendlyName string `json:"friendly_name"`

	// The key ID.
	ID string `json:"id"`

	// The key's algorithm.
	//
	// * `rsaEncryption` — RSA.
	// * `id-ecPublicKey` — ECDSA.
	//
	// Possible values: `rsaEncryption`, `id-ecPublicKey`.
	KeyAlgorithm string `json:"key_algorithm"`

	// The key's modulus, in hexadecimal format.
	//
	// * `null` — The key is **not** an RSA key.
	Modulus *string `json:"modulus"`

	// The length, in bits, of the key's modulus.
	//
	// * `null` — The key is **not** an RSA key.
	ModulusLength *int64 `json:"modulus_length"`
}

// SSLListSSLItemsArgs are the parameters of the UAPI function `SSL::list_ssl_items`.
type SSLListSSLItemsArgs struct {
	// The domain name or names.
	Domains *string `cpanel:"domains,omitempty"`

	// The SSL item type or types.
	// * `key`
	// * `csr`
	// * `crt`
	//
	// Defaults to `key`.
	Item *string `cpanel:"item,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// ListSSLItems calls the UAPI function `SSL::list_ssl_items` — Return SSL-related items
//
// This function lists SSL-related items on a domain.
//
// **Important:**
//
// When you disable the [Calendars and Contacts, Receive Mail, Web Disk, Webmail, and Web Server roles](https://go.cpanel.com/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/cpanel-account-ssl-management/ssl-list_ssl_items.md
func (c *SSLClient) ListSSLItems(ctx context.Context, args *SSLListSSLItemsArgs) (*cpanel.UAPIResult[[]SSLListSSLItemsDataItem], error) {
	return cpanel.UAPICall[[]SSLListSSLItemsDataItem](ctx, c.c, http.MethodGet, "SSL", "list_ssl_items", args)
}

// SSLListSSLItemsDataItem is a generated payload type.
type SSLListSSLItemsDataItem struct {
	// The hostname.
	Host string `json:"host"`

	// The certificate's ID.
	ID string `json:"id"`

	// The type of SSL item.
	// * `key`
	// * `csr`
	// * `crt`
	//
	// Possible values: `key`, `csr`, `crt`.
	Type2 string `json:"type"`
}

// SSLMailSniStatusArgs are the parameters of the UAPI function `SSL::mail_sni_status`.
type SSLMailSniStatusArgs struct {
	// The account's domain.
	//
	// This parameter is required.
	Domain string `cpanel:"domain"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// MailSniStatus calls the UAPI function `SSL::mail_sni_status` — Return status of domain's SNI mail services
//
// This function retrieves the status of the domain's SNI mail services.
//
// **Warning:**
//
// Mail SNI is **not** compatible with Webmail and will not function for any Webmail connection. Webmail connections use the cPanel service SSL certificate.
//
// **Note:**
//
// Mail SNI is always enabled.
// * Functions that enable Mail SNI succeed with a warning that Mail SNI is always enabled.
// * Functions that disable Mail SNI fail and make no changes.
//
// Available since cPanel & WHM version cPanel 11.48.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/sni-email-settings/ssl-mail_sni_status.md
func (c *SSLClient) MailSniStatus(ctx context.Context, args *SSLMailSniStatusArgs) (*cpanel.UAPIResult[SSLMailSniStatusData], error) {
	return cpanel.UAPICall[SSLMailSniStatusData](ctx, c.c, http.MethodGet, "SSL", "mail_sni_status", args)
}

// SSLMailSniStatusData is a generated payload type.
type SSLMailSniStatusData struct {
	// Whether SNI for mail is enabled.
	// * `1` - SNI is enabled.
	// * `0` - SNI is **not** enabled.
	//
	// Possible values: `0`, `1`.
	Enabled int64 `json:"enabled"`
}

// SSLRebuildMailSniConfigArgs are the parameters of the UAPI function `SSL::rebuild_mail_sni_config`.
type SSLRebuildMailSniConfigArgs struct {
	// Whether to reload the Dovecot service after the system rebuilds
	// the configuration files.
	// * `1` - Reload Dovecot.
	// * `0` - Do **not** reload Dovecot.
	//
	// Possible values: `0`, `1`.
	//
	// Defaults to `1`.
	ReloadDovecot *int64 `cpanel:"reload_dovecot,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// RebuildMailSniConfig calls the UAPI function `SSL::rebuild_mail_sni_config` — Start SNI configuration files rebuild
//
// This function rebuilds the SNI configuration files.
//
// **Note:**
//
// * You **must** run this function after you change the SNI status through the UAPI's `enable_mail_sni` or `disable_mail_sni` functions.
// * Mail SNI is **always** enabled.
//   - Functions that enable Mail SNI succeed with a warning that Mail SNI is always enabled. Functions that disable Mail SNI fail and make no changes.
//   - Functions that disable Mail SNI will fail and make no changes.
//
// **Important:**
//
//	When you disable the _Calendars and Contacts_, _Receive Mail_, _Web Disk_, _Webmail_, **and**  _Web Server_ [roles](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.48.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/sni-email-settings/ssl-rebuild_mail_sni_config.md
func (c *SSLClient) RebuildMailSniConfig(ctx context.Context, args *SSLRebuildMailSniConfigArgs) (*cpanel.UAPIResult[SSLRebuildMailSniConfigData], error) {
	return cpanel.UAPICall[SSLRebuildMailSniConfigData](ctx, c.c, http.MethodGet, "SSL", "rebuild_mail_sni_config", args)
}

// SSLRebuildMailSniConfigData is a generated payload type.
type SSLRebuildMailSniConfigData struct {
	// Whether the system rebuilt the configuration files.
	// * `1` - Configuration files rebuilt.
	// * `0` - Configuration files **not** rebuilt.
	//
	// Possible values: `0`, `1`.
	Success int64 `json:"success"`
}

// Rebuildssldb calls the UAPI function `SSL::rebuildssldb` — Start SSL database rebuild
//
// This function rebuilds the account's SSL database.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/cpanel-account-ssl-management/ssl-rebuildssldb.md
func (c *SSLClient) Rebuildssldb(ctx context.Context, extra ...cpanel.Args) (*cpanel.UAPIResult[json.RawMessage], error) {
	return cpanel.UAPICall[json.RawMessage](ctx, c.c, http.MethodGet, "SSL", "rebuildssldb", cpanel.CombineArgs(extra...))
}

// SSLRemoveAutoSSLExcludedDomainsArgs are the parameters of the UAPI function `SSL::remove_autossl_excluded_domains`.
type SSLRemoveAutoSSLExcludedDomainsArgs struct {
	// Enable AutoSSL for this domain.
	//
	// This parameter is required.
	Domains string `cpanel:"domains"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// RemoveAutoSSLExcludedDomains calls the UAPI function `SSL::remove_autossl_excluded_domains` — Enable AutoSSL for specifed domains
//
// This function enables AutoSSL for the domains that you specify.
//
// **Important:**
//
// When you disable the [Calendars and Contacts, Receive Mail, Web Disk, Webmail, and Web Server roles](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 66.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/auto-generated-ssl-certificates/ssl-remove_autossl_excluded_domains.md
func (c *SSLClient) RemoveAutoSSLExcludedDomains(ctx context.Context, args *SSLRemoveAutoSSLExcludedDomainsArgs) (*cpanel.UAPIResult[json.RawMessage], error) {
	return cpanel.UAPICall[json.RawMessage](ctx, c.c, http.MethodGet, "SSL", "remove_autossl_excluded_domains", args)
}

// SSLSetAutoSSLExcludedDomainsArgs are the parameters of the UAPI function `SSL::set_autossl_excluded_domains`.
type SSLSetAutoSSLExcludedDomainsArgs struct {
	// A comma-separated list of domains for which to disable AutoSSL.
	//
	// **Note:**
	//
	// If you do not include this parameter, the function will **enable** AutoSSL for every domain on the account.
	Domains *string `cpanel:"domains,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// SetAutoSSLExcludedDomains calls the UAPI function `SSL::set_autossl_excluded_domains` — Disable AutoSSL for specifed domains
//
// This function disables AutoSSL for every domain that you specify.
//
// **Warning:**
//
// This function **replaces** the list of any domains that you previously excluded. To add domains to the list of excluded domains, use the UAPI function `SSL::add_autossl_excluded_domains`.
//
// **Important:**
//
// When you disable the the [Calendar and Contacts, Receive Mail, Web Disk, Webmail, and Web Server roles](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 66.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/auto-generated-ssl-certificates/ssl-set_autossl_excluded_domains.md
func (c *SSLClient) SetAutoSSLExcludedDomains(ctx context.Context, args *SSLSetAutoSSLExcludedDomainsArgs) (*cpanel.UAPIResult[json.RawMessage], error) {
	return cpanel.UAPICall[json.RawMessage](ctx, c.c, http.MethodGet, "SSL", "set_autossl_excluded_domains", args)
}

// SSLSetCertFriendlyNameArgs are the parameters of the UAPI function `SSL::set_cert_friendly_name`.
type SSLSetCertFriendlyNameArgs struct {
	// The certificate's friendly name.
	//
	// This parameter is required.
	FriendlyName string `cpanel:"friendly_name"`

	// The certificate's new friendly name.
	//
	// This parameter is required.
	NewFriendlyName string `cpanel:"new_friendly_name"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// SetCertFriendlyName calls the UAPI function `SSL::set_cert_friendly_name` — Update SSL certificate's friendly name
//
// This function changes a certificate's friendly name.
//
// **Important:**
//
// When you disable the [Calendars and Contacts, Receive Mail, Web Disk, Webmail, and Web Server roles](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/ssl-certificate-management/ssl-set_cert_friendly_name.md
func (c *SSLClient) SetCertFriendlyName(ctx context.Context, args *SSLSetCertFriendlyNameArgs) (*cpanel.UAPIResult[json.RawMessage], error) {
	return cpanel.UAPICall[json.RawMessage](ctx, c.c, http.MethodGet, "SSL", "set_cert_friendly_name", args)
}

// SSLSetCsrFriendlyNameArgs are the parameters of the UAPI function `SSL::set_csr_friendly_name`.
type SSLSetCsrFriendlyNameArgs struct {
	// The CSR's friendly name.
	//
	// This parameter is required.
	FriendlyName string `cpanel:"friendly_name"`

	// The CSR's ID.
	//
	// **Note:**
	//
	// To retrieve a CSR's ID, use the UAPI `list_csrs` function.
	//
	// This parameter is required.
	ID string `cpanel:"id"`

	// The CSR's new friendly name.
	//
	// This parameter is required.
	NewFriendlyName string `cpanel:"new_friendly_name"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// SetCsrFriendlyName calls the UAPI function `SSL::set_csr_friendly_name` — Update certificate signing request's friendly name
//
// This function changes a certificate signing request's (CSR) friendly name.
//
// **Important:**
//
// * You **must** call either the `friendly_name` or `id` parameter.
// * When you disable the Calendars and Contacts, Receive Mail, Web Disk, Webmail, and Web Server [roles](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/ssl-certificate-management/ssl-set_csr_friendly_name.md
func (c *SSLClient) SetCsrFriendlyName(ctx context.Context, args *SSLSetCsrFriendlyNameArgs) (*cpanel.UAPIResult[json.RawMessage], error) {
	return cpanel.UAPICall[json.RawMessage](ctx, c.c, http.MethodGet, "SSL", "set_csr_friendly_name", args)
}

// SSLSetDefaultKeyTypeArgs are the parameters of the UAPI function `SSL::set_default_key_type`.
type SSLSetDefaultKeyTypeArgs struct {
	// The key type to set.
	//
	// * `system` — Use the system’s `ssl_default_key_type` value.
	// * `rsa-2048` — 2,048-bit RSA.
	// * `rsa-4096` — 4,096-bit RSA.
	// * `ecdsa-prime256v1` — ECDSA prime256v1 (“P-256”).
	// * `ecdsa-secp384r1` — ECDSA secp384r1 (“P-384”).
	//
	// Possible values: `system`, `rsa-2048`, `rsa-4096`, `ecdsa-prime256v1`, `ecdsa-secp384r1`.
	//
	// This parameter is required.
	Type2 string `cpanel:"type"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// SetDefaultKeyType calls the UAPI function `SSL::set_default_key_type` — Update SSL TLS key type
//
// This function sets a user’s preferred SSL/TLS key type.
//
// Available since cPanel & WHM version cPanel 92.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/cpanel-account-ssl-management/ssl-set_default_key_type.md
func (c *SSLClient) SetDefaultKeyType(ctx context.Context, args *SSLSetDefaultKeyTypeArgs) (*cpanel.UAPIResult[json.RawMessage], error) {
	return cpanel.UAPICall[json.RawMessage](ctx, c.c, http.MethodGet, "SSL", "set_default_key_type", args)
}

// SSLSetKeyFriendlyNameArgs are the parameters of the UAPI function `SSL::set_key_friendly_name`.
type SSLSetKeyFriendlyNameArgs struct {
	// The key's friendly name.
	//
	// This parameter is required.
	FriendlyName string `cpanel:"friendly_name"`

	// The key's ID.
	//
	// This parameter is required.
	ID string `cpanel:"id"`

	// The key's new friendly name.
	//
	// This parameter is required.
	NewFriendlyName string `cpanel:"new_friendly_name"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// SetKeyFriendlyName calls the UAPI function `SSL::set_key_friendly_name` — Update private key's friendly name
//
// This function changes a key's friendly name.
//
// **Important:**
//
// * You **must** call either the `friendly_name` or `id` parameter.
// * When you disable the Calendars and Contacts, Receive Mail, Web Disk, Webmail, and Web Server [roles](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/ssl-certificate-management/ssl-set_key_friendly_name.md
func (c *SSLClient) SetKeyFriendlyName(ctx context.Context, args *SSLSetKeyFriendlyNameArgs) (*cpanel.UAPIResult[json.RawMessage], error) {
	return cpanel.UAPICall[json.RawMessage](ctx, c.c, http.MethodGet, "SSL", "set_key_friendly_name", args)
}

// SSLSetPrimarySSLArgs are the parameters of the UAPI function `SSL::set_primary_ssl`.
type SSLSetPrimarySSLArgs struct {
	// The primary SSL website's servername.
	//
	// This parameter is required.
	Servername string `cpanel:"servername"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// SetPrimarySSL calls the UAPI function `SSL::set_primary_ssl` — Update SSL website for dedicated IP address
//
// This function sets a new primary SSL website for a dedicated IP address.
//
// **Important:**
//
// When you disable the [Calendars and Contacts, Receive Mail, Web Disk, Webmail, and Web Server roles](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/ssl-certificate-management/ssl-set_primary_ssl.md
func (c *SSLClient) SetPrimarySSL(ctx context.Context, args *SSLSetPrimarySSLArgs) (*cpanel.UAPIResult[json.RawMessage], error) {
	return cpanel.UAPICall[json.RawMessage](ctx, c.c, http.MethodGet, "SSL", "set_primary_ssl", args)
}

// SSLShowCertArgs are the parameters of the UAPI function `SSL::show_cert`.
type SSLShowCertArgs struct {
	// The certificate's friendly name.
	FriendlyName *string `cpanel:"friendly_name,omitempty"`

	// The certificate's ID.
	ID *string `cpanel:"id,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// ShowCert calls the UAPI function `SSL::show_cert` — Export SSL certificate
//
// This function retrieves a certificate.
//
// **Note:**
//
// When you call this parameter, you **must** include either the `id` or
// the `friendly_name` parameter.
//
// **Important:**
//
// When you disable the Calendars and Contacts, Receive Mail, Web Disk,
// Webmail, **and** Web Server [roles](https://go.cpanel.net/howtouseserverprofiles#roles),
// the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/ssl-certificate-management/ssl-show_cert.md
func (c *SSLClient) ShowCert(ctx context.Context, args *SSLShowCertArgs) (*cpanel.UAPIResult[SSLShowCertData], error) {
	return cpanel.UAPICall[SSLShowCertData](ctx, c.c, http.MethodGet, "SSL", "show_cert", args)
}

// A object that contains the issuer's details.
type SSLShowCertDataDetailsIssuer struct {
	// The issuer's Common Name or Distinguished Name.
	CommonName string `json:"commonName"`

	// The certificate's two-letter country code.
	CountryName string `json:"countryName"`

	// The issuer's email address.
	EmailAddress string `json:"emailAddress"`

	// The issuer's locality or city.
	LocalityName string `json:"localityName"`

	// The issuer's organization name.
	OrganizationName string `json:"organizationName"`

	// The issuer's state or province name.
	StateOrProvinceName string `json:"stateOrProvinceName"`
}

// An object containing the certificate's ownership details.
type SSLShowCertDataDetailsSubject struct {
	// The certificate's Common Name or Distinguished Name.
	CommonName string `json:"commonName"`

	// The certificate's two-letter country code.
	CountryName string `json:"countryName"`

	// The certificate's email address.
	EmailAddress string `json:"emailAddress"`

	// The certificate's locality or city.
	LocalityName string `json:"localityName"`

	// The certificate's organization name.
	OrganizationName string `json:"organizationName"`

	// The certificate's state or province name.
	StateOrProvinceName string `json:"stateOrProvinceName"`
}

// An object containing the certificate's details.
type SSLShowCertDataDetails struct {
	// A list of the certificate's domains.
	Domains []string `json:"domains"`

	// The ECDSA curve that the certificate's key uses.
	//
	// * `prime256v1`
	// * `secp384r1`
	// * `null` — The certificate's key is **not** an ECDSA key.
	//
	// Possible values: `prime256v1`, `secp384r1`.
	EcdsaCurveName *string `json:"ecdsa_curve_name"`

	// The certificate's key's ECDSA compressed public point, in hexadecimal format.
	//
	// * `null` — The certificate's key is **not** an ECDSA key.
	EcdsaPublic *string `json:"ecdsa_public"`

	// The certificate's friendly name.
	FriendlyName string `json:"friendly_name"`

	// The certificate's ID.
	ID string `json:"id"`

	// Whether the certificate is self-signed.
	//
	// * `1` — Self-signed.
	// * `0` — Not self-signed.
	//
	// Possible values: `1`, `0`.
	IsSelfSigned int64 `json:"is_self_signed"`

	// A object that contains the issuer's details.
	Issuer SSLShowCertDataDetailsIssuer `json:"issuer"`

	// The certificate's key's algorithm.
	//
	// * `rsaEncryption` — RSA.
	// * `id-ecPublicKey` — ECDSA.
	//
	// Possible values: `rsaEncryption`, `id-ecPublicKey`.
	KeyAlgorithm string `json:"key_algorithm"`

	// The certificate's key's modulus, in hexadecimal format.
	//
	// * `null` — The certificate's key is **not** an RSA key.
	Modulus *string `json:"modulus"`

	// The certificate's expiration date.
	NotAfter int64 `json:"not_after"`

	// The certificate's start time.
	NotBefore int64 `json:"not_before"`

	// The certificate's OID hash algorithm signature.
	SignatureAlgorithm string `json:"signature_algorithm"`

	// An object containing the certificate's ownership details.
	Subject SSLShowCertDataDetailsSubject `json:"subject"`

	// The certificate's validation type.
	//
	// * `ev` — Extended Validation.
	// * `ov` — Organization Validation.
	// * `dv` — Domain Validation.
	// * `null` — The system could not parse and determine the
	// certificate's validation type.
	//
	// Possible values: `ev`, `ov`, `dv`.
	ValidationType *string `json:"validation_type"`
}

// SSLShowCertData is a generated payload type.
type SSLShowCertData struct {
	// The contents of the certificate.
	Cert string `json:"cert"`

	// An object containing the certificate's details.
	Details SSLShowCertDataDetails `json:"details"`

	// The parsed information from the OpenSSL command-line tool.
	Text string `json:"text"`
}

// SSLShowCsrArgs are the parameters of the UAPI function `SSL::show_csr`.
type SSLShowCsrArgs struct {
	// The CSR's friendly name.
	FriendlyName *string `cpanel:"friendly_name,omitempty"`

	// The CSR's ID.
	ID *string `cpanel:"id,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// ShowCsr calls the UAPI function `SSL::show_csr` — Export certificate signing request
//
// This function retrieves a certificate signing request (CSR).
//
// **Important:**
//
// When you disable the Calendars and Contacts, Receive Mail, Web Disk, Webmail,
// and Web Server [roles](https://go.cpanel.net/howtouseserverprofiles#roles),
// the system **disables** this function.
//
// **Note:**
//
// When you call this function, you **must** include either the `id`
// or the `friendly_name` parameter.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/ssl-certificate-management/ssl-show_csr.md
func (c *SSLClient) ShowCsr(ctx context.Context, args *SSLShowCsrArgs) (*cpanel.UAPIResult[SSLShowCsrData], error) {
	return cpanel.UAPICall[SSLShowCsrData](ctx, c.c, http.MethodGet, "SSL", "show_csr", args)
}

// An object contaning the CSR's contents.
type SSLShowCsrDataDetails struct {
	// The CSR's Common Name or Distinguished Name.
	CommonName string `json:"commonName"`

	// The CSR's [ISO-3166](https://en.wikipedia.org/wiki/ISO_3166)
	// country code.
	CountryName string `json:"countryName"`

	// The CSR's creation date.
	Created int64 `json:"created"`

	// A list of the domains that the CSR covers.
	Domains []string `json:"domains"`

	// The CSR's email address.
	EmailAddress string `json:"emailAddress"`

	// The CSR's friendly name.
	FriendlyName string `json:"friendly_name"`

	// The CSR's ID.
	ID string `json:"id"`

	// The key algorithm that encrypts the CSR.
	KeyAlgorithm string `json:"key_algorithm"`

	// The certificate's locality or city.
	LocalityName string `json:"localityName"`

	// The CSR's modulus.
	Modulus *string `json:"modulus"`

	// The CSR's organization name.
	OrganizationName string `json:"organizationName"`

	// The CSR's organizational unit name.
	OrganizationalUnitName string `json:"organizationalUnitName"`

	// The CSR's state or province name.
	StateOrProvinceName string `json:"stateOrProvinceName"`
}

// SSLShowCsrData is a generated payload type.
type SSLShowCsrData struct {
	// The CSR's text.
	Csr string `json:"csr"`

	// An object contaning the CSR's contents.
	Details SSLShowCsrDataDetails `json:"details"`

	// The parsed information from the OpenSSL command-line tool.
	Text string `json:"text"`
}

// SSLShowKeyArgs are the parameters of the UAPI function `SSL::show_key`.
type SSLShowKeyArgs struct {
	// The key's friendly name.
	//
	// **Note:**
	//
	// You **must** use either the `id` or the `friendly_name` parameter.
	FriendlyName *string `cpanel:"friendly_name,omitempty"`

	// The key's ID.
	//
	// **Note:**
	//
	// You **must** use either the `id` or the `friendly_name` parameter.
	ID *string `cpanel:"id,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// ShowKey calls the UAPI function `SSL::show_key` — Export private key
//
// This function retrieves a private key.
//
// **Important:**
//
// When you disable the Calendars and Contacts, Receive Mail, Web Disk, Webmail, **and** Web Server [roles](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/ssl-certificate-management/ssl-show_key.md
func (c *SSLClient) ShowKey(ctx context.Context, args *SSLShowKeyArgs) (*cpanel.UAPIResult[SSLShowKeyData], error) {
	return cpanel.UAPICall[SSLShowKeyData](ctx, c.c, http.MethodGet, "SSL", "show_key", args)
}

// An object of the key's details.
type SSLShowKeyDataDetails struct {
	// The key's creation date.
	Created int64 `json:"created"`

	// The ECDSA curve that the key uses.
	//
	// * `prime256v1`
	// * `secp384r1`
	// * `null` — The key is **not** an ECDSA key.
	//
	// Possible values: `prime256v1`, `secp384r1`.
	EcdsaCurveName *string `json:"ecdsa_curve_name"`

	// The key's ECDSA compressed public point, in hexadecimal format.
	//
	// * `null` — The key is **not** an ECDSA key.
	EcdsaPublic *string `json:"ecdsa_public"`

	// The key's friendly name.
	FriendlyName string `json:"friendly_name"`

	// The key's ID.
	ID string `json:"id"`

	// The key's contents.
	Key string `json:"key"`

	// The key's algorithm.
	//
	// * `rsaEncryption` — RSA.
	// * `id-ecPublicKey` — ECDSA.
	//
	// Possible values: `rsaEncryption`, `id-ecPublicKey`.
	KeyAlgorithm string `json:"key_algorithm"`

	// The key's modulus, in hexadecimal format.
	//
	// * `null` — The key is **not** an RSA key.
	Modulus *string `json:"modulus"`

	// The length, in bits, of the key's modulus.
	//
	// * `null` — The key is **not** an RSA key.
	ModulusLength *int64 `json:"modulus_length"`
}

// SSLShowKeyData is a generated payload type.
type SSLShowKeyData struct {
	// An object of the key's details.
	Details SSLShowKeyDataDetails `json:"details"`

	// The raw information from the [OpenSSL](https://www.openssl.org/) command-line tool.
	Text string `json:"text"`
}

// StartAutoSSLCheck calls the UAPI function `SSL::start_autossl_check` — Start AutoSSL for current user
//
// This function initiates an [AutoSSL](https://go.cpanel.net/whmdocsManageAutoSSL) check for the user.
//
// **Important:**
//
// When you disable the Calendars and Contacts, Receive Mail, Web Disk, Webmail, and  Web Server [roles](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 68.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/auto-generated-ssl-certificates/ssl-start_autossl_check.md
func (c *SSLClient) StartAutoSSLCheck(ctx context.Context, extra ...cpanel.Args) (*cpanel.UAPIResult[json.RawMessage], error) {
	return cpanel.UAPICall[json.RawMessage](ctx, c.c, http.MethodGet, "SSL", "start_autossl_check", cpanel.CombineArgs(extra...))
}

// SSLToggleSSLRedirectForDomainsArgs are the parameters of the UAPI function `SSL::toggle_ssl_redirect_for_domains`.
type SSLToggleSSLRedirectForDomainsArgs struct {
	// A comma-separated list of the cPanel account's domains for which to enable or disable secure redirects.
	//
	// **Important:**
	//
	// To enable or disable redirects for addon domains, you **must** pass the addon domain **and** its subdomain.
	//
	// This parameter is required.
	Domains string `cpanel:"domains"`

	// Whether to enable or disable redirects for the specified domains.
	//
	// * `1` — Enable.
	// * `0` — Disable.
	//
	// Possible values: `0`, `1`.
	//
	// This parameter is required.
	State int64 `cpanel:"state"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// ToggleSSLRedirectForDomains calls the UAPI function `SSL::toggle_ssl_redirect_for_domains` — Enable or disable secure redirects
//
// This function enables or disables secure redirects (HTTPS) for the cPanel account's domains that you specify.
//
// **Important:**
//
// To call this function, one of the following conditions **must** exist:
//
// * AutoSSL **must** exist on the domains for which you enable secure redirects.
// * A valid SSL certificate **must** exist for each domain for which you wish to enable secure redirects.
// * You **must** own the domains for which you wish to enable secure redirects.
//
// Available since cPanel & WHM version cPanel 80.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/cpanel-account-ssl-management/ssl-toggle_ssl_redirect_for_domains.md
func (c *SSLClient) ToggleSSLRedirectForDomains(ctx context.Context, args *SSLToggleSSLRedirectForDomainsArgs) (*cpanel.UAPIResult[[]string], error) {
	return cpanel.UAPICall[[]string](ctx, c.c, http.MethodGet, "SSL", "toggle_ssl_redirect_for_domains", args)
}

// SSLUploadCertArgs are the parameters of the UAPI function `SSL::upload_cert`.
type SSLUploadCertArgs struct {
	// The certificate's contents.
	//
	// This parameter is required.
	Crt string `cpanel:"crt"`

	// The certificate's friendly name.
	FriendlyName *string `cpanel:"friendly_name,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// UploadCert calls the UAPI function `SSL::upload_cert` — Import SSL certificate
//
// This function uploads a certificate.
//
// **Important:**
//
// * Due to the limited field length of HTTP GET method calls, you **must** use the HTTP
// POST method. For this reason, you **cannot** use a cPanel or Webmail session URL to
// call this function.
// * When you disable the Calendar and Contacts, Receive Mail, Web Disk, Webmail, and
// Web Server [roles](https://go.cpanel.net/howtouseserverprofiles#roles),
// the system **disables** this function.
//
// This function requires an HTTP POST request.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/ssl-certificate-management/ssl-upload_cert.md
func (c *SSLClient) UploadCert(ctx context.Context, args *SSLUploadCertArgs) (*cpanel.UAPIResult[[]SSLUploadCertDataItem], error) {
	return cpanel.UAPICall[[]SSLUploadCertDataItem](ctx, c.c, http.MethodPost, "SSL", "upload_cert", args)
}

// SSLUploadCertDataItem is a generated payload type.
type SSLUploadCertDataItem struct {
	// The certificate's creation date.
	Created int64 `json:"created"`

	// A list of the certificate's domains.
	Domains []string `json:"domains"`

	// The ECDSA curve that the certificate's key uses.
	//
	// * `prime256v1`
	// * `secp384r1`
	// * `null` — The certificate's key is **not** an ECDSA key.
	//
	// Possible values: `prime256v1`, `secp384r1`.
	EcdsaCurveName *string `json:"ecdsa_curve_name"`

	// The certificate's key's ECDSA compressed public point, in hexadecimal format.
	//
	// * `null` — The certificate's key is **not** an ECDSA key.
	EcdsaPublic *string `json:"ecdsa_public"`

	// The certificate's friendly name.
	FriendlyName string `json:"friendly_name"`

	// The certificate's ID.
	ID string `json:"id"`

	// Whether the certificate is self-signed.
	//
	// * `1` — Self-signed.
	// * `0` — Not self-signed.
	//
	// Possible values: `1`, `0`.
	IsSelfSigned int64 `json:"is_self_signed"`

	// The issuer's Common Name or Distinguished Name.
	IssuerCommonName string `json:"issuer.commonName"`

	// The issuer's Organization Name.
	IssuerOrganizationName string `json:"issuer.organizationName"`

	// The certificate's key's algorithm.
	//
	// * `rsaEncryption` — RSA.
	// * `id-ecPublicKey` — ECDSA.
	//
	// Possible values: `rsaEncryption`, `id-ecPublicKey`.
	KeyAlgorithm string `json:"key_algorithm"`

	// The certificate's key's modulus, in hexadecimal format.
	//
	// * `null` — The certificate's key is **not** an RSA key.
	Modulus *string `json:"modulus"`

	// The length, in bits, of the certificate's key's modulus.
	//
	// * `null` — The certificate's key is **not** an RSA key.
	ModulusLength *int64 `json:"modulus_length"`

	// The certificate's expiration date.
	NotAfter int64 `json:"not_after"`

	// The certificate's start date.
	NotBefore int64 `json:"not_before"`

	// The certificiate's OID hash algorithm signature.
	SignatureAlgorithm string `json:"signature_algorithm"`

	// The issuer's Common Name or Distinguished Name.
	SubjectCommonName string `json:"subject.commonName"`

	// The certificate's validation type.
	//
	// * `ev` — Extended Validation.
	// * `ov` — Organization Validation.
	// * `dv` — Domain Validation.
	// * `null` — The system could not parse and determine
	// the certificate's validation type.
	//
	// Possible values: `ev`, `ov`, `dv`.
	ValidationType *string `json:"validation_type"`
}

// SSLUploadKeyArgs are the parameters of the UAPI function `SSL::upload_key`.
type SSLUploadKeyArgs struct {
	// The key's contents.
	//
	// This parameter is required.
	Key string `cpanel:"key"`

	// The key's friendly name.
	FriendlyName *string `cpanel:"friendly_name,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// UploadKey calls the UAPI function `SSL::upload_key` — Import private key
//
// This function uploads a private key.
//
// **Note:**
//
// Due to the limited field length of HTTP GET method calls, you **must** use
// the HTTP POST method. For this reason, you **cannot** use a cPanel or Webmail
// session URL to call this function.
//
// **Important:**
//
// When you disable the Calendars and Contacts, Receive Mail, Web Disk, Webmail,
// **and** Web Server [roles](https://go.cpanel.net/howtouseserverprofiles#roles),
// the system **disables** this function.
//
// This function requires an HTTP POST request.
//
// Available since cPanel & WHM version cPanel 11.42.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/ssl-certificate-management/ssl-upload_key.md
func (c *SSLClient) UploadKey(ctx context.Context, args *SSLUploadKeyArgs) (*cpanel.UAPIResult[[]SSLUploadKeyDataItem], error) {
	return cpanel.UAPICall[[]SSLUploadKeyDataItem](ctx, c.c, http.MethodPost, "SSL", "upload_key", args)
}

// SSLUploadKeyDataItem is a generated payload type.
type SSLUploadKeyDataItem struct {
	// The key's creation date.
	Created int64 `json:"created"`

	// The ECDSA curve that the key uses.
	//
	// * `prime256v1`
	// * `secp384r1`
	// * `null` — The key is **not** an ECDSA key.
	//
	// Possible values: `prime256v1`, `secp384r1`.
	EcdsaCurveName *string `json:"ecdsa_curve_name"`

	// The key’s ECDSA compressed public point, in hexadecimal format.
	//
	// * `null` — The key is **not** an ECDSA key.
	EcdsaPublic *string `json:"ecdsa_public"`

	// The key's friendly name.
	FriendlyName string `json:"friendly_name"`

	// The key's ID.
	ID string `json:"id"`

	// The key’s algorithm.
	//
	// * `rsaEncryption` — RSA.
	// * `id-ecPublicKey` — ECDSA.
	//
	// Possible values: `rsaEncryption`, `id-ecPublicKey`.
	KeyAlgorithm string `json:"key_algorithm"`

	// The key's modulus, in hexadecimal format.
	//
	// * `null` — The key is **not** an RSA key.
	Modulus *string `json:"modulus"`

	// The length, in bits, of the key's modulus.
	//
	// * `null` — The key is **not** an RSA key.
	ModulusLength *int64 `json:"modulus_length"`
}
