// Code generated by go-cpanel/tools/gen from the official
// cPanel & WHM OpenAPI documents. DO NOT EDIT.
// Source: https://api.docs.cpanel.net (cPanel & WHM version 138)

package uapi

import (
	"context"
	"net/http"

	cpanel "github.com/fmotalleb/go-cpanel"
)

// DNSSECActivateZoneKeyArgs are the parameters of the UAPI function `DNSSEC::activate_zone_key`.
type DNSSECActivateZoneKeyArgs struct {
	// The security key's domain.
	//
	// This parameter is required.
	Domain string `cpanel:"domain"`

	// The security key's ID.
	//
	// This parameter is required.
	KeyID int64 `cpanel:"key_id"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// ActivateZoneKey calls the UAPI function `DNSSEC::activate_zone_key` — Enable DNSSEC security key
//
// This function activates a DNSSEC security key.
//
// **Important:**
//
// When you disable the [DNS role](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 84.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/dns-security/dnssec-activate_zone_key.md
func (c *DNSSECClient) ActivateZoneKey(ctx context.Context, args *DNSSECActivateZoneKeyArgs) (*cpanel.UAPIResult[DNSSECActivateZoneKeyData], error) {
	return cpanel.UAPICall[DNSSECActivateZoneKeyData](ctx, c.c, http.MethodGet, "DNSSEC", "activate_zone_key", args)
}


// DNSSECActivateZoneKeyData is a generated payload type.
type DNSSECActivateZoneKeyData struct {
	// The domain for which the system activated a security key.
	Domain  string `json:"domain"`

	// An error message that describes why the system could not activate the security key.
	//
	// **Note:**
	//
	// The function **only** displays this return when the `success` return is a `0` value.
	Error   string `json:"error"`

	// The security key's ID.
	KeyID   string `json:"key_id"`

	// Whether the system activated the security key.
	// * `1` - Activated.
	// * `0` - The system failed to activate the security key.
	//
	// Possible values: `1`, `0`.
	Success int64 `json:"success"`
}

// DNSSECAddZoneKeyArgs are the parameters of the UAPI function `DNSSEC::add_zone_key`.
type DNSSECAddZoneKeyArgs struct {
	// The algorithm that the system uses to generate the security key.
	//
	// * `5` — RSA/SHA-1
	// * `6` — DSA-NSEC3-SHA1
	// * `7` — RSASHA1-NSEC3-SHA1
	// * `8` — RSA/SHA-256
	// * `10` — RSA/SHA-512
	// * `13` — ECDSA Curve P-256 with SHA-256
	// * `14` — ECDSA Curve P-384 with SHA-384
	//
	// **Note:**
	//
	// We recommend that you use 'ECDSA Curve P-256 with SHA-256' if your registrar supports it.
	//
	// Possible values: `5`, `6`, `7`, `8`, `10`, `13`, `14`.
	//
	// This parameter is required.
	AlgoNum int64 `cpanel:"algo_num"`

	// The domain on which to enable DNSSEC.
	//
	// This parameter is required.
	Domain string `cpanel:"domain"`

	// The type of key to add.
	//
	// * `ksk`
	// * `zsk`
	//
	// Possible values: `ksk`, `zsk`.
	//
	// This parameter is required.
	KeyType string `cpanel:"key_type"`

	// Whether to activate the newly-created key.
	//
	// * `1` — Activate the key.
	// * `0` — Do **not** activate the key.
	//
	// Possible values: `0`, `1`.
	//
	// Defaults to `1`.
	Active *int64 `cpanel:"active,omitempty"`

	// The key's size, in bits.
	//
	// **Note:**
	//
	// For the following `algo_num` and `key_type` parameters, the `key_size` defaults to the following values:
	// * `5` — ksk `2048` zsk `1024`
	// * `6` — ksk `2048` zsk `1024`
	// * `7` — ksk `2048` zsk `1024`
	// * `8` — ksk `2048` zsk `1024`
	// * `10` — ksk `2048` zsk `1024`
	// * `13` — ksk `256` zsk `256`
	// * `14` — ksk `384` zsk `384`
	KeySize *int64 `cpanel:"key_size,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// AddZoneKey calls the UAPI function `DNSSEC::add_zone_key` — Create DNSSEC security key
//
// This function generates a DNSSEC zone key for a domain.
//
// **Note:**
//
// * After you enable DNSSEC on the domain, you **must** add the DS records to your registrar.
// * You **cannot** modify the DNSSEC security key. To make any changes, you **must** disable (and delete) and re-create the DNSSEC security key.
//
// **Important:**
//
// When you disable the [DNS role](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 84.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/dns-security/dnssec-add_zone_key.md
func (c *DNSSECClient) AddZoneKey(ctx context.Context, args *DNSSECAddZoneKeyArgs) (*cpanel.UAPIResult[DNSSECAddZoneKeyData], error) {
	return cpanel.UAPICall[DNSSECAddZoneKeyData](ctx, c.c, http.MethodGet, "DNSSEC", "add_zone_key", args)
}


// DNSSECAddZoneKeyData is a generated payload type.
type DNSSECAddZoneKeyData struct {
	// The domain for which the system added a security key.
	Domain   string `json:"domain"`

	// An error message that describes why the system could not add the security key.
	//
	// **Note:**
	//
	// The function **only** displays this return when the `success` return is a `0` value.
	Error    string `json:"error"`

	// The security key's ID.
	NewKeyID string `json:"new_key_id"`

	// Whether the system added the security key.
	//
	// * `1` — The system added the security key.
	// * `0` — The system failed to add the security key.
	//
	// Possible values: `1`, `0`.
	Success  int64 `json:"success"`
}

// DNSSECDeactivateZoneKeyArgs are the parameters of the UAPI function `DNSSEC::deactivate_zone_key`.
type DNSSECDeactivateZoneKeyArgs struct {
	// The security key's domain.
	//
	// This parameter is required.
	Domain string `cpanel:"domain"`

	// The security key's ID.
	//
	// This parameter is required.
	KeyID int64 `cpanel:"key_id"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// DeactivateZoneKey calls the UAPI function `DNSSEC::deactivate_zone_key` — Disable DNSSEC security key
//
// This function deactivates a DNSSEC security key.
//
// **Important:**
//
// When you disable the [*DNS* role](https://go.cpanel.net/serverroles/), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 84.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/dns-security/dnssec-deactivate_zone_key.md
func (c *DNSSECClient) DeactivateZoneKey(ctx context.Context, args *DNSSECDeactivateZoneKeyArgs) (*cpanel.UAPIResult[DNSSECDeactivateZoneKeyData], error) {
	return cpanel.UAPICall[DNSSECDeactivateZoneKeyData](ctx, c.c, http.MethodGet, "DNSSEC", "deactivate_zone_key", args)
}


// DNSSECDeactivateZoneKeyData is a generated payload type.
type DNSSECDeactivateZoneKeyData struct {
	// The domain for which the system deactivated a security key.
	Domain  string `json:"domain"`

	// An error message that describes why the system could not
	// deactivate the security key.
	//
	// **Note**
	//
	// The function **only** displays this return when the `success` return is a `0` value.
	Error   string `json:"error"`

	// The security key's ID.
	KeyID   string `json:"key_id"`

	// Whether the system deactivated the security key.
	//
	// * `1` - Deactivated.
	// * `0` - The system failed to deactivate the security key.
	//
	// Possible values: `1`, `0`.
	Success int64 `json:"success"`
}

// DNSSECDisableDNSSECArgs are the parameters of the UAPI function `DNSSEC::disable_dnssec`.
type DNSSECDisableDNSSECArgs struct {
	// The domain on which to disable DNSSEC.
	//
	// **Note:**
	//
	//  To enable DNSSEC on multiple domains, increment the parameter name. For example: `domain-0`, `domain-1`, `domain-2`.
	//
	// This parameter is required.
	Domain string `cpanel:"domain"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// DisableDNSSEC calls the UAPI function `DNSSEC::disable_dnssec` — Disable DNSSEC
//
// This function disables DNSSEC on the domain.
//
// **Warning:**
//
//  * This action is **irreversible**. If you disable DNSSEC on the domain, you will lose the associated keys. You can only retrieve the previous state with a full backup.
//  * If you disable DNSSEC, you **must** remove the DNS records at the registrar.
//
// **Important:**
//
//   When you disable the [DNS role](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 60.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/dns-security/dnssec-disable_dnssec.md
func (c *DNSSECClient) DisableDNSSEC(ctx context.Context, args *DNSSECDisableDNSSECArgs) (*cpanel.UAPIResult[DNSSECDisableDNSSECData], error) {
	return cpanel.UAPICall[DNSSECDisableDNSSECData](ctx, c.c, http.MethodGet, "DNSSEC", "disable_dnssec", args)
}


// An array of objects that contain the domains for which the system disabled DNSSEC.
type DNSSECDisableDNSSECDataDisabled struct {
	// Information about the domains for which the system
	// disabled DNSSEC.
	//
	// * `1` — Disabled.
	// * `0` — The system failed to disable DNSSEC.
	//
	// **Note:**
	//
	// This return's name is the `domain` parameter's value.
	//
	// Possible values: `0`, `1`.
	AdditionalProperties int64 `json:"additionalProperties"`
}

// DNSSECDisableDNSSECData is a generated payload type.
type DNSSECDisableDNSSECData struct {
	// An array of objects that contain the domains for which the system disabled DNSSEC.
	Disabled DNSSECDisableDNSSECDataDisabled `json:"disabled"`
}

// DNSSECEnableDNSSECArgs are the parameters of the UAPI function `DNSSEC::enable_dnssec`.
type DNSSECEnableDNSSECArgs struct {
	// The domain on which to enable DNSSEC.
	//
	// **Note:**
	//
	//  To enable DNSSEC on multiple domains, increment the parameter name. For example: `domain-0`, `domain-1`, `domain-2`.
	//
	// This parameter is required.
	Domain string `cpanel:"domain"`

	// Whether to activate the newly-created key.
	//
	// * `1` — Activate the key.
	// * `0` — Do **not** activate the key.
	//
	// Possible values: `0`, `1`.
	//
	// Defaults to `1`.
	Active *int64 `cpanel:"active,omitempty"`

	// The algorithm that the system uses to generate the security key.
	//
	// * `5` — RSA/SHA-1
	// * `6` — DSA-NSEC3-SHA1
	// * `7` — RSASHA1-NSEC3-SHA1
	// * `8` — RSA/SHA-256
	// * `10` — RSA/SHA-512
	// * `13` — ECDSA Curve P-256 with SHA-256
	// * `14` — ECDSA Curve P-384 with SHA-384
	//
	// **Note:**
	//
	//  We recommend that you use `ECDSA Curve P-256 with SHA-256` if your registrar supports it.
	//
	// Defaults to `8`.
	AlgoNum *int64 `cpanel:"algo_num,omitempty"`

	// The manner in which the system creates the security key.
	//
	// * `simple` — Use a single key for both KSK and ZSK. Use this value when the `algo_nom` parameter is greater than `8`.
	// * `classic` — Use separate keys for KSK and ZSK. Use this value when the `algo_nom` parameter is equal to or less than `8`.
	//
	// Possible values: `simple`, `classic`.
	//
	// Defaults to `classic`.
	KeySetup *string `cpanel:"key_setup,omitempty"`

	// The number of times that the system rehashes the first resource record hash operation.
	//
	// A positive integer less than `501`.
	//
	// **Note:**
	//
	// In cPanel & WHM version 132 and earlier, the default value is `7`.
	//
	// Defaults to `0`.
	Nsec3Iterations *int64 `cpanel:"nsec3_iterations,omitempty"`

	// Whether NSEC3 will operate in Narrow or Inclusive mode.
	//
	// In Narrow mode, PowerDNS sends out white lies about the next secure record. Rather than query the resource record in the database, PowerDNS sends the hash plus `1` as the next secure record.
	//
	// * `1` — Narrow mode.
	// * `0` — Inclusive mode.
	//
	// Possible values: `0`, `1`.
	//
	// Defaults to `1`.
	Nsec3Narrow *int64 `cpanel:"nsec3_narrow,omitempty"`

	// Whether the system will create records for all delegations.
	//
	// * `1` — Create records for all delegations.
	// * `0` — Create records only for secure delegations.
	//
	// **Note:**
	//
	//  **Only** select `1` if you **must** create records for all delegations.
	//
	// Possible values: `0`, `1`.
	//
	// Defaults to `0`.
	Nsec3OptOut *int64 `cpanel:"nsec3_opt_out,omitempty"`

	// A hexadecimal string that the system appends to the domain name before it applies the hash function to the name. For more information about the salt value, read the [RFC 5155](https://tools.ietf.org/html/rfc5155#section-3.1.5) documentation.
	//
	// **NOTE:** …
	Nsec3Salt *string `cpanel:"nsec3_salt,omitempty"`

	// Whether the domain will use [Next Secure Record](https://tools.ietf.org/html/rfc4470) (NSEC) or NSEC3 semantics.
	//
	// * `1` — Use NSEC3 semantics.
	// * `0` — Use NSEC semantics.
	//
	// **Note:**
	//
	//  If you use NSEC semantics (`0`), the system ignores the other NSEC3 options.
	//
	// Possible values: `0`, `1`.
	//
	// Defaults to `1`.
	UseNsec3 *int64 `cpanel:"use_nsec3,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// EnableDNSSEC calls the UAPI function `DNSSEC::enable_dnssec` — Enable DNSSEC
//
// This function enables DNSSEC on the domain.
//
// **Note:**
//
//  * After you enable DNSSEC on the domain, you **must** add the DNS records to your registrar.
//  * You **cannot** modify the DNSSEC security key. To make any changes, you **must** disable (and delete) and re-create the DNSSEC security key.
//
// **Important:**
//
//   When you disable the [DNS role](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 60.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/dns-security/dnssec-enable_dnssec.md
func (c *DNSSECClient) EnableDNSSEC(ctx context.Context, args *DNSSECEnableDNSSECArgs) (*cpanel.UAPIResult[DNSSECEnableDNSSECData], error) {
	return cpanel.UAPICall[DNSSECEnableDNSSECData](ctx, c.c, http.MethodGet, "DNSSEC", "enable_dnssec", args)
}


// Information about the domain for which the system
// enabled DNSSEC.
//
// **Note:**
//
// This return's name is the `domain` parameter's value.
type DNSSECEnableDNSSECDataEnabledAdditionalProperties struct {
	// Whether the system enabled DNSSEC.
	//
	// * `1` — The system enabled DNSSEC.
	// * `0` — The system failed to enable DNSSEC.
	//
	// Possible values: `1`, `0`.
	Enabled     int64 `json:"enabled"`

	// The assigned security key ID.
	NewKeyID    int64 `json:"new_key_id"`

	// The version of DNSSEC the system used.
	//
	// * `NSEC3`
	// * `NSEC`
	//
	// Possible values: `NSEC3`, `NSEC`.
	NsecVersion string `json:"nsec_version"`
}

// An object containing information about the domains for which the system enabled DNSSEC.
type DNSSECEnableDNSSECDataEnabled struct {
	// Information about the domain for which the system
	// enabled DNSSEC.
	//
	// **Note:**
	//
	// This return's name is the `domain` parameter's value.
	AdditionalProperties DNSSECEnableDNSSECDataEnabledAdditionalProperties `json:"additionalProperties"`
}

// DNSSECEnableDNSSECData is a generated payload type.
type DNSSECEnableDNSSECData struct {
	// An object containing information about the domains for which the system enabled DNSSEC.
	Enabled DNSSECEnableDNSSECDataEnabled `json:"enabled"`
}

// DNSSECExportZoneDnskeyArgs are the parameters of the UAPI function `DNSSEC::export_zone_dnskey`.
type DNSSECExportZoneDnskeyArgs struct {
	// The domain from which to fetch the DNSKEY record value.
	//
	// This parameter is required.
	Domain string `cpanel:"domain"`

	// The DNSSEC record's ID.
	//
	// This parameter is required.
	KeyID int64 `cpanel:"key_id"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// ExportZoneDnskey calls the UAPI function `DNSSEC::export_zone_dnskey` — Export DNSKEY record value
//
// This function exports a domain's DNSKEY record value.
//
// **Important:**
//
// When you disable the [_DNS_ role](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 88.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/dns-security/dnssec-export_zone_dnskey.md
func (c *DNSSECClient) ExportZoneDnskey(ctx context.Context, args *DNSSECExportZoneDnskeyArgs) (*cpanel.UAPIResult[DNSSECExportZoneDnskeyData], error) {
	return cpanel.UAPICall[DNSSECExportZoneDnskeyData](ctx, c.c, http.MethodGet, "DNSSEC", "export_zone_dnskey", args)
}


// DNSSECExportZoneDnskeyData is a generated payload type.
type DNSSECExportZoneDnskeyData struct {
	// The DNSKEY record value.
	Dnskey  string `json:"dnskey"`

	// The DNSSEC record's ID.
	KeyID   int64 `json:"key_id"`

	// Whether the DNSKEY record exported successfully.
	//
	// * `1` - The system exported the record successfully.
	// * `0` - The system failed to export the record.
	//
	// Possible values: `0`, `1`.
	Success int64 `json:"success"`
}

// DNSSECExportZoneKeyArgs are the parameters of the UAPI function `DNSSEC::export_zone_key`.
type DNSSECExportZoneKeyArgs struct {
	// The security key's domain.
	//
	// This parameter is required.
	Domain string `cpanel:"domain"`

	// The security key's ID.
	//
	// This parameter is required.
	KeyID int64 `cpanel:"key_id"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// ExportZoneKey calls the UAPI function `DNSSEC::export_zone_key` — Export DNSSEC security key
//
// This function exports a DNSSEC security key.
//
// **Important:**
//
// When you disable the [DNS](https://go.cpanel.net/serverroles) role, the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 84.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/dns-security/dnssec-export_zone_key.md
func (c *DNSSECClient) ExportZoneKey(ctx context.Context, args *DNSSECExportZoneKeyArgs) (*cpanel.UAPIResult[DNSSECExportZoneKeyData], error) {
	return cpanel.UAPICall[DNSSECExportZoneKeyData](ctx, c.c, http.MethodGet, "DNSSEC", "export_zone_key", args)
}


// DNSSECExportZoneKeyData is a generated payload type.
type DNSSECExportZoneKeyData struct {
	// The security key's domain.
	Domain     string `json:"domain"`

	// The content of the key, which includes the algorithm.
	KeyContent string `json:"key_content"`

	// The security key's ID.
	KeyID      int64 `json:"key_id"`

	// The security key's internal identifier.
	KeyTag     int64 `json:"key_tag"`

	// The security key's signing type.
	//
	// * `CSK` - Combined Signing Key.
	// * `KSK` - Key Signing Key.
	// * `ZSK` - Zone Signing Key.
	//
	// Possible values: `CSK`, `KSK`, `ZSK`.
	KeyType    string `json:"key_type"`

	// Whether the function succeeded.
	//
	// * `1` - The function succeeded.
	// * `0` - The function failed.
	//
	// Possible values: `0`, `1`.
	Success    int64 `json:"success"`
}

// DNSSECFetchDsRecordsArgs are the parameters of the UAPI function `DNSSEC::fetch_ds_records`.
type DNSSECFetchDsRecordsArgs struct {
	// The domain from which to fetch DS records.
	//
	// **Note:**
	//
	//  To enable DNSSEC on multiple domains, increment the parameter name. For example: `domain-0`, `domain-1`, `domain-2`.
	//
	// This parameter is required.
	Domain string `cpanel:"domain"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// FetchDsRecords calls the UAPI function `DNSSEC::fetch_ds_records` — Return domain's DS records
//
// This function fetches a domain's Delegation of Signing (DS) records.
//
// **Important:**
//
// When you disable the [DNS role](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 82.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/dns-security/dnssec-fetch_ds_records.md
func (c *DNSSECClient) FetchDsRecords(ctx context.Context, args *DNSSECFetchDsRecordsArgs) (*cpanel.UAPIResult[DNSSECFetchDsRecordsData], error) {
	return cpanel.UAPICall[DNSSECFetchDsRecordsData](ctx, c.c, http.MethodGet, "DNSSEC", "fetch_ds_records", args)
}


// DNSSECFetchDsRecordsDataAdditionalPropertiesKeysAdditionalPropertiesDigestsItem is a generated payload type.
type DNSSECFetchDsRecordsDataAdditionalPropertiesKeysAdditionalPropertiesDigestsItem struct {
	// A description of the algorithm
	//  that the DS key uses.
	AlgoDesc string `json:"algo_desc"`

	// A IETF-recognized DNSSEC Algorithm Number.
	AlgoNum  int64 `json:"algo_num"`

	// The actual digest in the DS record.
	Digest   string `json:"digest"`
}

// The information related to the domain's DNSSEC record.
//
// **Note:**
//
// The return's name is the `key_tag` return's integer value.
type DNSSECFetchDsRecordsDataAdditionalPropertiesKeysAdditionalProperties struct {
	// Whether the DS key is active.
	//
	// * `1` - Active.
	// * `0` - Inactive.
	//
	// Possible values: `0`, `1`.
	Active     int64 `json:"active"`

	// A description of the algorithm
	// that the DS key uses.
	AlgoDesc   string `json:"algo_desc"`

	// The algorithm the system generated for the security key.
	//
	// * `5` - RSA/SHA-1
	// * `6` - DSA-NSEC3-SHA1
	// * `7` - RSASHA1-NSEC3-SHA1
	// * `8` - RSA/SHA-256
	// * `10` - RSA/SHA-512
	// * `13` - ECDSA Curve P-256 with SHA-256
	// * `14` - ECDSA Curve P-384 with SHA-384
	//
	// Possible values: `5`, `6`, `7`, `8`, `10`, `13`, `14`.
	AlgoNum    int64 `json:"algo_num"`

	// The short-form reference to the algorithm.
	AlgoTag    string `json:"algo_tag"`

	// The DS key's size, in bits.
	Bits       int64 `json:"bits"`

	// The key's creation time.
	Created    int64 `json:"created"`

	// The information that
	//  the registrar uses to populate the DS records.
	Digests    []DNSSECFetchDsRecordsDataAdditionalPropertiesKeysAdditionalPropertiesDigestsItem `json:"digests"`

	// An integer that determines the `key_type` value.
	//
	// * `256` - A Zone Signing Key (ZSK).
	// * `257` - A Combined Signing Key (CSK) or Key Signing Key (KSK).
	//
	// Possible values: `256`, `257`.
	Flags      int64 `json:"flags"`

	// PowerDNS's internal identifier.
	KeyID      int64 `json:"key_id"`

	// The DS key's identification number.
	KeyTag     int64 `json:"key_tag"`

	// The DS key's signing type.
	// * `CSK` - Combined Signing Key.
	// * `KSK` - Key Signing Key.
	// * `ZSK` - Zone Signing Key.
	//
	// Possible values: `CSK`, `KSK`, `ZSK`.
	KeyType    string `json:"key_type"`

	// The DS key's private key, in ISC format.
	PrivateKey string `json:"privatekey"`
}

// The DS keys on the requested domain.
type DNSSECFetchDsRecordsDataAdditionalPropertiesKeys struct {
	// The information related to the domain's DNSSEC record.
	//
	// **Note:**
	//
	// The return's name is the `key_tag` return's integer value.
	AdditionalProperties DNSSECFetchDsRecordsDataAdditionalPropertiesKeysAdditionalProperties `json:"additionalProperties"`
}

// An object containing the [Next Secure Record](https://tools.ietf.org/html/rfc4470) (NSEC) information for the selected domain.
//
// **Note:**
//
//  If the domain uses NSEC semantics, only the `nsec_version` return appears in the object.
type DNSSECFetchDsRecordsDataAdditionalPropertiesNsecDetails struct {
	// A description of the NSEC3 key's algorithm.
	Nsec3HashAlgoDesc string `json:"nsec3_hash_algo_desc"`

	// The DNSSEC Digest Algorithm Number.
	Nsec3HashAlgoNum  int64 `json:"nsec3_hash_algo_num"`

	// The number of times that the system rehashes the first hash operation.
	Nsec3Iterations   int64 `json:"nsec3_iterations"`

	// Whether NSEC3 will operate in Narrow or Inclusive mode.
	//
	// * `1` - Narrow mode.
	// * `0` - Inclusive mode.
	//
	// **Note:**
	//
	//  For more information about these modes, read [PowerDNS's DNSSEC documentation](https://doc.powerdns.com/authoritative/dnssec/intro.html).
	//
	// Possible values: `0`, `1`.
	Nsec3Narrow       int64 `json:"nsec3_narrow"`

	// Whether NSEC3 will create records for all delegations or only for secure delegations.
	//
	// * `1` - Create records for all delegations.
	// * `0` - Create records only for secure delegations.
	//
	// Possible values: `0`, `1`.
	Nsec3OptOut       int64 `json:"nsec3_opt_out"`

	// The salt value that PowerDNS uses in the hashes.
	//
	// **Note:**
	//
	//  For more information about the salt value, read [RFC 5155](https://tools.ietf.org/html/rfc5155#section-3.1.5).
	Nsec3Salt         string `json:"nsec3_salt"`

	// Whether the domain uses NSEC or [Next Secure Record version 3](https://tools.ietf.org/html/rfc5155) (NSEC3) [Domain Name Security Extensions](https://en.wikipedia.org/wiki/Domain_Name_System_Security_Extensions) (DNSSEC) semantics.
	//
	// * `NSEC`
	// * `NSEC3`
	//
	// Possible values: `NSEC`, `NSEC3`.
	NsecVersion       string `json:"nsec_version"`
}

// An object containing the domain's DS record information.
//
// **Note:**
//
// The return's name is the domain's name.
type DNSSECFetchDsRecordsDataAdditionalProperties struct {
	// The DS keys on the requested domain.
	Keys        DNSSECFetchDsRecordsDataAdditionalPropertiesKeys `json:"keys"`

	// An object containing the [Next Secure Record](https://tools.ietf.org/html/rfc4470) (NSEC) information for the selected domain.
	//
	// **Note:**
	//
	//  If the domain uses NSEC semantics, only the `nsec_version` return appears in the object.
	NsecDetails DNSSECFetchDsRecordsDataAdditionalPropertiesNsecDetails `json:"nsec_details"`
}

// DNSSECFetchDsRecordsData is a generated payload type.
type DNSSECFetchDsRecordsData struct {
	// An object containing the domain's DS record information.
	//
	// **Note:**
	//
	// The return's name is the domain's name.
	AdditionalProperties DNSSECFetchDsRecordsDataAdditionalProperties `json:"additionalProperties"`
}

// DNSSECImportZoneKeyArgs are the parameters of the UAPI function `DNSSEC::import_zone_key`.
type DNSSECImportZoneKeyArgs struct {
	// The security key's domain.
	//
	// This parameter is required.
	Domain string `cpanel:"domain"`

	// The security key data that the [`pdnsutil`](https://doc.powerdns.com/authoritative/manpages/pdnsutil.1.html)
	// utility's `export-zone-key` call returns.
	//
	// This parameter is required.
	KeyData string `cpanel:"key_data"`

	// The security key's type.
	//
	// * `ksk`
	// * `zsk`
	//
	// Possible values: `ksk`, `zsk`.
	//
	// This parameter is required.
	KeyType string `cpanel:"key_type"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// ImportZoneKey calls the UAPI function `DNSSEC::import_zone_key` — Add DNSSEC security key
//
// This function imports a DNSSEC security key.
//
// **Important:**
//
// When you disable the [DNS role](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 84.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/dns-security/dnssec-import_zone_key.md
func (c *DNSSECClient) ImportZoneKey(ctx context.Context, args *DNSSECImportZoneKeyArgs) (*cpanel.UAPIResult[DNSSECImportZoneKeyData], error) {
	return cpanel.UAPICall[DNSSECImportZoneKeyData](ctx, c.c, http.MethodGet, "DNSSEC", "import_zone_key", args)
}


// DNSSECImportZoneKeyData is a generated payload type.
type DNSSECImportZoneKeyData struct {
	// The domain for which the system imported the zone key.
	Domain   string `json:"domain"`

	// An error message that describes why the system could not import the security key.
	//
	// **Note:**
	//
	// The function **only** displays this return when the `success` return is a `0` value.
	Error    string `json:"error"`

	// The security key's ID.
	NewKeyID string `json:"new_key_id"`

	// Whether the system imported the security key.
	//
	// * `1` - The system imported the security key.
	// * `0` - The system **failed** to import the security key.
	//
	// Possible values: `0`, `1`.
	Success  int64 `json:"success"`
}

// DNSSECRemoveZoneKeyArgs are the parameters of the UAPI function `DNSSEC::remove_zone_key`.
type DNSSECRemoveZoneKeyArgs struct {
	// The security key's domain.
	//
	// This parameter is required.
	Domain string `cpanel:"domain"`

	// The security key's ID.
	//
	// This parameter is required.
	KeyID int64 `cpanel:"key_id"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// RemoveZoneKey calls the UAPI function `DNSSEC::remove_zone_key` — Remove DNSSEC security key
//
// This function removes a DNSSEC security key.
//
// **Important:**
//
// When you disable the [DNS role](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 84.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/dns-security/dnssec-remove_zone_key.md
func (c *DNSSECClient) RemoveZoneKey(ctx context.Context, args *DNSSECRemoveZoneKeyArgs) (*cpanel.UAPIResult[DNSSECRemoveZoneKeyData], error) {
	return cpanel.UAPICall[DNSSECRemoveZoneKeyData](ctx, c.c, http.MethodGet, "DNSSEC", "remove_zone_key", args)
}


// DNSSECRemoveZoneKeyData is a generated payload type.
type DNSSECRemoveZoneKeyData struct {
	// The domain for which the system removed a security key.
	Domain  string `json:"domain"`

	// An error message that describes why the system could not remove the security key.
	//
	// **Note:**
	//
	// The function **only** displays this return when the `success` return is a `0` value.
	Error   string `json:"error"`

	// The security key's ID.
	KeyID   string `json:"key_id"`

	// Whether the system removed the security key.
	//
	// * `1` — The system removed the security key.
	// * `0` — The system failed to remove the security key.
	//
	// Possible values: `1`, `0`.
	Success int64 `json:"success"`
}

// DNSSECSetNsec3Args are the parameters of the UAPI function `DNSSEC::set_nsec3`.
type DNSSECSetNsec3Args struct {
	// The domain on which to enable NSEC3 semantics.
	//
	// This parameter is required.
	Domain string `cpanel:"domain"`

	// The number of times that the system re-executes the first resource record hash operation.
	//
	// This parameter is required.
	Nsec3Iterations int64 `cpanel:"nsec3_iterations"`

	// Whether NSEC3 will operate in Narrow mode or Inclusive mode. In Narrow mode, PowerDNS sends out white lies about the next secure record. Rather than query the resource record in the database, PowerDNS sends the hash plus 1 as the next secure record.
	// * `1` - Narrow mode. * `0` - Inclusive mode.
	//
	// Possible values: `0`, `1`.
	//
	// This parameter is required.
	Nsec3Narrow int64 `cpanel:"nsec3_narrow"`

	// Whether the system will create records for all delegations.
	//
	// * `1` - Create records for all delegations.
	// * `0` - Create records **only** for secure delegations.
	//
	// **Note:**
	//
	//   **Only** select `1` if you **must** create records for all delegations.
	//
	// Possible values: `0`, `1`.
	//
	// This parameter is required.
	Nsec3OptOut int64 `cpanel:"nsec3_opt_out"`

	// The salt value that PowerDNS uses in the hashes. For more information about the salt value, read the [RFC 5155 documentation](https://tools.ietf.org/html/rfc5155).
	//
	//  **Note:**
	//
	//  In cPanel & WHM version 132 and later, this parameter also accepts the literal value `-` to indicate no salt value.
	//
	// This parameter is required.
	Nsec3Salt string `cpanel:"nsec3_salt"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// SetNsec3 calls the UAPI function `DNSSEC::set_nsec3` — Update domain to use NSEC3
//
// This function configures the domain to use [Next Secure Record 3](https://tools.ietf.org/html/rfc4470) (NSEC3) semantics.
//
// **Important:**
//
//   When you disable the [DNS role](https://go.cpanel.net/howtouseserverprofiles#roles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 60.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/dns-security/dnssec-set_nsec3.md
func (c *DNSSECClient) SetNsec3(ctx context.Context, args *DNSSECSetNsec3Args) (*cpanel.UAPIResult[DNSSECSetNsec3Data], error) {
	return cpanel.UAPICall[DNSSECSetNsec3Data](ctx, c.c, http.MethodGet, "DNSSEC", "set_nsec3", args)
}


// DNSSECSetNsec3Data is a generated payload type.
type DNSSECSetNsec3Data struct {
	// Contains the domains for which the system enabled NSEC3.
	//
	// **Note:**
	//
	// This return's name is the `domain` parameter's value.
	Enabled map[string]int64 `json:"enabled"`
}

// DNSSECUnsetNsec3Args are the parameters of the UAPI function `DNSSEC::unset_nsec3`.
type DNSSECUnsetNsec3Args struct {
	// The domain on which to disable NSEC3 semantics and use NSEC semantics.
	//
	// This parameter is required.
	Domain string `cpanel:"domain"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// UnsetNsec3 calls the UAPI function `DNSSEC::unset_nsec3` — Update domain to use NSEC
//
// This function configures the domain to use [Next Secure Record](https://tools.ietf.org/html/rfc4470) (NSEC) semantics instead of Next Secure Record 3 (NSEC3) semantics.
//
// **Important:**
//
//   When you disable the [DNS role](https://go.cpanel.net/serverroles), the system **disables** this function.
//
// Available since cPanel & WHM version cPanel 60.
//
// Documentation: https://api.docs.cpanel.net/specifications/cpanel.openapi/dns-security/dnssec-unset_nsec3.md
func (c *DNSSECClient) UnsetNsec3(ctx context.Context, args *DNSSECUnsetNsec3Args) (*cpanel.UAPIResult[DNSSECUnsetNsec3Data], error) {
	return cpanel.UAPICall[DNSSECUnsetNsec3Data](ctx, c.c, http.MethodGet, "DNSSEC", "unset_nsec3", args)
}


// A list of the domains for which the system disabled NSEC3.
type DNSSECUnsetNsec3DataDisabled struct {
	// Whether the system disabled NSEC3 on the domain.
	//
	// * `1` — Disabled.
	// * `0` — The system failed to disable NSEC3.
	//
	// **Note:**
	//
	// This return's name is the `domain` parameter's value.
	//
	// Possible values: `1`, `0`.
	AdditionalProperties int64 `json:"additionalProperties"`
}

// DNSSECUnsetNsec3Data is a generated payload type.
type DNSSECUnsetNsec3Data struct {
	// A list of the domains for which the system disabled NSEC3.
	Disabled DNSSECUnsetNsec3DataDisabled `json:"disabled"`
}
