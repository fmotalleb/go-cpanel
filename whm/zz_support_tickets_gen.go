// Code generated by go-cpanel/tools/gen from the official
// cPanel & WHM OpenAPI documents. DO NOT EDIT.
// Source: https://api.docs.cpanel.net (cPanel & WHM version 138)

package whm

import (
	"context"
	"encoding/json"
	"net/http"

	cpanel "github.com/fmotalleb/go-cpanel"
)

// TicketCreateStubTicket calls the WHM API 1 function `ticket_create_stub_ticket` — Create initial Support ticket request
//
// This function creates a stub ticket. The system uses the stub ticket
// to create a cPanel support ticket.
//
// **Note:**
//
// This function is **not** available through the command line. You **must** call
// it as a request body.
//
// Available since cPanel & WHM version 60.
//
// Documentation: https://api.docs.cpanel.net/specifications/whm.openapi/ticket-management/ticketsupport-ticket_create_stub_ticket.md
func (c *Client) TicketCreateStubTicket(ctx context.Context, extra ...cpanel.Args) (*cpanel.WHMResult[TicketCreateStubTicketData], error) {
	return cpanel.WHMCall[TicketCreateStubTicketData](ctx, c.c, http.MethodGet, "ticket_create_stub_ticket", cpanel.CombineArgs(extra...))
}


// TicketCreateStubTicketData is a generated payload type.
type TicketCreateStubTicketData struct {
	// The support ticket's secure ID token.
	SecureID string `json:"secure_id"`

	// The support ticket number that other functions can look up.
	TicketID int64 `json:"ticket_id"`
}

// TicketGetSupportAgreement calls the WHM API 1 function `ticket_get_support_agreement` — Import Technical Support Agreement text
//
// This function retrieves the WebPros International, LLC Technical Support Agreement text
// and metadata about the user's agreement status from the
// [cPanel Customer Portal](https://tickets.cpanel.net/).
//
// **Note:**
// * This function is **not** available through the command line. You **must** call it
// as a request body.
// * To use this function, you **must** set up a cPanelID token in your current session.
// For more information, read our
// [cPanelID](https://docs.cpanel.net/knowledge-base/accounts/cpanelid/)
// documentation.
//
// Available since cPanel & WHM version 60.
//
// Documentation: https://api.docs.cpanel.net/specifications/whm.openapi/ticket-management/ticketsupport-ticket_get_support_agreement.md
func (c *Client) TicketGetSupportAgreement(ctx context.Context, extra ...cpanel.Args) (*cpanel.WHMResult[TicketGetSupportAgreementData], error) {
	return cpanel.WHMCall[TicketGetSupportAgreementData](ctx, c.c, http.MethodGet, "ticket_get_support_agreement", cpanel.CombineArgs(extra...))
}


// TicketGetSupportAgreementData is a generated payload type.
type TicketGetSupportAgreementData struct {
	// Whether the user associated with the OAuth token has already accepted this
	// version of the agreement.
	//
	// * `1` — Accepted.
	// * `0` — **Not** accepted.
	//
	// Possible values: `0`, `1`.
	Accepted          int64 `json:"accepted"`

	// The [Unix timestamp](https://en.wikipedia.org/wiki/Unix_time)
	// at which the user associated with the OAuth token accepted the
	// Technical Support Agreement.
	//
	// * A valid Unix timestamp.
	// * `null` — Never accepted.
	AcceptedDate      *int64 `json:"accepted_date"`

	// The human-readable date at which the user associated with the OAuth token
	// accepted the Technical Support Agreement.
	//
	// * A human-readable date, in `MM/DD/YY` format.
	// * `null` — Never accepted.
	AcceptedDateHuman *string `json:"accepted_date_human"`

	// The full Technical Support Agreement.
	//
	// **Note:**
	//
	// This returned value may contain HTML.
	Body              string `json:"body"`

	// The creation or publication Unix timestamp of the Technical Support Agreement's current version.
	CreateDate        int64 `json:"create_date"`

	// The human-readable date at which the current version of the Technical Support Agreement was created or published, in `MM/DD/YY` format.
	CreateDateHuman   string `json:"create_date_human"`

	// The URL at which the end-user may download the support agreement.
	DownloadURL       string `json:"download_url"`

	// The Technical Support Agreement's title.
	Title             string `json:"title"`

	// The Technical Support Agreement's version.
	Version           string `json:"version"`

	// The URL at which the end-user may view the Technical Support Agreement agreement as a standalone HTML document.
	ViewURL           string `json:"view_url"`
}

// TicketGetSupportInfo calls the WHM API 1 function `ticket_get_support_info` — Import customer information from Customer Portal
//
// This function retrieves the license holder's support-related information.
//
// **Note:**
//
// * This function is **not** available through the command line. You **must** call
// it as a request body.
// * To use this function, you **must** set up a cPanelID token in your current session.
// For more information, read our
// [cPanelID](https://docs.cpanel.net/knowledge-base/accounts/cpanelid/)
// documentation.
//
// Available since cPanel & WHM version 78.0.6.
//
// Documentation: https://api.docs.cpanel.net/specifications/whm.openapi/ticket-management/ticketsupport-ticket_get_support_info.md
func (c *Client) TicketGetSupportInfo(ctx context.Context, extra ...cpanel.Args) (*cpanel.WHMResult[TicketGetSupportInfoData], error) {
	return cpanel.WHMCall[TicketGetSupportInfoData](ctx, c.c, http.MethodGet, "ticket_get_support_info", cpanel.CombineArgs(extra...))
}


// TicketGetSupportInfoData is a generated payload type.
type TicketGetSupportInfoData struct {
	// The server's CPU architecture retrieved at the last license check.
	Arch                 string `json:"arch"`

	// Whether cPanel Support provides assistance with the server's CPU architecture.
	//
	// * `1` — Yes.
	// * `0` — No.
	// * `-1` — Unknown architecture.
	//
	// Possible values: `1`, `0`, `-1`.
	ArchSupported        int64 `json:"arch_supported"`

	// A number that represents the company with the licensed IP address.
	CompanyID            int64 `json:"company_id"`

	// The company name associated with the submitted IP address.
	CompanyName          string `json:"company_name"`

	// The server's operating system retrieved during the last license check.
	Distro               string `json:"distro"`

	// Whether cPanel Technical Support provides assistance for the server's
	// operating system.
	//
	// * `1` — Yes.
	// * `0` — No.
	// * `-1` — Unknown OS.
	//
	// Possible values: `1`, `0`, `-1`.
	DistroSupported      int64 `json:"distro_supported"`

	// Whether the IP address owner's primary support is WebPros International LLC.
	//
	// * `1` — Receives initial support from WebPros International, LLC.
	// * `0` — Receives initial support from a partner.
	//
	// Possible values: `0`, `1`.
	GetsDirectSupport    int64 `json:"gets_direct_support"`

	// Whether the response contains the company information associated with
	// the licensed IP address.
	//
	// * `1` — Contains company info.
	// * `0` — Does **not** contain company info.
	//
	// Possible values: `0`, `1`.
	HasCompanyInfo       int64 `json:"has_company_info"`

	// Whether the response contains the server's compatibility information.
	//
	// * `1` — Contains compatibility info.
	// * `0` — Does **not** contain compatibility info.
	//
	// Possible values: `0`, `1`.
	HasCompatibilityInfo int64 `json:"has_compatibility_info"`

	// The hostname or IP address of the server, as recorded in the license database.
	Hostname             string `json:"hostname"`

	// The license provider's IP address.
	IP                   string `json:"ip"`

	// The optional URL to the company's brand image file on the partner website.
	LogoURL              string `json:"logo_url"`

	// The URL to the company's general support resources, if available.
	PubSupportContact    string `json:"pub_support_contact"`

	// The optional URL to the company's technical support resources.
	//
	// **Note:**
	//
	// If the `pub_tech_contact` return does not contain a value, the system
	// will use the `pub_support_contact` return's value.
	PubTechContact       string `json:"pub_tech_contact"`

	// The contact email address for the company's technical support.
	TechContactEmail     string `json:"tech_contact_email"`
}

// TicketGrantArgs are the parameters of the WHM API 1 function `ticket_grant`.
type TicketGrantArgs struct {
	// The support ticket's secure ID token.
	//
	// This parameter is required.
	SecureID string `cpanel:"secure_id"`

	// The server's support ticket ID number.
	//
	// This parameter is required.
	ServerNum int64 `cpanel:"server_num"`

	// The support ticket's ID number.
	//
	// This parameter is required.
	TicketID int64 `cpanel:"ticket_id"`

	// The username for incoming SSH connections before they escalate to the `root` user. If you do **not** specify a value, the function retrieves the user from the support ticket.
	SSHUsername *string `cpanel:"ssh_username,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// TicketGrant calls the WHM API 1 function `ticket_grant` — Create Support SSH key
//
// This function installs an SSH key from the
// [cPanel Customer Portal](https://tickets.cpanel.net/).
//
// **Note:**
//
// This function is **not** available through the command line. You **must** call
// it as a request body.
//
// Available since cPanel & WHM version 11.44.
//
// Documentation: https://api.docs.cpanel.net/specifications/whm.openapi/support-access/ticketsupport-ticket_grant.md
func (c *Client) TicketGrant(ctx context.Context, args *TicketGrantArgs) (*cpanel.WHMResult[TicketGrantData], error) {
	return cpanel.WHMCall[TicketGrantData](ctx, c.c, http.MethodGet, "ticket_grant", args)
}


// TicketGrantData is a generated payload type.
type TicketGrantData struct {
	// The installation status.
	AuthStatus         string `json:"auth_status"`

	// When the system authorized access.
	AuthTime           int64 `json:"auth_time"`

	// The status of the
	// [`iptables`](https://netfilter.org/projects/iptables/index.html)
	// whitelist entries.
	//
	// * `ACTIVE` — The system added the appropriate `iptables` whitelist
	// entries or they already existed.
	// * `INACTIVE` — The `iptables` whitelist entries were successfully
	// removed or were already deleted.
	// * `ERR_SETUP` — The system failed to add the `iptables` whitelist
	// entries.
	// * `ERR_UNSETUP` — The s …
	ChainStatus        string `json:"chain_status"`

	// The status of the
	// [ConfigServer Security & Firewall (CSF)](https://www.configserver.com/)
	// whitelist entries.
	//
	// * `ACTIVE` — The system added the appropriate CSF whitelist entries or they
	// already existed.
	// * `INACTIVE` — The CSF whitelist entries were successfully removed or they
	// were already deleted.
	// * `ERR_SETUP` — The system failed to add the CSF whitelist entries.
	// * `ERR_UNSETUP` — The system fai …
	CsfWlStatus        string `json:"csf_wl_status"`

	// The status of the `/etc/hosts.allow` file's whitelist entries.
	//
	// * `ACTIVE` — The system added the appropriate `/etc/hosts.allow` file whitelist
	// entries or they already existed.
	// * `INACTIVE` — The `/etc/hosts.allow` file whitelist entries were successfully
	// removed or they were already deleted.
	// * `ERR_SETUP` — The system failed to add the `/etc/hosts.allow` file whitelist
	// entries.
	// * `ERR_UNSETUP` — …
	HostAccessWlStatus string `json:"host_access_wl_status"`

	// The status of the
	// [cPHulk](https://docs.cpanel.net/whm/security-center/cphulk-brute-force-protection/)
	// whitelist entries.
	//
	// * `ACTIVE` — The system added the appropriate cPHulk whitelist entries or they
	// already existed.
	// * `INACTIVE` — The cPHulk whitelist entries were successfully removed or they
	// were already deleted.
	// * `ERR_SETUP` — The system failed to add the cPHulk whitelist entries.
	// * `ERR_UNS …
	HulkWlStatus       string `json:"hulk_wl_status"`

	// Any non-fatal errors.
	//
	// * `ticket_system_log_entry` — The function could not record the grant
	// operation in the ticket.
	// * `audit_log` — The function could not record the grant operation in the
	// local audit log.
	//
	// **Note:**
	//
	// The system stores the local audit log in the
	// `/var/cpanel/logs/supportauth/audit.log`
	// file.
	NonFatals          []string `json:"non_fatals"`

	// The server's human-readable description from the
	// [cPanel Customer Portal](https://tickets.cpanel.net/).
	ServerName         string `json:"server_name"`

	// The server's ID number.
	ServerNum          int64 `json:"server_num"`

	// The username for incoming SSH connections before they escalate the `root` user.
	SSHUsername        string `json:"ssh_username"`

	// The support ticket's ID number.
	TicketID           int64 `json:"ticket_id"`

	// The support ticket's status.
	//
	// * `OPEN`
	// * `CLOSED`
	// * `UNKNOWN`
	//
	// Possible values: `OPEN`, `CLOSED`, `UNKNOWN`.
	TicketStatus       string `json:"ticket_status"`
}

// TicketListArgs are the parameters of the WHM API 1 function `ticket_list`.
type TicketListArgs struct {
	// The server's ID number.
	//
	// This parameter is required.
	ServerNum int64 `cpanel:"server_num"`

	// The ticket's ID number.
	//
	// This parameter is required.
	TicketID int64 `cpanel:"ticket_id"`

	// The username for incoming SSH connections before they escalate to the `root` user. If you do not specify a value, the function will select the user from the ticket.
	SSHUsername *string `cpanel:"ssh_username,omitempty"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// TicketList calls the WHM API 1 function `ticket_list` — Return Support ticket status
//
// This function lists all active and open support tickets from the
// [cPanel Customer Portal](https://tickets.cpanel.net/).
//
// **Note:**
//
// This function is **not** available through the command line. You **must** call
// it as a request body.
//
// Available since cPanel & WHM version 11.44.
//
// Documentation: https://api.docs.cpanel.net/specifications/whm.openapi/ticket-management/ticketsupport-ticket_list.md
func (c *Client) TicketList(ctx context.Context, args *TicketListArgs) (*cpanel.WHMResult[TicketListData], error) {
	return cpanel.WHMCall[TicketListData](ctx, c.c, http.MethodGet, "ticket_list", args)
}


// TicketListDataAuthsItemServersItem is a generated payload type.
type TicketListDataAuthsItemServersItem struct {
	// The installation status.
	//
	// * `AUTHED`
	// * `NOT_AUTHED`
	// * `EXPIRED`
	//
	// Possible values: `AUTHED`, `NOT_AUTHED`, `EXPIRED`.
	AuthStatus  string `json:"auth_status"`

	// When the grant happened, in
	// [Unix time format](http://en.wikipedia.org/wiki/Unix_time).
	AuthTime    int64 `json:"auth_time"`

	// The server's human-readable description, from the
	// [cPanel Customer Portal](https://tickets.cpanel.net/).
	ServerName  string `json:"server_name"`

	// The server's ID number.
	ServerNum   int64 `json:"server_num"`

	// The server's IP address and port number for SSH connections.
	SSH         string `json:"ssh"`

	// The username for incoming SSH connections before they escalate to the `root` user.
	SSHUsername string `json:"ssh_username"`

	// The server's IP address for WHM connections.
	WHMIP       string `json:"whm_ip"`
}

// TicketListDataAuthsItem is a generated payload type.
type TicketListDataAuthsItem struct {
	// An array of objects containing the server information.
	Servers       []TicketListDataAuthsItemServersItem `json:"servers"`

	// The ticket's ID number.
	TicketID      int64 `json:"ticket_id"`

	// The ticket's status.
	//
	// * `OPEN`
	// * `CLOSED`
	// * `UNKNOWN`
	//
	// Possible values: `OPEN`, `CLOSED`, `UNKNOWN`.
	TicketStatus  string `json:"ticket_status"`

	// The ticket's subject line.
	TicketSubject string `json:"ticket_subject"`
}

// TicketListData is a generated payload type.
type TicketListData struct {
	// An array of objects containing the ticket authorization information.
	Auths     []TicketListDataAuthsItem `json:"auths"`

	// Any non-fatal errors.
	//
	// * `ticket_system_log_entry` — The function could not record the grant
	// operation in the ticket.
	// * `audit_log` — The function could not record the grant operation in the
	// local audit log.
	//
	// **Note:**
	//
	// The system stores the local audit log in the
	// `/var/cpanel/logs/supportauth/audit.log` file.
	NonFatals []string `json:"non_fatals"`
}

// TicketRemoveClosed calls the WHM API 1 function `ticket_remove_closed` — Delete Support SSH key and closed tickets
//
// This function removes cPanel Support's SSH keys and removes closed
// support tickets. You can view closed support tickets in WHM's
// [*Grant cPanel Support Access*](https://docs.cpanel.net/whm/support/grant-cpanel-support-access/)
// interface (*WHM >> Home >> Support >> Grant cPanel Support Access*).
//
// **Note:**
//
// This function is **not** available through the command line. You **must** call
// it as a request body.
//
// Available since cPanel & WHM version 11.44.
//
// Documentation: https://api.docs.cpanel.net/specifications/whm.openapi/support-access/ticketsupport-ticket_remove_closed.md
func (c *Client) TicketRemoveClosed(ctx context.Context, extra ...cpanel.Args) (*cpanel.WHMResult[TicketRemoveClosedData], error) {
	return cpanel.WHMCall[TicketRemoveClosedData](ctx, c.c, http.MethodGet, "ticket_remove_closed", cpanel.CombineArgs(extra...))
}


// TicketRemoveClosedData is a generated payload type.
type TicketRemoveClosedData struct {
	// The status of the `iptables` whitelist entries.
	//
	// * `ACTIVE` — The system added the appropriate `iptables` whitelist entries,
	// or they already existed.
	// * `INACTIVE` — The system successfully removed the `iptables` whitelist
	// entries, or they did not exist.
	// * `ERR_SETUP` — The system failed to add the `iptables` whitelist entries.
	// * `ERR_UNSETUP` — The system failed to remove the `iptables` whitelist …
	ChainStatus        string `json:"chain_status"`

	// The status of the
	// [ConfigServer Security & Firewall (CSF)](https://www.configserver.com/)
	// whitelist entries.
	//
	// * `ACTIVE` — The system added the appropriate CSF whitelist entries or they
	// already existed.
	// * `INACTIVE` — The CSF whitelist entries were successfully removed or they
	// were already deleted.
	// * `ERR_SETUP` — The system failed to add the CSF whitelist entries.
	// * `ERR_UNSETUP` — The system fai …
	CsfWlStatus        string `json:"csf_wl_status"`

	// The status of the `/etc/hosts.allow` file's whitelist entries.
	//
	// * `ACTIVE` — The system added the appropriate `/etc/hosts.allow` file whitelist
	// entries or they already existed.
	// * `INACTIVE` — The `/etc/hosts.allow` file whitelist entries were successfully
	// removed or they were already deleted.
	// * `ERR_SETUP` — The system failed to add the `/etc/hosts.allow` file whitelist
	// entries.
	// * `ERR_UNSETUP` — …
	HostAccessWlStatus string `json:"host_access_wl_status"`

	// The status of the
	// [cPHulk](https://docs.cpanel.net/whm/security-center/cphulk-brute-force-protection/)
	// whitelist entries.
	//
	// * `ACTIVE` — The system added the appropriate cPHulk whitelist entries or they
	// already existed.
	// * `INACTIVE` — The cPHulk whitelist entries were successfully removed or they
	// were already deleted.
	// * `ERR_SETUP` — The system failed to add the cPHulk whitelist entries.
	// * `ERR_UNS …
	HulkWlStatus       string `json:"hulk_wl_status"`

	// Any non-fatal errors.
	//
	// * `ticket_system_log_entry` — The function could not record the grant operation
	// in the ticket.
	// * `audit_log` — The function could not record the grant operation in the local
	// audit log.
	//
	// **Note:**
	//
	// The system stores the local audit log in the
	// `/var/cpanel/logs/supportauth/audit.log` file.
	NonFatals          []string `json:"non_fatals"`

	// The total number of SSH keys that the system removed.
	RevokedKeys        int64 `json:"revoked_keys"`
}

// TicketRevokeArgs are the parameters of the WHM API 1 function `ticket_revoke`.
type TicketRevokeArgs struct {
	// The server's ID number.
	//
	// This parameter is required.
	ServerNum int64 `cpanel:"server_num"`

	// The username for incoming SSH connections before they escalate to the `root` user.
	//
	// This parameter is required.
	SSHUsername string `cpanel:"ssh_username"`

	// The support ticket's ID number.
	//
	// This parameter is required.
	TicketID int64 `cpanel:"ticket_id"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// TicketRevoke calls the WHM API 1 function `ticket_revoke` — Delete Support SSH key
//
// This function removes a
// [cPanel Customer Portal](https://tickets.cpanel.net/)
// SSH key from the server.
//
// **Note:**
//
// This function is **not** available through the command line. You **must** call it
// as a request body.
//
// Available since cPanel & WHM version 11.44.
//
// Documentation: https://api.docs.cpanel.net/specifications/whm.openapi/support-access/ticketsupport-ticket_revoke.md
func (c *Client) TicketRevoke(ctx context.Context, args *TicketRevokeArgs) (*cpanel.WHMResult[TicketRevokeData], error) {
	return cpanel.WHMCall[TicketRevokeData](ctx, c.c, http.MethodGet, "ticket_revoke", args)
}


// TicketRevokeData is a generated payload type.
type TicketRevokeData struct {
	// The status of the
	// [`iptables`](https://netfilter.org/projects/iptables/index.html)
	// whitelist entries.
	//
	// * `ACTIVE` — The system added the appropriate whitelist entries or they
	// already existed.
	// * `INACTIVE` — The whitelist entries were successfully removed or were
	// already deleted.
	// * `ERR_SETUP` — The system failed to add the whitelist entries.
	// * `ERR_UNSETUP` — The system failed to remove the whitel …
	ChainStatus        string `json:"chain_status"`

	// The status of the
	// [ConfigServer Security & Firewall (CSF)](https://www.configserver.com/)
	// whitelist entries.
	//
	// * `ACTIVE` — The system added the appropriate CSF whitelist entries or they
	// already existed.
	// * `INACTIVE` — The CSF whitelist entries were successfully removed or they
	// were already deleted.
	// * `ERR_SETUP` — The system failed to add the CSF whitelist entries.
	// * `ERR_UNSETUP` — The system fai …
	CsfWlStatus        string `json:"csf_wl_status"`

	// The status of the `/etc/hosts.allow` file's whitelist
	// entries.
	//
	// * `ACTIVE` — The system added the appropriate `/etc/hosts.allow` file whitelist
	// entries or they already existed.
	// * `INACTIVE` — The `/etc/hosts.allow` file whitelist entries were successfully
	// removed or they were already deleted.
	// * `ERR_SETUP` — The system failed to add the `/etc/hosts.allow` file whitelist entries.
	// * `ERR_UNSETUP` — …
	HostAccessWlStatus string `json:"host_access_wl_status"`

	// The status of the
	// [cPHulk](https://docs.cpanel.net/whm/security-center/cphulk-brute-force-protection/)
	// whitelist entries.
	//
	// * `ACTIVE` — The system added the appropriate cPHulk whitelist entries or they
	// already existed.
	// * `INACTIVE` — The cPHulk whitelist entries were successfully removed or they
	// were already deleted.
	// * `ERR_SETUP` — The system failed to add the cPHulk whitelist entries.
	// * `ERR_UNS …
	HulkWlStatus       string `json:"hulk_wl_status"`

	// Any non-fatal errors.
	//
	// * `ticket_system_log_entry` — The function could not record the grant operation
	// in the ticket.
	// * `audit_log` — The function could not record the grant operation in the local
	// audit log.
	//
	// **Note:**
	//
	// The system stores the local audit log in the
	// `/var/cpanel/logs/supportauth/audit.log` file.
	NonFatals          []string `json:"non_fatals"`

	// The total number of revoked SSH keys.
	RevokedKeys        int64 `json:"revoked_keys"`

	// The server's ID number.
	ServerNum          int64 `json:"server_num"`

	// The username for incoming SSH connections before they escalate to the `root` user.
	SSHUsername        string `json:"ssh_username"`

	// The revoked support ticket's ID number.
	TicketID           int64 `json:"ticket_id"`
}

// TicketSSHTestArgs are the parameters of the WHM API 1 function `ticket_ssh_test`.
type TicketSSHTestArgs struct {
	// The server's ID number.
	//
	// This parameter is required.
	ServerNum int64 `cpanel:"server_num"`

	// The ticket's ID number.
	//
	// This parameter is required.
	TicketID int64 `cpanel:"ticket_id"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// TicketSSHTest calls the WHM API 1 function `ticket_ssh_test` — Validate Customer Portal connection
//
// This function verifies the connection from the
// [cPanel Customer Portal](https://tickets.cpanel.net/)
// to the server.
//
// **Note:**
//
// This function is **not** available through the command line. You **must** call it as
// a request body.
//
// Available since cPanel & WHM version 11.44.
//
// Documentation: https://api.docs.cpanel.net/specifications/whm.openapi/support-access/ticketsupport-ticket_ssh_test.md
func (c *Client) TicketSSHTest(ctx context.Context, args *TicketSSHTestArgs) (*cpanel.WHMResult[TicketSSHTestData], error) {
	return cpanel.WHMCall[TicketSSHTestData](ctx, c.c, http.MethodGet, "ticket_ssh_test", args)
}


// TicketSSHTestData is a generated payload type.
type TicketSSHTestData struct {
	// A list of non-fatal errors, if any exist.
	//
	// * `ticket_system_log_entry` — The function could not record the grant operation
	// in the ticket.
	// * `audit_log` — The function could not record the grant operation in the local
	// audit log. The system stores the local audit log in the `/var/cpanel/logs/supportauth/audit.log`
	// file.
	NonFatals []string `json:"non_fatals"`

	// The SSH test's result.
	Result    string `json:"result"`
}

// TicketSSHTestStartArgs are the parameters of the WHM API 1 function `ticket_ssh_test_start`.
type TicketSSHTestStartArgs struct {
	// An integer that represents the ticket's actionable server. Increment the value by one for each server in the support ticket.
	//
	// This parameter is required.
	ServerNum int64 `cpanel:"server_num"`

	// The support ticket number that other functions can use to query or make additional changes to the support ticket.
	//
	// This parameter is required.
	TicketID int64 `cpanel:"ticket_id"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// TicketSSHTestStart calls the WHM API 1 function `ticket_ssh_test_start` — Validate Support SSH connection
//
// This function initiates an SSH connection test.
//
// **Important:**
//
// This function is **not** available through the command line. You **must** call it
// as a request body.
//
// Available since cPanel & WHM version 60.
//
// Documentation: https://api.docs.cpanel.net/specifications/whm.openapi/support-access/ticketsupport-ticket_ssh_test_start.md
func (c *Client) TicketSSHTestStart(ctx context.Context, args *TicketSSHTestStartArgs) (*cpanel.WHMResult[json.RawMessage], error) {
	return cpanel.WHMCall[json.RawMessage](ctx, c.c, http.MethodGet, "ticket_ssh_test_start", args)
}


// TicketUpdateServiceAgreementApprovalArgs are the parameters of the WHM API 1 function `ticket_update_service_agreement_approval`.
type TicketUpdateServiceAgreementApprovalArgs struct {
	// The version of the TSA that the user approved.
	//
	// This parameter is required.
	Version string `cpanel:"version"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// TicketUpdateServiceAgreementApproval calls the WHM API 1 function `ticket_update_service_agreement_approval` — Enable Technical Support Agreement acceptance
//
// This function records a user's acceptance of the Technical Support
// Agreement (TSA). The cPanel Customer Portal stores each record with the user's
// login, the date and time, and the TSA's version.
//
// **Note:**
//
// This function is **not** available through the command line.
// You **must** call it as a request body.
//
// Available since cPanel & WHM version 60.
//
// Documentation: https://api.docs.cpanel.net/specifications/whm.openapi/ticket-management/ticketsupport-ticket_update_service_agreement_approval.md
func (c *Client) TicketUpdateServiceAgreementApproval(ctx context.Context, args *TicketUpdateServiceAgreementApprovalArgs) (*cpanel.WHMResult[json.RawMessage], error) {
	return cpanel.WHMCall[json.RawMessage](ctx, c.c, http.MethodGet, "ticket_update_service_agreement_approval", args)
}


// TicketValidateOauth2CodeArgs are the parameters of the WHM API 1 function `ticket_validate_oauth2_code`.
type TicketValidateOauth2CodeArgs struct {
	// The code from the OAuth2 redirect. The system validates this code and exchanges it for a token.
	//
	// This parameter is required.
	Code string `cpanel:"code"`

	// The redirect URI query argument that the system sent to the initial OAuth2 authentication endpoint.
	//
	// This parameter is required.
	RedirectUri string `cpanel:"redirect_uri"`

	// Extra carries any additional arguments (e.g. UAPI/WHM meta arguments such as api.filter.*, api.sort.*, api.paginate.*).
	Extra cpanel.Args `cpanel:"-"`
}

// TicketValidateOauth2Code calls the WHM API 1 function `ticket_validate_oauth2_code` — Validate Customer Portal OAuth2 code
//
// This function validates the OAuth2 code from the
// [cPanel Customer Portal](https://tickets.cpanel.net/).
// After the function validates the token, the system stores it on the current session.
//
// **Note:**
//
// This function is **not** available through the command line. You **must** call it
// as a request body.
//
// Available since cPanel & WHM version 60.
//
// Documentation: https://api.docs.cpanel.net/specifications/whm.openapi/ticket-management/ticketsupport-ticket_validate_oauth2_code.md
func (c *Client) TicketValidateOauth2Code(ctx context.Context, args *TicketValidateOauth2CodeArgs) (*cpanel.WHMResult[json.RawMessage], error) {
	return cpanel.WHMCall[json.RawMessage](ctx, c.c, http.MethodGet, "ticket_validate_oauth2_code", args)
}


// TicketWhitelistCheck calls the WHM API 1 function `ticket_whitelist_check` — Validate Support IP addresses on firewall
//
// This function checks whether the server's firewall whitelist correlates
// with the granted support tickets. You can view granted support tickets in
// WHM's
// [*Grant cPanel Support Access*](https://docs.cpanel.net/whm/support/grant-cpanel-support-access/)
// interface (*WHM >> Home >> Support >> Grant cPanel Support Access*).
//
// **Note:**
//
// This function is **not** available through the command line. You **must** call it as
// a request body.
//
// Available since cPanel & WHM version 11.44.
//
// Documentation: https://api.docs.cpanel.net/specifications/whm.openapi/support-access/ticketsupport-ticket_whitelist_check.md
func (c *Client) TicketWhitelistCheck(ctx context.Context, extra ...cpanel.Args) (*cpanel.WHMResult[TicketWhitelistCheckData], error) {
	return cpanel.WHMCall[TicketWhitelistCheckData](ctx, c.c, http.MethodGet, "ticket_whitelist_check", cpanel.CombineArgs(extra...))
}


// TicketWhitelistCheckData is a generated payload type.
type TicketWhitelistCheckData struct {
	// The status of the server's firewall whitelist.
	//
	// * `ACTIVE` — The whitelist is active.
	// * `INACTIVE` — The whitelist is inactive.
	//
	// Possible values: `ACTIVE`, `INACTIVE`.
	ChainStatus string `json:"chain_status"`

	// message that indicates whether a problem exists with the current status.
	// * `NO` — The firewall does not require changes.
	// * `NEED_SETUP` — You have granted cPanel Support access, but the system's
	// firewall whitelist does not contain support's IP addresses.
	// * `NEED_UNSETUP` — The system detected that **no** cPanel Support access
	// grants exist, but the system's firewall whitelist contains support's IP …
	Problem     string `json:"problem"`
}

// TicketWhitelistSetup calls the WHM API 1 function `ticket_whitelist_setup` — Add Support IP addresses to firewall
//
// This function adds cPanel Support's IP addresses to your server's firewall
// whitelist. This function is firewall-specific and does **not** change the
// [cPHulk](https://docs.cpanel.net/whm/security-center/cphulk-brute-force-protection/)
// whitelist.
//
// **Note:**
//
// * This function is **not** available through the command line. You **must** call it as
// a request body.
// * This function logs error messages to the
// [`/usr/local/cpanel/logs/error_log`](https://docs.cpanel.net/knowledge-base/cpanel-product/the-cpanel-log-files/#usr-local-cpanel-logs-error-log)
// file.
//
// Available since cPanel & WHM version 11.44.
//
// Documentation: https://api.docs.cpanel.net/specifications/whm.openapi/support-access/ticketsupport-ticket_whitelist_setup.md
func (c *Client) TicketWhitelistSetup(ctx context.Context, extra ...cpanel.Args) (*cpanel.WHMResult[TicketWhitelistSetupData], error) {
	return cpanel.WHMCall[TicketWhitelistSetupData](ctx, c.c, http.MethodGet, "ticket_whitelist_setup", cpanel.CombineArgs(extra...))
}


// TicketWhitelistSetupData is a generated payload type.
type TicketWhitelistSetupData struct {
	// A message that indicates whether the whitelist setup succeeded.
	//
	// * `ACTIVE` — A successful setup.
	// * `ERR_SETUP` — An error occurred.
	//
	// Possible values: `ACTIVE`, `ERR_SETUP`.
	ChainStatus string `json:"chain_status"`
}

// TicketWhitelistUnsetup calls the WHM API 1 function `ticket_whitelist_unsetup` — Remove Support IP addresses from firewall
//
// This function removes cPanel Support's IP addresses from your server's firewall
// whitelist. This function is firewall-specific and does **not** change the
// [cPHulk](https://docs.cpanel.net/whm/security-center/cphulk-brute-force-protection/)
// whitelist.
//
// **Note:**
//
// * This function is **not** available through the command line. You **must** call it as
// a request body.
// * This function logs error messages to the
// [`/usr/local/cpanel/logs/error_log`](https://docs.cpanel.net/knowledge-base/cpanel-product/the-cpanel-log-files/#usr-local-cpanel-logs-error-log)
// file.
//
// Available since cPanel & WHM version 11.44.
//
// Documentation: https://api.docs.cpanel.net/specifications/whm.openapi/support-access/ticketsupport-ticket_whitelist_unsetup.md
func (c *Client) TicketWhitelistUnsetup(ctx context.Context, extra ...cpanel.Args) (*cpanel.WHMResult[TicketWhitelistUnsetupData], error) {
	return cpanel.WHMCall[TicketWhitelistUnsetupData](ctx, c.c, http.MethodGet, "ticket_whitelist_unsetup", cpanel.CombineArgs(extra...))
}


// TicketWhitelistUnsetupData is a generated payload type.
type TicketWhitelistUnsetupData struct {
	// A message that indicates whether the whitelist removal succeeded.
	//
	// * `INACTIVE` — Success.
	// * `ERR_UNSETUP` — An error occurred.
	//
	// Possible values: `INACTIVE`, `ERR_UNSETUP`.
	ChainStatus string `json:"chain_status"`
}
